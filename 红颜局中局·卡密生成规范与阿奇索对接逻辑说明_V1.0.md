# 《红颜局中局·卡密生成规范与阿奇索对接逻辑说明_V1.0》

**项目名称：** 红颜局中局
**文档版本：** V1.0
**日期：** 2026-06-08
**作者：** Manus AI

---

## 1. 综述

根据 Claude 提供的最新信息，我们已确认采用“阿奇索发货助手”作为链路 B 的发货载体，并明确了卡密（Token）的生成规范。本说明旨在固化卡密的生成逻辑、解析规则以及与阿奇索插件的配合流程，确保 6 月 14 日彩排时核销链路的顺畅。

---

## 2. 卡密 (Token) 生成算法规范

为降低用户输入错误率并确保唯一性，我将按照以下规则生成卡密池：

### 2.1 字符集定义 (排除易混淆字符)
*   **可用数字**：`2, 3, 4, 5, 6, 7, 8, 9` (排除 0, 1)
*   **可用字母**：`A, B, C, D, E, F, G, H, J, K, M, N, P, Q, R, S, T, U, V, W, X, Y, Z` (排除 I, L, O)

### 2.2 格式与长度
*   **结构**：`XXXX-XXXX-XXXX-XXXX` (4组，每组4位)
*   **示例**：`RFZJ-A3K7-M9P2-X4Q8`
*   **生成量**：首批预生成 10,000 个，存储于数据库 `tokens` 表。

---

## 3. 链路 B (卡密核销) 业务逻辑

### 3.1 数据表结构 (简述)
| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `token_id` | String | 唯一卡密字符串 (如 RFZJ-...) |
| `player_id` | Int | 关联选手 ID |
| `points` | Int | 对应人气值 (如 19900) |
| `status` | Enum | `unused` (未使用), `used` (已使用) |
| `used_at` | DateTime | 核销时间 |
| `user_id` | String | 核销用户抖音 ID |

### 3.2 核销流程
1.  **用户输入**：用户在小程序输入框粘贴/输入卡密。
2.  **后端校验**：
    *   校验 Token 是否存在且为 `unused` 状态。
    *   校验 Token 是否过期（如有）。
3.  **权益释放**：
    *   立即为关联选手增加 `points`。
    *   **展示数字写真**：返回该 Token 绑定的唯一图片 URL（一次性展示）。
4.  **状态标记**：将 Token 标记为 `used`，记录核销人及时间。

---

## 4. 与阿奇索插件的对接 SOP

1.  **导出卡密**：我将从数据库导出包含 `token_id` 的纯文本文件。
2.  **导入阿奇索**：由 John 将卡密文件导入阿奇索插件的“有卡密发货”库中。
3.  **挂机运行**：John 需指定一名工作人员，确保一台电脑 24 小时运行阿奇索客户端。
4.  **发货触发**：用户付款后，阿奇索自动从库中取出一个 `token_id` 发送给用户。

---

## 5. 蓝军建议

*   **防爆破机制**：由于卡密格式相对固定，为防止恶意猜测（爆破），小程序端必须实现“**单位时间内错误输入次数限制**”（如：5分钟内连续错误3次，锁定输入10分钟）。
*   **批量生成脚本**：我将在 Phase 1 完成该脚本，John 可随时要求我导出指定金额（如 19.9元、49.9元）的卡密包。

---

**John，卡密规范已闭环。我将把这些逻辑写入 V2.1 功能定义文档。请确认是否还有其他格式要求？**
