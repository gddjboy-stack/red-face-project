# 《红颜局中局》墨刀与 AI 协同开发指南

> **版本：** V1.0  
> **适用对象：** 彬少（设计师）、John（项目经理）、Manus AI（开发）、Claude AI（审查）

---

## 1. 为什么选择墨刀？（评估结论）

墨刀（Modao）不仅是原型工具，其 **D2C (Design to Code)** 能力是连接彬少视觉设计与 Manus AI 代码实现的关键桥梁。

*   **模块化优势**：墨刀内置大量小程序组件库（如 NutUI、支付宝/微信组件），彬少可以像搭积木一样快速构建页面。
*   **AI 友好度**：墨刀支持导出 **CSS、HTML、React、Vue** 甚至 **Uniapp** 代码片段，这让我（Manus）可以直接读取视觉参数，实现 100% 视觉还原。
*   **标注自动化**：提供悬停即得的智能标注，我无需向彬少询问间距、颜色、字体，直接在标注稿中提取。

---

## 2. 协同开发流程设计

为确保开发效率，建议采用以下“三步走”协同流程：

### 第一步：彬少输出（视觉定稿）
*   **任务**：彬少在墨刀中完成 UI 设计，并确保使用了“组件化”逻辑（如：统一定义按钮样式、导航栏等）。
*   **交付**：彬少将墨刀项目的 **“分享链接”**（需开启“开发者模式”权限）发给 John。

### 第二步：Manus AI 接收（代码实现）
*   **任务**：我（Manus）访问墨刀链接，通过其 **D2C 功能** 提取以下信息：
    *   **布局逻辑**：Flex 布局参数、组件层级。
    *   **视觉资产**：一键导出 @2x/@3x 切图，直接放入小程序资源目录。
    *   **代码片段**：复制 CSS 样式和 HTML 结构，转化为抖音小程序的 `.ttml` 和 `.ttss` 代码。
*   **优势**：极大减少了手动写样式的时间，确保 UI 与设计稿一致。

### 第三步：Claude AI 审查（质量把控）
*   **任务**：我将生成的 UI 代码上传 GitHub，Claude 访问 GitHub 检查：
    *   **代码规范性**：是否符合抖音小程序开发标准。
    *   **交互逻辑**：按钮点击、页面跳转是否与墨刀原型一致。

---

## 3. 给彬少的“协作小贴士”

为了让 AI 开发更顺畅，请彬少在设计时留意：
1.  **命名规范**：在墨刀图层中，尽量给关键元素命名（如 `btn_vote`, `list_player`），这样我生成的代码变量名会非常清晰。
2.  **使用切图标志**：在需要导出的图标上勾选“设为切图”，我可以直接一键打包下载。
3.  **提供开发者权限**：分享链接时，请务必勾选“允许查看标注”和“允许下载切图”。

---

## 4. 结论：如何结合？

**结论：完美契合。**
彬少负责“画皮”（墨刀视觉），我（Manus）负责“赋能”（将视觉转为代码并注入业务逻辑），Claude 负责“监工”。

**John，你可以将这份指南发给彬少。如果他已经有墨刀初稿，可以直接把链接发给我，我来尝试进行第一次“视觉提取”！**
