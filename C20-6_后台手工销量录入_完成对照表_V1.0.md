# C20-6 后台手工销量录入　完成对照表　V1.0

| 项目 | 内容 |
| --- | --- |
| 卡片编号 | C20-6 后台手工销量录入 |
| 决策依据 | Claude 裁定《C20-4C 改走简化路线》2026-08-02 |
| 完成日期 | 2026-08-02 |
| 后端测试 | 220 项全绿（既有 196 + C20-6 新增 24） |
| 前端构建 | `vue-tsc --noEmit` 无错，`vite build` 通过 |
| 交付状态 | 已完成，**默认启用**，8/9 首场主用销量入账通道 |
| 待人类确认 | 两个阈值常量（见「待确认事项」一节） |

---

## 一、本卡片的定位

C20-6 是在 C20-4C（订单表批量导入）因 `players.display_code` 缺陷无法在生产环境使用后，
按裁定改走的**简化替代路线**。两者的关键差别不在于功能多少，而在于**防御方向相反**：

> 订单表导入防的是「无意识丢失」——文件里有行，系统没算，运营不知道。
> 手工录入防的是「无意识多算」——运营多打一个零，系统照算，没人察觉。

多算比少算更难被发现：选手人气偏高，不会有人来投诉。因此 C20-6 的防线全部指向「录入前
让运营自己看见数字」，而非事后审计。

C20-6 **不依赖** `players.display_code`。选手从下拉框直接选取 `player_id`，商家编码仅用于
取单价。这是刻意的设计：该字段在生产环境无任何写入入口（见 DEFECT-001），任何依赖它的
链路都不可用。

---

## 二、逐条对照

| 裁定要求 | 实现方式 | 验证用例 |
| --- | --- | --- |
| 按「商品原价 × 件数」折算人气 | 复用 `product_price_config` 与订单导入完全相同的换算口径（单价分 × 件数 × 10） | `basicEntryUsesSameFormulaAsOrderImport` |
| 人气变更走唯一入口 | 一律调用 `PopularityService.applyChange`，`source` 取 `manual`，不绕过引擎 | `popularityGoesThroughSingleEntryPoint` |
| 幂等防连点 | 前端生成 `idempotencyKey`，表上 `uq_msl_idem` 唯一约束 | `sameIdempotencyKeyIsBlocked`、`idempotencyKeyRequired` |
| 支持负数冲销纠错 | 负数件数走同一路径，人气值为负，日志类型区分为 `manual_sales_reversal` | `negativeQuantityReversesPopularity` |
| 每笔留痕 | 双写：`manual_sales_ledger` 业务流水 + `operations_log` 操作日志，操作人与原因均必填 | `entryPersistsLedgerAndLog`、`reasonAndOperatorRequired` |
| 本轮合计核对视图 | 两级展开：外层按选手人气合计，内层按商品件数与人气 | `summaryGroupsByPlayerThenProduct`、`summaryIsScopedByRound` |

---

## 三、超出裁定原文的设计，及其理由

以下五项裁定书未要求，是我在实现中判断必需而加入的。每一项都附了「不做会怎样」，
便于评审时判断是否值得保留。

### 1　软重复提示（60 秒窗口）

**问题**：幂等键只能防「同一次点击的重复提交」。运营以为没成功、手动再点一次，那是一次
新点击，会得到新键并正常入账——这才是现场真正会发生的情况。

**做法**：服务端检查 60 秒内是否已有「相同选手 + 相同商品 + 相同件数」的记录。若有，
**不入账**，返回 `needs_confirm` 与提示语，等运营看清后再确认。

**为何不直接拒绝**：同一场里运营完全可能先卖 30 件、稍后又真的卖了 30 件。直接拒绝会
静默吞掉真实销量，比重复入账更危险。判断权必须留给运营。

**附带发现**：群投票录入（C13）存在同一个缺口，且**至今没有软重复提示**。项目此前已默认
接受该风险，只是无人明确记录。此处一并提出，是否为群投票补同样机制请裁定。

### 2　异常量二次确认

**问题**：手工录入最典型的错误是件数多打一位数字（30 打成 300）。这个错误一旦入账，
表现为某选手人气突然领先，而没人会怀疑。

**做法**：单笔折算人气若超过「本轮当前最高选手人气 × 2」，返回 `needs_confirm` 要求二次确认，
提示语直接写明「请核对件数是否多打了一位数字」。

**本轮首笔跳过此检查**：没有参照基准时任何数字都会触发，只会制造噪音，运营很快就会
习惯性点确认，防线随即失效。

### 3　单价快照

**问题**：`product_price_config` 的价格可改。若不快照，事后核对会用新价重算，得出一个与
账面人气不符的数字，且无人能解释差异。

**做法**：`manual_sales_ledger.unit_price_cent` 存录入时的单价。改价只影响此后录入，
不追溯已入账记录。与 C20-4C 的订单账本保持一致。

### 4　单价不一致显式告警

**问题**：同一商品若在本轮内被改过价，「件数 × 单价」再也反推不出人气合计——运营的核对
手段实际失效，而界面上看不出来。

**做法**：汇总视图比较该商品最早与最新的单价快照，不一致则在选手行打「单价不一致」标签，
并在明细里同时显示两个价格。

### 5　件数不跨商品相加

**问题**：明信片 30 件加写真 5 件等于「35 件」，这个数字没有业务含义，无法用来判断单价是否
配错——而这正是把件数与人气值并列展示的全部理由。

**做法**：件数只在「选手 + 商品」这一层汇总，选手层只给人气合计，不给件数合计。

---

## 四、冲销的边界

裁定要求支持负数冲销，边界按以下方式收紧：

| 边界 | 处置 | 理由 | 用例 |
| --- | --- | --- | --- |
| 冲销到负数总量 | **禁止** | 负销量无业务含义，极可能是运营把冲销对象搞错 | `reversalCannotDriveTotalNegative` |
| 冲销额度计算范围 | 按「选手 + 商品」**独立**计算，不跨商品挪用 | 某选手明信片 10 件、写真 2 件，冲销写真 5 件必须被拒，不能因「该选手共 12 件」而放行 | `reversalLimitIsPerProductNotPerPlayer` |
| 冲销原因 | **必填** | 与 C20-4C 覆盖留痕一致：每一次减人气都必须有人具名负责 | `reasonAndOperatorRequired` |
| 件数为零 | 拒绝 | 零件数不产生任何效果，却会留下一条无意义流水 | `zeroQuantityRejected` |

---

## 五、前端三态必须分开展示

这是本卡片前端唯一不可妥协的一点。录入返回三种终态，含义完全不同：

| 状态 | 含义 | 若与「成功」混同的后果 |
| --- | --- | --- |
| `recorded` | 已入账，人气已变动 | — |
| `duplicated` | 这一笔**早已入账**，未重复计算 | 运营以为没成功，会再录一遍 |
| `needs_confirm` | **尚未入账**，等待运营确认 | 本该入账的销量凭空消失 |

`needs_confirm` 用黄色告警呈现，标题直写「这一笔尚未入账，请核对后再确认」，并**不清空
表单**。二次确认时**复用同一个幂等键**——若重新生成，运营连点确认按钮就会入账两笔，
这恰是幂等键本该防住的事。

另在提交按钮上方增设一行提交前预览「单价 × 件数 = 人气」，这是比服务端异常量提示更早的
一道自查：数字摆在眼前时，多打一位更容易被自己发现。

---

## 六、交付物清单

### 新增文件

| 文件 | 说明 |
| --- | --- |
| `service/ManualSalesService.java` | 核心业务：校验、软重复、异常量、冲销边界、双写留痕、汇总 |
| `mapper/ManualSalesLedgerMapper.java` | 流水账读写与汇总查询 |
| `dto/ManualSalesEntryResult.java` | 录入结果三态 |
| `dto/ManualSalesSummaryItem.java` | 汇总内层（选手 + 商品） |
| `test/ManualSalesC20Test.java` | 24 项专项用例 |
| `frontend/src/api/sales.ts` | 前端 API 封装与幂等键生成 |

### 修改文件

| 文件 | 说明 |
| --- | --- |
| `controller/AdminControlController.java` | 新增 `POST /api/admin/sales/manual-entry`、`GET /api/admin/sales/manual-summary` |
| `dto/AdminRequests.java` | 新增 `ManualSalesEntryRequest` |
| `db/db_schema.sql` | 新增 `manual_sales_ledger` 表（生产 MySQL） |
| `test/resources/schema-h2.sql` | 同步新增表 + 顶部 DROP 清理段 |
| `test/SchemaInitializationTest.java` | 表清单与 `EXPECTED_TABLE_COUNT` 更新 |
| `frontend/src/App.vue` | 新增「销量录入」标签页 |

---

## 七、实施中踩到的坑（必须写入项目规范）

### 坑一：新增一张表要改四处，漏一处会导致 181 个错误且报错指不到根因

本项目有两份建表脚本（生产 MySQL 与测试 H2），靠人工同步。新增一张表必须同时改：

1. `db/db_schema.sql` 的 `CREATE TABLE`
2. `test/resources/schema-h2.sql` 的 `CREATE TABLE`
3. `test/resources/schema-h2.sql` **顶部的 `DROP TABLE IF EXISTS` 清理段**
4. `SchemaInitializationTest` 的表名清单与 `EXPECTED_TABLE_COUNT`

我漏了第 3 处。后果是第二个 Spring 测试上下文初始化时报「Table already exists」，
ApplicationContext 加载失败，**雪崩为 181 个错误**。而我拿到的报错是「无法解析构造函数
参数 CoefficientService」——与建表毫无关系，需翻到日志第 208 行才见真因。

这与 Claude 在 C20-4B 记录的「两份 schema 不同步而测试全绿」是同一类问题。**根源是两份
schema 需人工同步这一设计本身**，建议后续单开一张卡片改为单一来源。

### 坑二：`</script>` 之后的代码不参与编译，可以长期藏着从未生效的实现

`App.vue` 中存在两份 `submitRefund`。第二份连同 `logoutAdmin` 位于 `</script>` **之后**，
TypeScript 从未检查过它们——页面上实际运行的是前一份用 `fetch` 手写的版本，其注释还留着
「目前没引入 refund api，这里用 fetch 临时替代」。

我在文件末尾追加 C20-6 代码时把 `</script>` 移到真正的末尾，这段死代码才第一次被编译，
重复定义随即暴露。已删除死代码块，**保留正在生效的实现不动**——退款涉及资金，不在 C20-6
范围内，不趁机重写。

此类问题构建阶段发现不了。建议后续加一条 lint 规则或检查脚本，禁止 `</script>` 之后出现
非空白内容。

---

## 八、待确认事项

| 编号 | 事项 | 现值 | 待谁确认 | 说明 |
| --- | --- | --- | --- | --- |
| A | 异常量阈值倍数 `ABNORMAL_MULTIPLE` | 2 | Vincent | 定低了会频繁打扰运营，定高了拦不住错误。需从节目效果角度权衡 |
| B | 软重复窗口 `RECENT_WINDOW_SECONDS` | 60 | Vincent / John | 现场录入节奏决定此值。节奏快则应缩短 |
| C | 群投票录入是否补软重复提示 | 未补 | Claude | C13 存在同一缺口，项目此前默认接受，此处提出请裁定 |
| D | 未知订单状态是否升级为硬阻断 | 仅告警 | Claude | C20-4C 遗留议题，因该卡暂不启用，优先级降低 |

A 与 B 均为单个常量，改动成本极低，不阻塞交付。

---

## 九、遗留风险

**测试全绿不等于现场可用。** C20-4C 的教训是 196 项测试全绿却漏掉了「生产环境唯一入口
写不进 `display_code`」，原因是所有测试都用 SQL 直接造数据、绕开了那个入口。

C20-6 在这一点上做了针对性规避：**测试中的选手刻意不写 `display_code`**，以证明本链路
完全不依赖该字段。但仍需指出，C20-6 尚未经过真人在浏览器上的端到端实测。建议在 8/9 之前
安排一次现场演练，重点验证三件事：

1. 商品原价配置是否齐全（每位选手每款商品一个独立编码，12 位选手若各 2 款即需 24 条配置）
2. 三态提示在真实网络延迟下是否清晰可辨，尤其 `needs_confirm`
3. 冲销流程运营是否能独立完成，无需他人指导

第 1 项工作量不小，且必须在开场前完成——**未配原价的编码无法录入**。建议列为 8/9 开场
检查清单的第一项。
