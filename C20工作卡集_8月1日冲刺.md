# C20 工作卡集（8/1 冲刺）

日期：2026-07-27　下发：Claude　执行：Manus

**执行纪律（沿用 C19 教训）**
- **一张卡 = 一个可独立演示的交付物。做完一张、提交一张、验收一张，再开下一张。**
- 每张卡提交必须附《逐项完成对照表》（每条要求 → 文件/函数 → 证据）；禁止"均已落实"式表述；未做项必须明写"未做"。
- 若执行中发现本指令与代码现实不符（字段名、行号等），**停下来在提交说明中写明差异并按现实调整**，不得静默绕过。

**已定决策（本卡集的前提，不再讨论）**
1. 人气值采用**稳妥档**：小程序内完全不露出人气数字与"人气值"字样
2. 移除方式：新建 `release/miniapp-lite` 分支，页面目录移出上传包
3. **分支纪律：release 分支只做减法。任何功能性修改一律先进 main 再 cherry-pick，禁止在 release 分支直接改功能。**
4. 渠道分界：**客服消息只谈权益，粉丝群才发榜单**
5. ⚠️ **重要区分**：稳妥档只约束**抖音小程序**。场控后台与大屏页部署在自有服务器，**不受抖音类目管辖，人气值可以正常显示**。C20-3/4/5 不适用稳妥档。

**优先级**：C20-1 → C20-2 完成并验收后**立即提审**（审核需 1~3 工作日，是 8/1 的关键路径）；C20-3/4/5 可在提审等待期并行推进。

---

## 卡 C20-1：小程序精简版 — 页面移除与入口清理

**目标**：打开小程序只有 3 个 tab，全站不存在任何投票 / 人气榜的入口或残留页面，上传包内不含相关文件。

**分支**：从 main 新建 `release/miniapp-lite`，本卡及 C20-2 均在此分支进行。

**具体步骤**
1. `app.json`：`tabBar.list` 由 4 项改为 3 项，移除 `pages/popularity/index`（保留 首页 / 选手 / 我的）
2. `app.json`：`pages` 数组中移除 `pages/suspicion/index` 与 `pages/popularity/index`
3. 将 `pages/suspicion/` 与 `pages/popularity/` 两个目录整体移动至 `archive/`（**不得留在上传包内**），并确认小程序打包配置不会包含 `archive/`
4. `pages/home/index.js`：移除 `onSpyTap()` 函数（约 43~49 行）
5. `pages/home/index.ttml`：移除 `spy-entry` 区块（约 29 行）
6. `pages/home/index.ttss`：移除因上述删除而产生的孤儿样式类（**不得留孤儿样式，C19 已有前车之鉴**）
7. 全工程 grep `suspicion` 与 `popularity`，确认**无任何残留跳转、引用或字符串**（后端接口调用如已无调用方，前端侧一并移除）

**验收标准**
- 开发者工具中小程序仅 3 个 tab，无任何路径可进入投票页或人气榜页
- 上传包内不含 suspicion / popularity 相关文件
- 全工程 grep 结果为空（或仅剩 archive/ 目录内）

**交付**：逐项完成对照表 + commit + grep 结果文本 + tabBar 截图

---

## 卡 C20-2：小程序精简版 — 人气值清除与全量文案中文化

**目标**：全站看不到任何人气数字或"人气值"字样，看不到任何"直播/直播间/点赞/评论"表述；分享出去的标题是自定义的。

**分支**：`release/miniapp-lite`（承接 C20-1）

**具体步骤**

**A. 人气值清除（稳妥档，共 4 页 7 处，请以实际 grep 为准）**
1. `pages/redeem-success/`：移除"增加人气值 +N"等表述。核销成功页**只陈述用户自己获得的权益**——如"写真已存入收藏""会员权益 +7 天"。
   - ⚠️ **不得改用"应援""助力""支持值"等替代词**（打榜色彩更重）；**不得提及为哪位选手加分**
2. `pages/players/index.ttml`：移除每位选手的人气数字，仅保留 编号 / 姓名 / 队伍 / 写真等中性信息
3. `pages/player-detail/index.ttml`：移除"当前人气值"及相关数字展示与引导文案
4. `pages/home/index.ttml`：移除两处实时人气展示框
5. 全工程 grep「人气」，确认用户可见文案中无残留

**B. 文案清理（"直播/直播间/点赞/评论/抖币" → 活动/积分表述）**
6. 逐页清理用户可见文案，参考映射：
   - 正在直播 · XXX → 活动进行中 · XXX
   - 当前暂无直播互动 → 当前暂无进行中的活动
   - 增加人气值/留言请在直播间进行 → 相关互动请在活动现场参与
   - 提交后请等待直播间揭晓 → 提交后请等待活动现场公布
   - 请留意直播间提示 → 请留意活动现场提示
7. `utils/constants.js:10`（错误码 40005）中的"直播轮次"表述一并处理
8. 检查 `app.json` 各页面标题、所有 toast 与错误提示

**C. 自定义分享文案**
9. 新增 `onShareAppMessage`，自定义分享标题与描述（不得出现投票/人气/直播字样）。建议方向：活动名称 + 粉丝权益工具定位

**验收标准**
- 全工程 grep「人气」「直播」「点赞」「评论」，用户可见文案中零命中（后端字段名、注释、日志不需改）
- 核销成功页只讲用户自己的权益，不提任何选手与分数
- 分享出去的标题为自定义内容

**交付**：逐项完成对照表 + commit + 四组 grep 结果 + 关键页面截图（首页/选手页/选手详情/核销成功页/分享卡片）

> C20-2 验收通过后，由 John 立即上传体验版并提交发版审核。

---

## 卡 C20-3：后台新增「群投票结果录入」

**目标**：运营能在后台按轮次录入各选手的群投票得票数，支持多次累计与冲销修正，全程有操作日志。

**分支**：main（后台功能，不受稳妥档约束）

**背景**：8/1 猜卧底投票在抖音粉丝群通过「投票管家」进行，结果**不会自动进入我方系统**，需人工录入。因投票在停播后仍持续，须支持**跨时段多次录入并累计**。

**具体步骤**
1. 后端新增接口：按 `roundId` + `playerId` 录入得票数增量
2. **多次累计**：同一轮次同一选手可多次录入，系统累加（非覆盖）
3. **修正机制**：复用现有 `manual` 来源的**冲销语义**（录入负数冲销），不采用覆盖式修改，保持账本一致性
4. 必带 `operatorId`，写入 `operations_log`（谁、何时、哪一轮、哪位选手、增减多少）
5. 幂等保护：沿用现有幂等键机制，防连点重复录入
6. 后台前端：在识破/卧底相关区域新增录入表单，并展示**本轮各选手当前累计票数**，便于运营核对
7. 测试：多次累计正确、冲销正确、连点不重复、日志落库

**验收标准**：运营可反复录入、可冲销纠错、可随时查看本轮累计票数，操作全程留痕

**交付**：逐项完成对照表 + commit + 测试输出 + 界面截图

---

## 卡 C20-4：后台直播数据录入改为「填当前总数，系统自动算差值」

**目标**：运营从抖音中控台读到累计总数后直接填入，系统自动算出增量并归属给当前场控目标；**目标可随时自由切换，无固定顺序**。

**分支**：main

**背景**：直播间点赞/评论/音浪由平台统计为**累计总数**，运营每隔数分钟读数录入。现方案需运营心算差值，高压环境下易错，且错账会污染轮次结算。

**具体步骤**
1. 后端为 gift / like_delta / comment_delta 三个来源各维护一条**"上次录入总数"水位线**（含录入时间）
2. 提交时：`本次差值 = 当前总数 − 上次总数`，按现有逻辑归属给**当前场控目标**，并更新水位线
3. 异常保护：当前总数**小于**上次总数时（可能是换场/读错）不得直接写入负值，须提示运营确认后再处理
4. 后台前端改为**单页面**（不分步骤、无固定顺序）：
   - 顶部：当前场控目标下拉（可随时切换任意选手/团队/公共池，**跳过谁都行**）
   - 中部：三行（点赞 / 评论 / 礼物），每行显示"上次总数 + 上次录入时间"，旁边输入"当前总数"，**实时预览算出的差值**
   - 底部：一次提交三项
5. **防错提醒（内建纪律）**：切换当前目标时弹窗提示"是否先录入当前目标的数据？"——避免跨目标错账
6. 沿用现有幂等与审计

**验收标准**：运营只填总数、不做心算；切目标无顺序限制；切换时有提醒；差值归属正确

**交付**：逐项完成对照表 + commit + 测试输出 + 界面截图

---

## 卡 C20-5：人气榜大屏页（含一键生成分享图）

**目标**：打开一个网址即为实时滚动的人气榜，可用于直播画面呈现，并能一键导出图片供运营发粉丝群。

**分支**：main　**部署**：自有服务器（**不受抖音类目约束，人气值正常显示**）

**用途（Vincent 已定的三个渠道之二依赖本卡）**
- 直播时：OBS / 直播伴侣以「浏览器源」采集进直播画面（若 PC 开播不可用，则用浏览器全屏 + 窗口捕获，或现场物理屏幕入镜）
- 非直播时：运营一键出图，发粉丝群 / 发短视频图文

**具体步骤**
1. 新增只读页面，**复用后台现有人气接口**，不新建数据逻辑
2. 每 2~5 秒轮询刷新（**8/1 用轮询即可，WebSocket 留给方向 C**）
3. 视觉：深色背景、大字体、高对比，适配直播画面观看；**排序沿用后台既有规则，不得在前端重排**
4. 新增「生成分享图」按钮：前端一键将当前榜面导出为图片（html2canvas 一类方案），版式统一
5. 需彬少提供一版分享图模板规范（配色、logo 位、标题区）
6. 页面需能在无登录状态下展示？**——不可**。须校验访问权限（避免链接外泄后被抓取），具体方式与后台现有 admin 鉴权保持一致或采用独立只读令牌，方案由 Manus 提出后 Claude 裁定

**验收标准**：网址打开即为实时榜单，数据随后台变化自动更新；一键出图版式正确；未授权无法访问

**交付**：逐项完成对照表 + commit + 页面截图 + 导出图样例

---

# 附：John 的提审准备清单（非代码，需你本人操作）

C20-2 验收通过后提审前逐项确认：

- [ ] **准备审核专用测试卡密**：未核销、不过期，在提审备注中写明使用方法与测试路径（否则审核员无法体验核心功能，可能以"功能不完整"驳回）
- [ ] **小程序后台基础信息**：简介、服务类目、截图素材全部改为"粉丝权益工具"定位，**不得出现投票、人气、打榜、直播互动等字样**（这部分不在代码库内，最易漏，且审核第一眼就看）
- [ ] **服务内容描述**：沿用已获客服认可的表述（活动信息查询 / 互动任务积分 / 兑换码换写真与会员权益 / 权益状态查询）
- [ ] **阿奇索商品文案排查**：抖店商品详情、发货话术中若承诺"+N 人气"，须同步修改为权益表述——否则用户在小程序里看不到人气，会产生客诉与退款
- [ ] **发码消息文案**：只写权益说明，**不放任何链接、不放榜单截图**（渠道分界原则）
- [ ] 保存客服关于类目认可的对话截图，备驳回时申诉使用

---

**下发方式**：C20-1 先行。验收通过后再下发 C20-2，以此类推。C20-3/4/5 可在提审等待期并行。
