# C12 H5 中间页 + 全链路联调技术实施方案 v1

**作者：Manus AI**  
**日期：2026-06-16**  
**状态：待 Claude 确认后编码**  
**适用范围：C12 彩排底线最后一张任务卡；H5 中间页、抖音小程序核销页参数承接、开发环境端到端联调脚本与联调报告**

## 一、背景与任务目标

Claude 已审查 `67f7103` 并确认 **C11 抖音小程序前端通过**。至此，C1~C11 与 C19 已完成，彩排底线只剩 C12。Claude 对 C12 的定义是“**H5 中间页 + 全链路联调**”，其核心价值不是新增复杂业务，而是把已经完成的场控后台、基础数据后台、人气引擎、小程序前端、卡密核销链路串起来，在 6/22 真实环境测试前完成开发环境预演。[1]

C12 的 H5 中间页需要承接阿奇索发货后的跳转场景：用户从抖店或私信点击链接，进入 H5，中间页接收阿奇索传参 `t={$卡券信息}&oid={$订单号}`，然后引导用户进入小程序核销页，并尽可能把 token 参数传给小程序核销页。[1] 由于抖音外部 H5 唤起小程序的公开能力存在平台约束，C12 不能在未确认平台能力前手写一个不可用的伪 scheme；本方案将这一点作为必须由 Claude 裁定的技术风险。[2] [3]

> 蓝军判断：C12 最大风险不是 H5 页面本身，而是“外部 H5 如何稳定打开抖音小程序指定页面并携带 token”。如果平台生成链接能力尚未开通，H5 必须提供复制卡密和手动进入小程序核销页的降级路径，否则会在真实环境测试中卡死。

## 二、C12 范围边界

C12 应保持“集成与联调”定位，不重写 C1~C11 的业务能力，不引入新的支付、订单、会员或写真管理逻辑。H5 中间页只做参数接收、参数清洗、跳转/复制引导和状态提示；小程序只需做最小改动：核销页支持从启动参数或页面 query 预填 token。

| 范围 | 本期处理 | 禁止事项 |
|---|---|---|
| H5 中间页 | 新增纯 HTML/CSS/JS 页面，接收 `t` 与 `oid`，展示卡密，提供跳转/复制/错误提示 | 不接入支付，不查订单，不存用户隐私。 |
| 小程序核销页 | 支持 `pages/redeem/index?token=...` 预填 token | 不改变 API-3 核销规则，不传 userId。 |
| 后端 | 原则上不改；如仅为联调脚本或静态文件服务需要，需 Claude 单独批准 | 不改 C2~C9 核心业务逻辑。 |
| 全链路联调 | 提供开发环境联调步骤、脚本/清单与报告 | 不伪造测试结论；未真机验证的项必须标注。 |
| 抖音真实能力 | 先预留配置项和降级路径 | 不假定手写 scheme 一定可用。 |

## 三、H5 中间页方案

建议新增 `frontend/h5-bridge/`，保持纯静态页面结构，便于部署到任意静态站点或对象存储。页面不依赖构建工具，避免在最后一张彩排底线卡引入额外复杂度。

```text
frontend/
  h5-bridge/
    index.html
    styles.css
    app.js
    config.js
    README.md
```

### 3.1 参数接收与解析

H5 页面接收以下 URL 参数：

| 参数 | 来源 | 是否必需 | 处理方式 |
|---|---|---:|---|
| `t` | 阿奇索卡券信息 | 是 | 视作卡密 token 原文，执行 trim、decode、upperCase 和格式校验。 |
| `oid` | 阿奇索订单号 | 否 | 仅展示“订单号已记录”，不发送后端，不作为核销凭证。 |
| `debug` | 开发联调 | 否 | 开启时显示解析明细，便于 C12 截图验证。 |

卡密格式仍沿用既有规范：`RFZJ-XXXX-XXXX-XXXX`。早期卡密规范文档明确卡密由阿奇索自动发给用户，并由用户在小程序输入框粘贴/输入核销。[4] C12 的改进是让 H5 尽可能承接跳转与预填，但不能改变卡密核销后端的原子抢占和防爆破机制。

### 3.2 页面状态

| 状态 | 展示内容 | 用户动作 |
|---|---|---|
| token 合法 | 显示卡密、订单号、打开小程序按钮、复制卡密按钮 | 优先打开小程序；失败则复制后手动粘贴。 |
| token 缺失 | 显示“未检测到卡密参数” | 提示返回订单消息查看完整链接。 |
| token 格式异常 | 显示“卡密格式异常” | 允许复制原文并联系工作人员。 |
| 非抖音环境 | 显示同样页面 | 不强依赖 UA；提供复制降级。 |

### 3.3 跳转策略与蓝军风险

公开资料显示，抖音小程序外部 H5 唤起并不像微信明文 URL Scheme 一样可以直接手写完整路径；公开整理文章指出“抖音暂时不支持用户手动拼写 URL 跳转路径，只能通过请求接口生成对应跳转链接”。[3] 抖音开放平台公开 H5 分享文档主要描述的是“分享内容至抖音 H5 场景”，其中 `micro_app_info` 是视频发布后挂载小程序入口，并不等同于普通 H5 直接打开小程序核销页。[2]

因此，C12 不建议硬编码一个未经验证的 `snssdk1128://microapp?...`。建议实现三层策略：

| 层级 | 方式 | 是否本期实现 | 说明 |
|---|---|---:|---|
| A | 配置化跳转链接 `MINI_PROGRAM_LINK_BASE` | 是 | 如果抖音开放平台或运维方提供正式生成链接，则 H5 拼接 token query 后跳转。 |
| B | 手动复制卡密 + 提示进入小程序核销页 | 是 | 平台跳转能力未就绪时的必备降级。 |
| C | 后端生成/刷新抖音小程序跳转链接 | 待 Claude 裁定 | 可能需要 appid/secret/client token 等，不应在纯 H5 中私放密钥。 |

H5 `config.js` 可预留：

```js
window.RED_FACE_H5_CONFIG = {
  miniProgramLinkBase: '',
  redeemPagePath: 'pages/redeem/index',
  fallbackCopyEnabled: true
}
```

如果 `miniProgramLinkBase` 为空，H5 不显示“自动打开”承诺，只显示“复制卡密并打开小程序”的彩排降级操作。这样即使真实跳转能力未开，也不会误导用户或制造死链。

## 四、小程序核销页最小改动方案

C11 已实现 `pages/redeem/index`，当前支持手动输入、用户点击粘贴、提交、防重复、错误五态。C12 只建议做最小增强：在 `onLoad(options)` 中读取 `options.token`，如果存在则执行同样的 trim/upperCase 并预填输入框。

| 改动点 | 说明 |
|---|---|
| `pages/redeem/index.js` | `onLoad(options)` 读取 `options.token` 并预填 `data.token`。 |
| `app.js` 可选 | 若平台跳转把参数放在 launch query，可在 globalData 暂存，再由 redeem 页读取。 |
| 不变项 | 核销仍调用 `POST /api/tokens/redeem`，不传 userId，只走 Bearer token。 |

这项改动不改变 C11 的合规红线：页面加载仍不自动读取剪贴板，也不自动提交核销；用户必须点击“确认核销”。

## 五、全链路联调方案

C12 的核心验收是把各端串起来。建议联调分为三条主链路，每条链路保存步骤、输入数据、关键截图和结果。

| 链路 | 步骤 | 验收重点 |
|---|---|---|
| 链路 A：基础数据 → 场控 → 首页/看板 | 后台录入选手/队伍/轮次/分队 → 设置 active 轮次 → 切换场控目标 → 小程序首页与看板刷新 | 首页显示 active 轮次和目标；看板按序号展示。 |
| 链路 B：模拟注入 → 人气引擎 → 看板变化 | 后台模拟点赞/留言/礼物 → 后端 `applyChange` 入账 → 小程序首页/人气页刷新 | 目标选手和团队人气增长；不按 value 排名。 |
| 链路 C：阿奇索参数 → H5 → 小程序核销 → 我的写真 | 构造 `index.html?t=RFZJ-...&oid=...` → H5 解析 → 跳转/复制 → 小程序核销 → 核销成功 → 我的写真 | token 不由前端传 userId；核销成功显示收藏提示；我的写真可见。 |

## 六、开发环境联调数据准备

C12 开发环境联调不依赖真实抖音 API。C9 的 `MockAuthProvider` 可支持测试阶段稳定登录；C10/C19 后台可录入基础数据；C7 模拟器可模拟直播事件；C5/C6 已具备卡密核销与生成能力。

建议准备一组固定彩排数据，便于截图前后对比：

| 数据 | 建议值 | 用途 |
|---|---|---|
| 队伍 | A组、B组 | 基础数据展示与团队人气。 |
| 选手 | 1号陈微、2号佳佳、3号林夏 | 验证看板按序号排列。 |
| active 轮次 | 第3轮 | 对齐 UI 示例和首页显示。 |
| 场控目标 | 3号林夏 A组 | 验证首页目标和模拟注入。 |
| 卡密 | 一条未使用 `RFZJ-...` | 验证 H5 → 小程序核销 → 我的写真。 |

## 七、验证物与报告

C12 完成后应提交：

| 验证物 | 形式 | 说明 |
|---|---|---|
| H5 页面代码 | `frontend/h5-bridge` | 纯 HTML/CSS/JS，可静态部署。 |
| 小程序核销页参数承接 | 代码 diff | 仅预填 token，不自动核销。 |
| H5 静态验证输出 | `reports/C12_h5_static_validation_output_v1.txt` | HTML/JS/CSS 文件存在、JS 语法、token 解析用例。 |
| 后端回归输出 | `reports/C12_mvn_test_output_v1.txt` | 如果 C12 未改后端，可仍跑全量 `mvn test` 证明回归。 |
| 联调检查清单 | `reports/C12_integration_checklist_v1.md` | 逐步记录待人工/已人工验证状态。 |
| 实施报告 | `C12_H5_Integration_Report_v1.md` | 说明实现、边界、测试、未完成真机项。 |

## 八、需要 Claude 裁定的问题

| 编号 | 问题 | Manus 建议 |
|---|---|---|
| C12-Q1 | C12 是否允许修改 C11 小程序 `pages/redeem/index`，增加 query token 预填？ | 建议允许，这是 H5 带 token 落地的最小必要改动。 |
| C12-Q2 | H5 跳小程序在本期是否采用“配置化跳转链接 + 复制降级”，而不硬编码未验证 scheme？ | 建议是。抖音外部 H5 唤起小程序能力需要平台生成链接或明确配置，不能臆造。 |
| C12-Q3 | 是否需要新增后端接口生成抖音小程序跳转链接？ | 建议暂不做，除非 John 已拿到官方 AppID/Secret 和明确接口文档；否则 C12 会被平台接入拖住。 |
| C12-Q4 | H5 是否需要把 `oid` 上报后端？ | 建议本期不做。`oid` 仅展示和调试，核销凭证仍是 token。订单追踪可留 C14/C16/上线后。 |
| C12-Q5 | C12 是否必须包含真实抖音开发者工具截图？ | 建议分两步：本卡提交代码与开发环境检查清单；真实云环境和抖音开发者工具截图纳入 6/22 测试报告。 |
| C12-Q6 | 是否在 C12 同时启动后端服务、后台前端和小程序进行本地截图？ | 建议若时间允许做，但不把截图作为阻塞编码完成的唯一标准；C12 的核心应是链路设计和可执行检查清单。 |

## 九、结论

C12 应作为彩排底线的总集成卡处理。Manus 建议先实现一个安全、可降级、可配置的 H5 中间页，再对小程序核销页做 token 预填最小增强，最后输出开发环境联调清单和验证报告。H5 跳小程序能力必须谨慎处理：如果没有官方生成链接或明确平台配置，不应硬编码伪 scheme；复制卡密和手动进入小程序核销页的降级路径必须保留。

## References

[1]: /home/ubuntu/upload/Claude审查_C11通过_C12开卡.md "Claude 审查_C11通过_C12开卡"
[2]: https://open.douyin.com/platform/resource/docs/develop/share/H5 "分享内容至抖音 H5 场景 - 抖音开放平台"
[3]: https://www.cnblogs.com/xwwin/p/18664668 "外部H5唤起常用小程序链接规则整理"
[4]: 红颜局中局·卡密生成规范与阿奇索对接逻辑说明_V1.0.md "卡密生成规范与阿奇索对接逻辑说明 V1.0"
[5]: C10_C19_Admin_Implementation_Report_v1.md "C10+C19 彩排运营后台实施报告 v1"
[6]: C11_Douyin_MiniProgram_Implementation_Report_v1.md "C11 抖音小程序前端实施报告 v1"
[7]: docs/API契约/当前页面级API契约定稿_C9依据_V1.0.md "当前页面级 API 契约定稿_C9依据_V1.0"
