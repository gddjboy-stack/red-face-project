# C12 全链路联调检查清单 v2

> 制定人：Manus AI  
> 日期：2026-06-17  
> 适用范围：红颜·局中局 C12（H5 Bridge & Full-link Integration）  
> 状态：本清单为用户要求“推倒重来”后的重新版本，替代此前已回滚的 C12 清单。

## 〇、执行原则

本清单以 Claude 对 `C12_H5_Integration_Implementation_Plan_v1` 的最终裁定为边界。Claude 明确批准 C12 编码，但要求 H5 采用**配置化跳转 + 复制降级**，禁止硬编码未验证的抖音跳转协议；小程序核销页只允许在 `onLoad` 中读取 `options.token` 预填输入框；`oid` 仅展示、不上报后端；未真机验证的项目必须诚实标注为待验证。[1] [2]

> Claude 裁定的关键边界是：“绝不允许硬编码臆想的跳转协议”；如果没有拿到抖音官方跳转链接，则 `miniProgramLinkBase` 留空，只显示“复制卡密 + 手动打开小程序核销页”的降级路径。[1]

## 一、H5 Bridge 验证清单

| 序号 | 验证项 | 操作方法 | 预期结果 | 当前状态 | 证据或备注 |
|---|---|---|---|---|---|
| H5-01 | H5 目录为纯静态实现 | 检查 `frontend/h5-bridge/` | 仅包含 `index.html`、`style.css`、`script.js`，不引入构建工具和后端接口 | 开发环境已验证 | 静态脚本 `scripts/verify_c12_static.py` 已通过 |
| H5-02 | 读取 `t` 参数 | 打开 `index.html?t=RFZJ-2345-6789-ABCD` | 页面显示该核销凭证 | 开发环境已验证 | 浏览器验证记录见 `C12_browser_verification_notes.md` |
| H5-03 | 读取 `oid` 参数 | 打开 `index.html?t=...&oid=ORDER-C12-001` | 页面展示订单号，并标注仅用于人工核对、本页不上报后端 | 开发环境已验证 | 浏览器验证记录见 `C12_browser_verification_notes.md` |
| H5-04 | 缺少 `t` 参数的异常提示 | 打开不带 `t` 的链接 | 页面提示链接缺少 `t` 参数，复制按钮不可用 | 代码级静态验证已通过 | 可在 6/22 测试中补充浏览器截图 |
| H5-05 | 默认不显示自动打开入口 | 保持 `miniProgramLinkBase` 为空 | 页面不显示“打开小程序核销页”入口，仅显示复制降级路径 | 开发环境已验证 | 符合 Claude 裁定，避免制造死链 |
| H5-06 | 已配置平台跳转链接时的拼参逻辑 | 将 `miniProgramLinkBase` 配置为官方生成链接后访问 | 页面显示小程序打开入口，并在链接上追加 `token` 参数 | 待 6/22 真机验证 | 依赖抖音官方跳转链接；当前不得伪造通过 |
| H5-07 | 复制核销凭证 | 点击“复制核销凭证” | 页面提示“核销凭证已复制”，用户可手动进入小程序核销 | 开发环境已验证 | 浏览器点击验证通过 |
| H5-08 | 不包含未验证 scheme | 静态扫描脚本检查 `script.js` | 不出现 `snssdk`、`douyin://` 等硬编码跳转 scheme | 开发环境已验证 | `python3 scripts/verify_c12_static.py` 已通过 |

## 二、小程序核销页验证清单

| 序号 | 验证项 | 操作方法 | 预期结果 | 当前状态 | 证据或备注 |
|---|---|---|---|---|---|
| MP-01 | `onLoad` 支持 `options.token` | 审查 `pages/redeem/index.js` | `onLoad(options)` 读取 `options.token` 并执行与输入框一致的 `trim().toUpperCase()` 归一化 | 代码级静态验证已通过 | `scripts/verify_c12_static.py` 已通过 |
| MP-02 | 不自动读取剪贴板 | 审查 `onLoad` 代码 | `onLoad` 中不出现 `tt.getClipboardData` | 代码级静态验证已通过 | 保持 C11 合规红线 |
| MP-03 | 不自动核销 | 审查 `onLoad` 代码 | `onLoad` 中不调用 `redeemToken`，用户仍必须点击确认核销 | 代码级静态验证已通过 | 符合 Claude 裁定 |
| MP-04 | 抖音开发者工具预填 | 在抖音开发者工具以 `pages/redeem/index?token=RFZJ-2345-6789-ABCD` 打开 | 输入框显示归一化后的 token | 待 6/22 或本地具备工具后验证 | 当前沙箱无抖音开发者工具，不伪造结论 |
| MP-05 | 真机预填与核销 | 真实抖音环境通过 H5 或手动路径进入核销页 | token 可预填或粘贴，用户点击确认核销后完成写真领取 | 待 6/22 真机验证 | 依赖真实小程序环境与云端服务 |

## 三、后端与管理后台回归清单

| 序号 | 验证项 | 操作方法 | 预期结果 | 当前状态 | 证据或备注 |
|---|---|---|---|---|---|
| BE-01 | 后端代码未被 C12 修改 | `git diff` 范围审查 | C12 不改 C2~C9 后端核心逻辑，不新增后端生成跳转链接接口 | 开发环境已验证 | 本次 C12 仅新增 H5、修改小程序核销页、添加文档与验证脚本 |
| BE-02 | 后端全量回归 | `cd backend/redface-backend && mvn clean test` | 58 个 JUnit 测试通过，0 failures，0 errors | 开发环境已验证 | 初次 `mvn test` 因旧 `target/test-classes` 资源污染失败；清理后 `mvn clean test` 通过 |
| BE-03 | `oid` 不上报后端 | 审查 H5 与小程序变更 | H5 只展示 `oid`；小程序与后端核销请求仍只传 token | 开发环境已验证 | 符合 Claude 裁定 |
| BE-04 | 管理后台联动 | 后端核销成功后观察后台数据变化 | 后台可显示对应核销/写真状态变化 | 待 6/22 云环境验证 | 本沙箱没有真实 MySQL 与三端部署环境，不伪造通过 |

## 四、Agiso 与真实平台全链路清单

| 序号 | 验证项 | 操作方法 | 预期结果 | 当前状态 | 证据或备注 |
|---|---|---|---|---|---|
| FL-01 | Agiso 发货链接模板 | 在 Agiso 插件配置 H5 链接模板 | 链接格式为 `https://{domain}/h5-bridge/index.html?t={TOKEN}&oid={ORDER_ID}` | 待 6/22 真实环境验证 | 依赖 Agiso 插件实际配置 |
| FL-02 | H5 降级链路 | 用户从 Agiso 链接进入 H5，复制凭证，手动进入小程序核销 | 即使未拿到官方跳转链接，也能完成“复制凭证 → 手动核销” | 开发环境局部验证，真实环境待验证 | H5 页面与复制交互已验证；Agiso 与真机仍待验证 |
| FL-03 | H5 自动跳转链路 | 获取抖音官方跳转链接后配置 `miniProgramLinkBase` | 点击 H5 入口能进入小程序核销页，并带上 token | 待 6/22 真机验证 | 依赖抖音平台能力，不得用臆造 scheme 测试 |
| FL-04 | 端到端核销 | H5/手动路径进入小程序，完成核销，进入写真页 | 用户可成功完成核销并查看写真 | 待 6/22 真机验证 | 需要真实云端、小程序、用户登录态与有效 token |
| FL-05 | 异常 token | 使用错误格式、已使用、冷却状态 token | 小程序展示对应错误信息，不崩溃 | 待 6/22 或开发者工具验证 | 后端相关错误码已有 JUnit 覆盖，端侧展示仍需真机/工具验证 |

## 五、6/22 现场执行脚本

| 步骤 | 执行动作 | 通过标准 | 记录材料 |
|---|---|---|---|
| 1 | 确认云端已部署后端、管理后台、H5 静态页和抖音小程序体验版 | 四端访问入口明确且版本一致 | 部署版本号、Git commit、访问域名 |
| 2 | 在 Agiso 中生成一条测试发货链接 | 链接包含 `t` 与 `oid`，且能打开 H5 Bridge | Agiso 配置截图、测试链接脱敏截图 |
| 3 | 未配置官方跳转链接时测试降级链路 | H5 不显示自动打开承诺，只提示复制并手动打开小程序 | H5 截图、复制成功截图 |
| 4 | 手动进入小程序核销页并粘贴 token | 输入框可承载 token，用户点击确认后发起核销 | 小程序核销页截图 |
| 5 | 使用有效 token 完成核销 | 成功页展示写真相关信息，写真进入“我的” | 成功页截图、我的写真截图 |
| 6 | 若拿到抖音官方跳转链接，则配置 `miniProgramLinkBase` 后复测自动跳转 | H5 显示打开入口，点击后进入小程序核销页并带 token | H5 截图、跳转后小程序截图 |
| 7 | 在管理后台核对数据变化 | 数据状态与核销动作一致 | 后台截图、操作记录 |
| 8 | 使用异常 token 做一次失败路径测试 | 小程序展示预期错误信息，不出现敏感词或崩溃 | 错误提示截图 |

## 六、引用

[1]: docs/c12/Claude_Ruling_C12_Final.md "Claude 裁定 — C12 技术方案确认（用户提供附件，已归档到仓库）"
[2]: https://github.com/gddjboy-stack/red-face-project/blob/main/C12_H5_Integration_Implementation_Plan_v1.md "C12_H5_Integration_Implementation_Plan_v1.md"
