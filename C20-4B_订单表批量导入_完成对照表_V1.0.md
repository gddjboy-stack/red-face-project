# C20-4B 订单表批量导入 · 完成对照表 V1.0

| 项目 | 内容 |
| --- | --- |
| 任务卡 | C20-4B 周边订单批量导入与人气入账 |
| 完成时间 | 2026-08-01 |
| 全量测试 | **174 项全部通过**（上轮基线 154，本次新增 20） |
| 新增测试 | OrderImportC20Test 19 项、SchemaParityC20Test 1 项 |
| 关键决策依据 | John 2026-08-01 三项决策：按金额、按原价、门槛设在支付完成、采用单价配置表 × 件数 |

---

## 一、逐项完成情况

| 序号 | 要求 | 状态 | 实现位置 |
| --- | --- | --- | --- |
| 1 | 新建订单明细表 | 已完成 | `order_sales_ledger`（两份 schema 同步） |
| 2 | 商品原价配置表 | 已完成 | `product_price_config` + `ProductPriceService` |
| 3 | CSV/Excel 解析 | 已完成 | `SheetReader`（POI，按文本读取避免科学计数法） |
| 4 | 表头映射与别名容错 | 已完成 | `OrderSheetParser.buildColumnMap` |
| 5 | 空值归一化 | 已完成 | 半角/全角横线、EN DASH、`-`、空白统一归一 |
| 6 | 订单有效性判定 | 已完成 | `OrderSheetParser.parseRow` |
| 7 | 归属选手 | 已完成 | 商家编码 → `players.display_code`（复用既有列，未新增字段） |
| 8 | 人气换算 | 已完成 | 配置原价（分） × 件数 × 10 |
| 9 | 两阶段预览确认 | 已完成 | `preview` / `confirm`，令牌一次性消费 |
| 10 | 幂等 | 已完成 | 双层：子订单号唯一约束 + `popularity_ledger` 幂等键 |
| 11 | 单行失败不中断整批 | 已完成 | `REQUIRES_NEW` + 自身代理绕回 |
| 12 | 审计留痕 | 已完成 | 无效行同样落库；改价写 `operations_log` |

---

## 二、有效性判定口径

判定采用**白名单**而非黑名单：只有明确认定为成立的订单状态才计入，未知状态一律判无效。抖店后续新增状态时，系统会少算而不会错算，事后可补录；反之若默认计入，错算的分数在结算后无法追回。

| 订单状态 | 售后状态 | 判定 | 说明 |
| --- | --- | --- | --- |
| 已完成 / 已发货 / 待发货 | 空 | 有效 | 已支付完成 |
| 已完成 / 已发货 / 待发货 | 售后关闭 | 有效 | 售后已了结，订单依然成立 |
| 已完成 / 已发货 / 待发货 | 售后中 / 待商家处理 | 有效但计入风险敞口 | 单列汇总供运营评估最坏情况 |
| 已关闭 | 退款成功 | 无效 | 已退款 |
| 已关闭 | 空 | 无效 | 订单已关闭 |
| 待支付 | 任意 | 无效 | **门槛设在支付完成，防零成本刷榜** |
| 未知状态 | 任意 | 无效 | 不猜测 |

> 需特别指出：**不可用「售后状态为空」作为有效判据**。售后关闭意味着售后流程已了结、订单成立，若按空值判据会把这类正常订单误判为无效。该场景已写入测试 `aftersaleClosedIsStillValid`。

---

## 三、人气换算与精度

按 John 决策，人气值取自**系统内维护的商品原价配置**，而非订单表中的应付金额。这样运费、平台优惠、商家优惠、主播优惠、优惠券一律不影响人气，粉丝支付多少与获得多少人气之间的关系可以向选手和观众清楚解释。

换算链路全程为整数：原价以「分」存储，`人气值 = 单价（分） × 件数 × 10`。19.9 元即 1990 分，对应 19900 人气值，与《人气值换算公式初稿 V1.3》第 2.2 节一致。价格由字符串按小数点切分转换，不经过浮点运算，`99.99` 精确得到 9999 分。

改价**不追溯**已入账记录，且每次改价写入 `operations_log`。若允许追溯，赛后同一批订单会因价格变动得出不同人气值，届时无法解释差异。

---

## 四、幂等设计

幂等做成两层，两层都必须存在：

第一层是 `order_sales_ledger.sub_order_no` 唯一约束，防同一子订单重复入账。第二层是 `popularity_ledger.idempotency_key = "order:{子订单号}"`，防绕过本服务的其他写入路径。仅靠第一层，将来新增别的订单入账路径就会绕开；仅靠第二层，订单明细表会积累重复行导致对账数字虚高。

此外还有两处防护：同一文件内子订单号重复时仅首行计入，重复行明确标注与第几行重复（而非静默去重，因为运营需要判断这是导出勾选问题还是真有两笔）；预览令牌一次性消费，防重复点击确认，也防「看着 A 文件的预览却导入了 B 文件」。

---

## 五、实测暴露的两个缺陷

### 缺陷一：两份 schema 不同步，且测试全绿无法发现

`players.display_code` 此前**只存在于生产 MySQL schema**，测试 H2 schema 缺失。这意味着任何依赖选手编号的功能在测试环境根本跑不起来，而当时全量 154 项测试**全部通过**——因为没有任何测试碰过这一列。

这个缺陷的性质值得强调：它说明「测试全绿」并不能证明两份 schema 一致，漏项会一直潜伏到某个功能真正用到它为止。若那个时刻发生在 8/9 现场，代价是整场结算不可用。

修复方式不只是补列，而是新增 `SchemaParityC20Test`，把这条隐式假设变成显式断言：逐表逐列比对两份脚本，任何一方漏加都会立即失败。该测试不连数据库，只做文本比对，不受运行环境影响。

### 缺陷二：REQUIRES_NEW 自调用静默失效

单行失败不应污染整批事务，因此 `persistRow` 标注 `REQUIRES_NEW`。但 Spring 事务通过代理生效，类内部直接 `this.persistRow()` 不经过代理，该标注会**静默失效**——编译通过、单元测试通过，只在真实回滚场景暴露。已改为通过 `ObjectProvider` 绕回代理。

---

## 六、待人类确认事项

| 编号 | 事项 | 负责人 | 时限 |
| --- | --- | --- | --- |
| A-1 | 8/7 上架后提供「商家编码 / 商品名 / 单价」清单并录入系统 | John | 8/7 |
| A-2 | 商家编码必须与选手编号一致（`players.display_code`），上架时即约定 | John / 彬少 | 8/7 |
| A-3 | 礼物归属按时段近似，需写入选手规则说明 | Vincent | 8/3 前 |
| A-4 | 主持人口播话术库定稿（含被动响应与故障兜底） | Vincent | 8/3 前 |
| A-5 | 校准按钮文案落 UI | 彬少 | 8/3 前 |

> A-1 与 A-2 是**硬阻塞**：商家编码若与选手编号不一致，或某商品漏录单价，该商品的订单将全部判为未归属、不计入人气。系统会在预览阶段明确提示，但届时已是直播现场。

---

## 七、蓝军提示：本模块最脆弱的环节

技术实现之外，本模块最脆弱的环节不在代码，而在**运营是否在正确的时点导出正确的报表**。

订单导出是手工操作，涉及筛选时间范围与勾选字段。若运营导出时时间范围少选了一段，系统无法察觉——它只会诚实地导入你给它的行，并报告「已导入 N 行」。系统能防重复计入，但**防不住漏导**。

因此建议在结算前增设一次人工核对：以抖店后台显示的商品总销量与系统内 `order_sales_ledger` 的有效行数对比。两个数字若不一致，说明漏导。这一步无法自动化，因为系统无从得知「应该有多少笔」。
