# 《红颜局中局》UI设计交付标准手册（设计师直通版）

**项目名称：** 红颜局中局
**文档版本：** V1.0
**日期：** 2026-06-04
**作者：** Manus AI

---

## 彬少，你好！

我是 Manus，你的 AI 开发搭档。我知道之前的功能定义文档里有些技术术语让你头大，非常抱歉！这份手册就是专门为你准备的，我会用最简单、最直白的方式，告诉你墨刀里哪些操作对我（Manus）最重要，以及为什么重要。这样，你的设计稿就能“一键”变成代码，大大加快我们的开发速度！

## 1. 墨刀设计“三板斧”——让我秒懂你的设计

### 1.1 第一板斧：图层命名“通关口诀”

**为什么重要？**
你给图层起的名字，就像是给它们贴上了“身份证”。我（Manus）会根据这些名字，自动识别出这是个按钮、一张图片还是一段文字，然后自动生成对应的代码。名字越规范，我生成的代码就越准确，你返工的次数就越少！

**怎么做？**
请给墨刀里的每一个**关键图层**（比如按钮、图片、输入框、文字块）起一个**有意义的英文名字**。不用太复杂，但要能看懂它是什么。

| **图层类型** | **推荐命名格式** | **举例** |
|---|---|---|
| 按钮 (Button) | `btn_功能描述` | `btn_submit` (提交按钮), `btn_share` (分享按钮) |
| 图片 (Image) | `img_内容描述` | `img_player_avatar` (选手头像), `img_bg_home` (首页背景) |
| 文本 (Text) | `txt_内容描述` | `txt_player_name` (选手名字), `txt_score` (分数) |
| 输入框 (Input) | `input_功能描述` | `input_phone` (手机号输入框), `input_comment` (评论输入框) |
| 容器/分组 (Container/Group) | `wrap_功能描述` | `wrap_player_card` (选手卡片容器), `wrap_header` (头部区域) |

**小贴士：**
*   **英文小写**：全部用小写字母。
*   **下划线连接**：单词之间用下划线 `_` 连接。
*   **语义化**：名字要能反映图层的内容或功能。

### 1.2 第二板斧：切图“秘籍”——图片一键导出

**为什么重要？**
小程序里的图片资源（图标、背景图、选手写真等）需要单独导出，我才能把它们放到代码里。如果你提前标记好，我就可以自动帮你“切图”，省去你手动导出的麻烦。

**怎么做？**
在墨刀里，所有需要作为独立图片导出的元素（比如图标、背景图、选手头像、写真），请务必在**图层属性面板中勾选“切图”选项**。并且，为了适配不同手机屏幕，请提供 **@2x 和 @3x** 两种倍率的图片。

**操作步骤：**
1.  选中需要切图的图层。
2.  在右侧属性面板中找到“切图”或“导出”选项。
3.  勾选，并选择导出格式（建议 PNG 或 JPG）。
4.  确保同时生成 @2x 和 @3x 尺寸的图片。

### 1.3 第三板斧：墨刀“开发者模式”——让我看透你的设计

**为什么重要？**
墨刀的“开发者模式”就像是给我（Manus）开了一扇“透视窗”。通过这个模式，我可以直接看到你设计稿里每个元素的精确尺寸、间距、颜色、字体等所有细节，这样我才能准确地还原你的设计，避免出现“像素级”的偏差。

**怎么做？**
当你完成设计稿并准备交付给我时，请生成一个**“分享链接”**，并在分享设置里**务必开启“开发者模式”**，同时勾选“允许查看标注”和“允许下载切图”权限。

**操作步骤：**
1.  在墨刀项目界面，点击右上角的“分享”按钮。
2.  在弹出的分享设置中，找到并**勾选“开发者模式”**。
3.  同时勾选“允许查看标注”和“允许下载切图”。
4.  复制生成的分享链接，然后发给 John 即可。

## 2. 交付与协作流程（超简单！）

1.  **你设计**：彬少在墨刀里完成所有 UI 页面设计、实物周边设计和数字资产设计。
2.  **你分享**：按照上面的“三板斧”要求，生成一个**开启了“开发者模式”**的墨刀分享链接，发给 John。
3.  **我提取**：我（Manus）会通过这个链接，自动提取设计参数、切图资源和代码片段。
4.  **Claude 审查**：我将提取并转化的代码上传至 GitHub，供 Claude AI 进行代码规范和交互逻辑审查。

## 3. 核心设计任务提醒

*   **小程序 UI 设计**：所有页面（选手列表、详情、人气榜、会员中心等）的 UI 视觉稿。
*   **实物周边设计**：一套可量产的**选手签名明信片**模板（作为合规发货载体）。
*   **数字资产设计**：每位选手的**电子写真集封面及内页排版**（用于扫码解锁）。

---

希望这份手册能让你更清晰地了解我们的协作方式。如果你有任何疑问，随时告诉我！我们一起把《红颜局中局》做得又快又好！
