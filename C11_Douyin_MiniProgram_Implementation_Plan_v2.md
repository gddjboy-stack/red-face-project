# C11 抖音小程序前端技术实施方案 v2

**作者：Manus AI**  
**日期：2026-06-16**  
**状态：待 Claude 确认后编码**  
**适用范围：C11 用户端抖音小程序；首页、人气看板、卡密核销、核销成功、极简我的写真；同步记录首页/选手页最新 UI 定稿影响**

## 一、更新背景

Claude 已审查并确认 C10+C19 彩排运营后台通过，后端与运营后台已具备录入真实选手、分队、设卧底、切场控、模拟注入、看人气变化的能力。Claude 同时裁定 C11 可以启动，仍按“先出方案→确认→编码”的协作流程推进。[1]

本次 v2 方案在 C11 v1 与 C11 裁定更新说明基础上，新增三项输入：第一，Claude 最新指出首页和选手页已由彬少改到位，写真素材不合规问题可等 C17 后台自助换图处理，不阻塞 C11；第二，用户补充说明彬少已删除首页中的“集赞送礼”类强营销词和“联系我们”等四入口；第三，用户倾向于暂时不再要求彬少继续修改 UI，因为照片后续可在后台替换。[1] [2] [3]

> 蓝军结论：用户“暂时先不修改 UI”的判断**总体可采纳**，但应加一个工程约束：C11 不应把当前 PDF 中的写真图片硬编码为长期资源，也不应扩展开发选手详情页的动态社交模块。照片合规问题可以交给 C17，但 C11 代码必须保留接口图片、占位图和失败降级能力。

## 二、C10+C19 关闭与 C11 启动结论

Claude 最新审查结论为 C10+C19 通过，且 C11 可以启动。当前彩排底线只剩 C11 与 C12：C11 负责用户端小程序前端，C12 负责 H5 中间页与全链路联调。[1]

| 项目 | 当前状态 | 对 C11 的影响 |
|---|---|---|
| C1~C9 后端 | 已通过 | API-0 至 API-4 可供 C11 直接对接。 |
| C10+C19 后台 | 已通过 | 后台可录入真实数据并驱动 C11 页面展示真实状态。 |
| 首页 UI | 已更新 | C11 首页按最新稿删除四入口和强营销词。 |
| 选手页 UI | 已更新 | 可作为 C15 选手详情视觉储备；C11 是否纳入需 Claude 再确认。 |
| 卡密核销页 | 沿用之前版本 | C11 直接按 API-3 与错误五态实现。 |
| 核销成功页 | 文案需改 | 删除“会员+7天”，改为“已存入收藏”。 |
| 写真素材 | 暂不要求继续改图 | C11 使用接口 URL/占位图，C17 后台负责后续替换。 |

## 三、彬少首页/选手页更新复核

根据 `首页(1).pdf`，首页顶部保留首页标题、项目名、更多与设置图标；主视觉为“本周精选推送”大卡片；直播状态区显示“正在直播·第3轮”、当前环节、人气对象、团队人气和“卡密核销”按钮。原来的首页四入口已经删除，底部 Tab 保留：首页、选手、人气、我的。[2]

根据 `选手(1).pdf`，选手详情页保留“选手详情”、选手号、选手姓名、团队、个人人气值、个人动态、个人写真、相关视频、进入直播间提示、购买选手周边按钮和底部 Tab。页面里仍出现“购买选手周边（跳转能力待确认）”这类开发标注残留，Claude 已将其标记为 C15 范围的遗留小项，不阻塞 C11 彩排版。[1] [3]

| 页面 | 已解决问题 | 仍需注意 | C11 处理建议 |
|---|---|---|---|
| 首页 | 删除四入口；强营销词明显收敛；卡密核销入口保留 | “LOVAL LEVEL”是否品牌拼写仍未在本次附件中裁定 | 按最新首页稿实现，不恢复四入口。 |
| 选手详情 | 删除或弱化部分敏感强引导；写真区保留 | 仍有个人动态、相关视频、周边按钮、跳转待确认标注等后置能力 | 不作为 C11 P0 动态页；如保留底部“选手”Tab，先做静态/占位或跳到人气页，需 Claude 裁定。 |
| 写真图片 | 用户决定暂不继续要求彬少改 | 当前视觉仍可能使用示例图，不应固化为正式资源 | C11 只展示 `photoPreviewUrl` 或清新占位图，C17 后台替换。 |

## 四、C11 v2 范围：建议坚持 5 个 P0 页面，不扩到完整选手详情

C11 v1 曾建议四个彩排核心页：首页、人气看板、卡密核销、核销成功。随后 C11 裁定更新说明已把“极简我的写真页”纳入 P0，因为核销成功页承诺“数字写真已自动存入我的收藏”，用户需要有一个地方看到收藏结果。[4]

因此，C11 v2 建议范围应为 **5 个 P0 页面**：

| 页面 | 路由建议 | API | 本期目标 |
|---|---|---|---|
| 首页 | `pages/home/index` | API-0、API-1 | 登录、直播状态、当前互动对象、目标人气、团队人气、卡密核销入口。 |
| 人气看板 | `pages/popularity/index` | API-2 | player/team/spy Tab；严格按后端顺序展示，不按 value 排序。 |
| 卡密核销 | `pages/redeem/index` | API-3 | 输入、手动粘贴、提交、防重复、错误五态。 |
| 核销成功 | `pages/redeem-success/index` | API-3 成功 data | 展示选手、团队、人气、写真预览、已存入收藏提示。 |
| 我的写真 | `pages/my-photos/index` | API-4 | 极简只读列表、空态、图片加载失败降级。 |

### 4.1 为什么不建议把“选手详情页”纳入 C11 P0

选手页虽然已经有新 PDF，但当前页面级 API 契约明确指出“选手详情页当前契约无独立 API，不应作为 P0 动态页面开发”。同时，选手页仍包含个人动态、关注、距离、相关视频、购买选手周边等后端尚未覆盖或被裁定后置的模块。[5]

因此，Manus 的蓝军建议是：**C11 不实现完整选手详情动态页**。如果底部 Tab 必须保持“选手”，可采用以下三选一方案，请 Claude 裁定：

| 方案 | 做法 | 风险 |
|---|---|---|
| A：Tab 隐藏或暂不启用 | C11 底部只保留首页、人气、我的；选手详情留 C15 | 与最新 UI 底部 Tab 不完全一致。 |
| B：Tab 点击跳人气看板 player Tab | 用户可看选手列表/人气，不进入详情页 | 交互略不符合“选手详情”视觉稿。 |
| C：做静态选手详情壳 | 使用当前选中/示例选手展示静态 UI，动态数据仅复用 API-2/API-1 | 容易被误认为完整功能，且个人动态/视频/周边仍无后端。 |

Manus 建议选 **B** 或 **A**。如果 Claude 认为必须贴近底部 Tab，建议选 B：底部“选手”Tab 进入人气看板的 player Tab，并用“按序号排列·不设名次”表达；完整选手详情仍留 C15。

## 五、首页 v2 编码调整

首页必须按彬少最新稿收敛，不恢复此前四入口，也不出现“打榜/排名/榜首/应援/PK打赏”等敏感词。API-1 已覆盖首页主动态字段，包括 `liveStatus`、`roundName`、`currentMode`、`targetDisplayName`、`targetPopularity`、`teamDisplayName`、`teamPopularity`、`spyChannelOpen` 和 `updatedAt`。[6]

| UI 区域 | 数据来源 | 编码说明 |
|---|---|---|
| 顶部项目名与视觉卡 | 静态资源 | 图片可用合规占位，不绑定业务逻辑。 |
| 正在直播/idle | API-1 `liveStatus`、`roundName` | idle 时显示“当前暂无直播互动”。 |
| 当前互动对象 | API-1 `targetDisplayName`、`targetPopularity` | 文案建议“当前互动计入”，避免强营销。 |
| 团队人气 | API-1 `teamDisplayName`、`teamPopularity` | 数字格式化，不做排行表达。 |
| 卧底入口 | API-1 `spyChannelOpen` | 未开启时灰置或提示“对应环节开启”。 |
| 卡密核销 | 静态路由 | 跳转 `pages/redeem/index`。 |
| 四入口 | 无 | 彩排版彻底删除，不灰置。 |

## 六、人气看板页编码约束

人气看板页使用 API-2 `GET /api/popularity/board?tab=player|team|spy&roundId=3`。后端已保证 items 按 `number` 或 `team_id` 的中性顺序返回，前端不得调用 `sort(value)` 重新排序，也不得显示“第1名”“榜首”“冠军”等排名化语言。[6] [7]

| Tab | C11 处理 |
|---|---|
| player | 展示 `number/name/teamName/value`，按后端返回顺序。 |
| team | 展示团队名称和团队人气，仍按后端返回顺序。 |
| spy | 保留 Tab，但在 `spyTabEnabled=false` 时显示“卧底档将在对应环节开启”。 |

## 七、卡密核销与核销成功页编码约束

卡密核销页只在用户点击“粘贴”时调用剪贴板能力，禁止页面打开自动读取剪贴板。提交时调用 API-3，不传 userId，只依赖登录后保存的 Bearer token。[6]

| 错误码 | 前端固定文案 |
|---:|---|
| 40001 | 卡密格式不正确，请检查是否为 RFZJ-XXXX-XXXX-XXXX。 |
| 40002 | 未找到该卡密，请确认复制完整后重试。 |
| 40003 | 该卡密已被核销，无法重复使用。 |
| 40004 | 连续输错次数较多，请等待倒计时结束后再试。 |
| 40005 | 当前暂无可计入的直播轮次，请稍后再试或联系工作人员。 |

核销成功页不重新发起核销请求，而是使用核销成功返回的 data 或临时缓存展示 `playerNumber/playerName/teamName/points/photoPreviewUrl/collected`。页面文案必须改为“数字写真已自动存入我的收藏”，不展示“会员有效期+7天”。[4] [6]

## 八、我的写真页与 C17 的关系

我的写真页只做极简只读列表，不做会员中心，不做订单查询，不做社群入口。API-4 返回 `total` 和 `items`，条目字段为 `assetId/previewUrl/playerName/createdAt`。[6]

照片合规问题不应阻塞 C11，但必须通过工程方式规避长期风险。具体要求如下：

| 阶段 | 图片处理方式 |
|---|---|
| C11 编码 | 展示接口返回的 `previewUrl`；若为空或加载失败，显示清新风占位图。 |
| 彩排 | 可用后台已有 `photo_assets` 数据或清新占位图。 |
| C17 完成后 | John 可在写真上传后台替换选手写真，C11 无需改业务代码。 |

这意味着用户“暂时先不修改照片”的判断可以执行，但不能理解成“照片风险消失”。照片风险仍是上线前合规红线，只是由 C17 承接，而不是 C11 本次反复改 UI。[3] [4]

## 九、建议目录结构

C11 仍建议新增 `frontend/douyin-miniprogram`，使用抖音原生小程序结构。C10 的 Vue 后台已位于 `frontend/control-admin`，两者不应混用工程。

```text
frontend/
  douyin-miniprogram/
    app.js
    app.json
    app.ttss
    project.config.json
    config/
      env.js
    utils/
      request.js
      auth.js
      format.js
      constants.js
    components/
      status-card/
      board-item/
      empty-state/
      error-tip/
      photo-card/
    pages/
      home/
      popularity/
      redeem/
      redeem-success/
      my-photos/
    assets/
      placeholders/
        photo_clean_stage.svg
```

## 十、验收标准

| 验收项 | 验证方式 | 通过标准 |
|---|---|---|
| API-0 登录 | 开发者工具/真机截图 | 小程序启动后能获得 token，后续请求带 Bearer。 |
| 首页最新稿 | 截图 | 无四入口；无强营销词；直播状态、人气对象、团队人气正确。 |
| 人气页合规 | 截图 + 数据验证 | 即使 3 号人气最高，也仍按序号排列，不显示名次。 |
| 核销页 | 截图/录屏 | 手动粘贴、提交 loading、防重复点击、错误五态完整。 |
| 核销成功 | 截图 | 文案为“数字写真已自动存入我的收藏”，无会员+7天。 |
| 我的写真 | 截图 | 空态、有收藏、图片失败降级均可展示。 |
| 素材风险 | 人工核查 | 不硬编码性感示例图；默认占位图清新、才艺、舞台风。 |

## 十一、需要 Claude 确认的问题

| 编号 | 问题 | Manus 建议 |
|---|---|---|
| C11v2-Q1 | C11 是否正式启动编码？ | 建议启动。首页/选手页已更新，核销成功文案可编码时直接改。 |
| C11v2-Q2 | 首页是否按最新 PDF 删除四入口，不再灰置？ | 建议是，彻底删除。 |
| C11v2-Q3 | 选手详情页是否仍留 C15，不进 C11 P0？ | 建议是。若底部 Tab 必须保留“选手”，建议跳转人气页 player Tab。 |
| C11v2-Q4 | 照片是否按 John 判断交给 C17，不阻塞 C11？ | 建议是，但 C11 必须做占位和加载失败降级。 |
| C11v2-Q5 | C11 是否仍为 5 页：首页、人气、核销、核销成功、极简我的写真？ | 建议是。 |
| C11v2-Q6 | 墨刀首页顶部“LOVAL LEVEL”是否保持原样？ | 建议请 Vincent/彬少确认品牌拼写；编码阶段先按图实现，但报告中记录风险。 |

## 十二、结论

C11 已具备启动条件。首页和选手页的最新修改解决了首页四入口和强营销词的主要 P0 风险；照片素材问题可按用户判断交给 C17 后台替换，不阻塞 C11，但 C11 必须通过接口图片、清新占位图和加载失败降级来避免把风险固化进代码。Manus 建议 C11 仍按 5 个 P0 页面实施，不把完整选手详情页纳入本期，以免重新引入个人动态、相关视频、周边跳转等后置模块。

## References

[1]: /home/ubuntu/upload/Claude审查_C10C19通过_C11启动.md "Claude 审查_C10C19通过_C11启动"
[2]: /home/ubuntu/upload/首页(1).pdf "彬少首页最新 PDF"
[3]: /home/ubuntu/upload/选手(1).pdf "彬少选手页最新 PDF"
[4]: C11_Douyin_MiniProgram_Approved_Delta_v1.md "C11 抖音小程序前端裁定更新说明 v1"
[5]: docs/API契约/API契约复审_UI覆盖核查记录_V1.0.md "API 契约复审：UI 覆盖核查记录 V1.0"
[6]: docs/API契约/当前页面级API契约定稿_C9依据_V1.0.md "当前页面级 API 契约定稿_C9依据_V1.0"
[7]: /home/ubuntu/upload/红颜局中局开发指导手册V3.0全景版.md "红颜局中局开发指导手册 V3.0 全景版"
[8]: C11_Douyin_MiniProgram_Implementation_Plan_v1.md "C11 抖音小程序前端技术实施方案 v1"
