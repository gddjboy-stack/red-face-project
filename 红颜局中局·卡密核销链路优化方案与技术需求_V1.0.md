# 《红颜局中局·卡密核销链路优化方案与技术需求_V1.0》

**项目名称：** 红颜局中局
**文档版本：** V1.0
**日期：** 2026-06-08
**作者：** Manus AI

---

## 1. 现状分析

根据阿奇索商家的回复及截图，我们已确认：
1.  **卡密动态嵌入**：阿奇索支持将卡密动态嵌入到 URL 链接中（如 `https://example.com/redeem?token={卡券信息}`）。
2.  **私信环境限制**：抖音私信窗口中的普通 HTTPS 链接**无法直接点击跳转**（商家提示需“长按屏幕复制”）。
3.  **用户体验痛点**：长按复制、切换浏览器、手动粘贴的链路过长，会导致 30%~50% 的用户流失。

---

## 2. 核心优化方案：H5 中间页 + Schema 唤起

为了实现从“私信消息”到“小程序核销”的最短路径，我们建议采用以下技术方案：

### 2.1 链路流程图

1.  **用户侧**：收到私信消息 -> 长按复制链接 -> 粘贴到浏览器（或抖音搜索框）。
2.  **技术侧**：
    *   **Step 1**：阿奇索发送链接：`https://hjzj.com/go?t=RFZJ-XXXX-XXXX`（此链接由我们提供，指向一个轻量级 H5 页面）。
    *   **Step 2**：用户访问该 H5 页面，页面自动解析 URL 中的 `t` 参数（卡密）。
    *   **Step 3**：H5 页面通过抖音官方 **Schema 链接**（`snssdk1128://...`）自动尝试唤起抖音小程序，并直接跳转到“核销确认页”。
    *   **Step 4**：小程序接收到 Schema 携带的 Token，自动填充输入框，用户只需点击“立即核销”。

### 2.2 技术需求点

| 模块 | 需求内容 | 负责人 |
| :--- | :--- | :--- |
| **H5 中间页** | 开发一个单页面，具备解析参数、显示引导按钮、自动执行 Schema 唤起逻辑的功能。 | Manus (AI) |
| **Schema 生成** | 调用抖音 OpenAPI `generate_schema` 接口，生成带参数的小程序跳转链接。 | Manus (AI) |
| **小程序端** | 在 `onLoad` 中增加对 `token` 参数的解析逻辑，并实现“自动填充”和“一键核销”。 | Manus (AI) |
| **阿奇索配置** | 在阿奇索内容模板中，将发货正文修改为包含 H5 链接的引导语。 | John |

---

## 3. 最终确认版：阿奇索发货正文模板建议

建议 John 将阿奇索的发货正文修改为以下内容（兼顾指引与转化）：

> **发货模板建议：**
> 
> 亲，您订购的数字写真已发货！
> 请【长按复制】下方链接，粘贴到浏览器或抖音搜索框打开，即可一键领取：
> 👇👇👇👇👇
> https://hjzj.com/go?t={$卡券信息}
> 👆👆👆👆👆
> （若无法跳转，请在小程序内手动输入卡密：{$卡券信息}）

---

## 4. 经验总结 (已记录)

*   **文档闭环**：后续所有文档更新必须同步生成修订说明。
*   **链路优化**：外部链接进入小程序必须考虑“中间页”作为缓冲和解析层。
*   **降级方案**：在自动化链路失效时，必须保留“手动输入”作为兜底。

---

John，请确认是否允许我额外开发这个轻量级的 H5 中间页（我会负责全部代码）。如果确认，我将把该需求正式写入 V2.3 文档，并作为 Phase 1 的一部分开始执行。
