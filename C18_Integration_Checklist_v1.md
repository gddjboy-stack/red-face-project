# C18「全链路闭环修复」联调清单 v1

> 作者：Manus AI  
> 日期：2026-06-28  
> 用途：供 John、Claude、运维与后续真实环境联调使用。

## 一、已在本地完成的验证

| 编号 | 验证项 | 当前结果 |
|---|---|---|
| C18-BE-01 | 发码时绑定的写真必须属于该选手 | 已通过 C18 专项测试 |
| C18-BE-02 | 发码时绑定的写真必须是 active 状态 | 已通过 C18 专项测试 |
| C18-BE-03 | 发码成功后返回 batchId | 已通过 C18 专项测试 |
| C18-BE-04 | 导出接口按 batchId 返回纯文本，一码一行无表头 | 已通过 C18 专项测试 |
| C18-FE-01 | 小程序 401 拦截与自愈重放逻辑 | 代码已就绪，待真机验证 |
| C18-FE-02 | 管理后台发码 Tab 与上传 loading | 代码已就绪，`pnpm build` 通过 |

## 二、生产/预发环境验收步骤

### 1. 验证登录态自愈 (小程序)
1. 在手机上打开小程序，进入“我的”页面，确认能正常显示。
2. 让后端开发或运维在数据库里把你的 `user_session` 记录删掉，或者把 `expires_at` 改到过去。
3. 再次在小程序点击“刷新我的信息”。
4. **期望结果**：不应该一直弹“登录态无效或已过期”，而是应该自动重新走一遍抖音登录，然后成功刷新出信息。

### 2. 验证上传反馈 (场控后台)
1. 在后台“写真管理”Tab，选择一个选手，上传一张图片。
2. **期望结果**：点击上传后按钮应有 loading 状态；成功后，下方的筛选条件应自动变成刚上传的选手，并且新图片出现在列表第一行。

### 3. 验证发码与导出闭环 (场控后台)
1. 切换到“发码与导出”Tab。
2. 选择刚才上传写真的选手，绑定写真下拉框应能选到该写真。
3. 填写单张人气 100，生成数量 5。
4. 点击“确认生成”。
5. **期望结果**：右侧卡片应出现“最新生成的批次：BATCH-xxx”，并出现“下载纯文本卡库”按钮。
6. 点击下载，用记事本打开下载的文件。
7. **期望结果**：文件内只有 5 行类似 `RFZJ-XXXX-XXXX-XXXX` 的字符串，没有表头，没有其他信息。

## 三、阿奇索导入注意事项

1. **绝对隔离**：每次生成的 `.txt` 文件只包含一种商品（一个选手的某张写真）。请在导入阿奇索时，严格对应到阿奇索里的具体商品卡库。
2. **禁止混导**：千万不要把 A 选手的码导进 B 选手的阿奇索商品里，系统后端防不住这种人为操作失误。
