# C11 抖音小程序前端技术实施方案 v1

**作者：Manus AI**  
**日期：2026-06-16**  
**状态：待 Claude 确认后编码**  
**适用范围：C11 用户端抖音小程序，彩排核心页，抖音原生框架 ttml/ttss/js/json**

## 一、背景与裁定依据

Claude 已确认 **C9 通过，后端 C1~C9 收官**，并裁定进入 C10/C11 前端阶段。C11 被定义为抖音小程序用户端，按彬少最终交付稿与已定稿 API 契约实现彩排核心页：首页、人气看板、卡密核销、核销成功页。Claude 同时提醒，C11 依赖彬少的正式开发交付稿，尤其是写真素材更换为清新风等 P0 修改；如果正式稿尚未完成，C11 可先用占位图开发结构，素材后续替换。[1]

项目技术栈已定稿：用户端使用**抖音小程序原生框架**，运营后台使用 Vue3 + Element Plus，后端使用 Java 17 + Spring Boot 3.x + MyBatis。[2] 因此 C11 不建议引入跨端框架，也不建议与 C10 Web 后台共用前端工程。小程序前端应保持轻量、直接对接 C9 的 API-0~API-4，并严格遵守平台合规与已定稿页面级 API 契约。

> 已定稿 API 契约明确 C9 范围包括 API-0 登录、API-1 首页直播状态、API-2 人气看板、API-3 卡密核销、API-4 我的写真，API-5 识破提交留 C13。[3]

## 二、蓝军判断：C11 的真实风险

C11 的风险不在于页面数量，而在于**设计稿范围收敛、合规话术、素材安全、接口错误态与登录态**。如果直接照早期墨刀或功能定义文档全量开发，会误把会员、订单、社群、识破投票、选手详情、购买周边等后置能力纳入本期，导致前端承诺超出后端能力。C11 必须以 C9 API 契约和 Claude P0 修改清单为边界。

| 风险 | 蓝军判断 | 方案控制 |
|---|---|---|
| 误实现后置页面 | 会拖慢彩排并制造空接口 | 本期只做首页、人气看板、卡密核销、核销成功四个彩排核心页。 |
| 写真素材擦边 | 可能导致平台审核失败 | 本期使用清新/才艺/舞台风占位图；若彬少正式稿未完成，先用占位图。 |
| 前端接受或保存 userId 伪造参数 | 会破坏 C9 防伪造设计 | 登录后只保存 token；所有 `/api/me/*` 和核销请求带 Bearer token，不传 userId。 |
| 人气页按 value 排序 | 触碰去排名化红线 | 前端不排序，完全按后端 API-2 返回顺序展示，并保留“按序号排列·不设名次”文案。 |
| 卡密错误态不完整 | 用户无法理解失败原因，客服成本高 | 严格实现 API-3 的 40001~40005 五态文案。 |
| 自动读取剪贴板 | 平台合规风险 | 只允许用户点击“粘贴”后调用剪贴板能力，禁止页面打开自动读取。[3] |

## 三、本期范围：四个彩排核心页

Claude 明确 C11 彩排核心页为首页、人气看板、卡密核销、核销成功页。[1] 虽然早期 UI 结构还包含选手页、我的页、真相识破、会员中心等页面树，[4] 但本期应收敛到能够闭环展示直播状态、人气数据和卡密核销权益。

| 页面 | 路由建议 | API | 本期目标 |
|---|---|---|---|
| 首页 | `pages/home/index` | API-0、API-1 | 登录、展示直播状态、当前集赞对象、人气摘要、入口按钮。 |
| 人气看板 | `pages/popularity/index` | API-2 | player/team/spy tab 展示，按序号或 team_id 展示，不排名。 |
| 卡密核销 | `pages/redeem/index` | API-3 | 输入/粘贴卡密、提交核销、错误五态、锁定倒计时。 |
| 核销成功 | `pages/redeem-success/index` | API-3 响应缓存/路由参数 | 展示选手、团队、人气值、写真预览、自动收藏提示。 |

### 3.1 明确不做或灰置的页面/模块

UI 审查记录显示早期原型中存在选手详情、真相识破、我的页、订单查询、会员群、购买周边、直播跳转等内容。[5] 这些内容要么后端尚未开放，要么属于平台能力与审核边界，要么被 Claude 标记为 P1/P2 或后续任务。因此 C11 本期不实现完整功能。

| 模块 | 本期处理 | 原因 |
|---|---|---|
| 真相识破提交 | 不做 | API-5 留 C13。[3] |
| 我的写真完整页 | 可后置，不作为彩排核心页 | API-4 已有，但 Claude C11 核心页未列入；若时间允许可加只读列表。 |
| 选手详情 | 不做 | 动态、相关视频、周边均无后端能力。[5] |
| 会员有效期 | 不展示 | API 契约明确 P0 删除展示，会员表留 C16。[3] |
| 订单查询 | 灰置或隐藏 | 后端未开放订单查询。 |
| 加入会员群 | 灰置或隐藏 | 社群跳转能力未确认。 |
| 首页四入口 | 建议隐藏或灰置 | 直播排期/联动合作/活动推荐/联系我们后端无能力。[3] |

## 四、建议目录结构

建议新增 `frontend/douyin-miniprogram`，使用抖音原生小程序目录结构。若仓库后续需要被第三方维护，原生结构最容易被抖音开发者工具识别，也符合已定稿技术栈。

```text
frontend/
  douyin-miniprogram/
    app.js
    app.json
    app.ttss
    project.config.json
    utils/
      request.js
      auth.js
      format.js
      constants.js
    components/
      status-card/
      board-item/
      empty-state/
      error-tip/
    pages/
      home/
        index.ttml
        index.ttss
        index.js
        index.json
      popularity/
        index.ttml
        index.ttss
        index.js
        index.json
      redeem/
        index.ttml
        index.ttss
        index.js
        index.json
      redeem-success/
        index.ttml
        index.ttss
        index.js
        index.json
    assets/
      placeholders/
        photo_clean_stage.svg 或 png
```

## 五、API 对接方案

C11 只对接 C9 已实现用户端 API。所有响应遵循 `{code,message,data}`，`code=0` 为成功。登录成功后保存 `data.token` 到本地缓存；后续 API-3 和 API-4 通过 `Authorization: Bearer <token>` 请求头调用，不传 userId。

| API | 小程序调用时机 | 成功处理 | 失败处理 |
|---|---|---|---|
| API-0 `POST /api/auth/login` | App 启动或首页加载时 `tt.login` 后 | 保存 token；可缓存 userId 仅用于展示或调试 | 登录失败显示“登录失败，请稍后重试”。 |
| API-1 `GET /api/live/home` | 首页 onShow、下拉刷新、定时轮询 | 展示 live/idle、当前集赞对象、人气值、更新时间 | 显示降级空态，不影响页面浏览。 |
| API-2 `GET /api/popularity/board` | 人气页加载与 tab 切换 | 展示 items；前端不重排 | 空数据展示“暂无数据”。 |
| API-3 `POST /api/tokens/redeem` | 用户点击确认核销 | 成功跳转核销成功页 | 按 40001~40005 展示固定文案。 |
| API-4 `GET /api/me/photos` | 本期可不做；若加“我的写真”入口则调用 | 展示写真列表 | 未登录则重新登录。 |

### 5.1 API-3 错误态文案建议

| API code | businessCode | 前端文案建议 |
|---:|---|---|
| 40001 | `invalid_format` | “卡密格式不正确，请检查是否为 RFZJ-XXXX-XXXX-XXXX。” |
| 40002 | `not_found` | “未找到该卡密，请确认复制完整后重试。” |
| 40003 | `already_used` | “该卡密已被核销，无法重复使用。” |
| 40004 | `locked` | “连续输错次数较多，请等待倒计时结束后再试。” |
| 40005 | `round_not_available` | “当前暂无可计入的直播轮次，请稍后再试或联系工作人员。” |

## 六、页面实现细节

首页应作为彩排入口页，展示直播状态、当前集赞对象、目标人气、团队人气和卡密核销入口。若 `liveStatus=idle`，首页不应假装正在直播，而应显示“当前暂无直播互动，开播后将自动更新”。`updatedAt` 是 Unix 秒，应在前端格式化为本地时间。

人气看板页应保留“按序号排列·不设名次”的合规提示。player tab 展示 `number/name/teamName/value`；team tab 可把 `number` 视作 team_id，展示团队名称与团队人气；spy tab 本期 `spyTabEnabled=false`，前端应灰置或显示“卧底档将在对应环节开启”。前端不允许根据 value 排序，也不允许显示“第1名/冠军/榜首”等排名化表达。

卡密核销页应只在用户点击“粘贴”时调用剪贴板能力，不能页面打开自动读取。提交按钮应有 loading 与防重复点击状态。核销成功后建议把 API-3 成功 data 存入页面跳转参数或本地临时缓存，再跳到核销成功页；不要让成功页重新发起核销请求。

核销成功页应删除“会员有效期+7天”，改为“数字写真已自动存入我的收藏”。写真素材必须使用清新、才艺、舞台风方向；如彬少正式稿未完成，则使用合规占位图，并把替换素材作为前端资源替换任务，而不是改业务代码。

## 七、状态管理与请求封装

C11 不需要复杂状态管理库。建议 `utils/auth.js` 封装登录与 token 缓存，`utils/request.js` 封装 baseURL、统一响应、Bearer token 注入和错误处理。页面只调用业务 API 方法，不直接处理 HTTP 细节。

```text
utils/request.js
- request({ url, method, data, auth })
- 自动读取 token 并注入 Authorization
- code=0 返回 data
- code!=0 抛出业务错误对象 {code,message,data}

utils/auth.js
- ensureLogin()
- loginByTtCode()
- getToken()/setToken()/clearToken()
```

## 八、测试与验收标准

C11 的自动化测试能力取决于抖音开发者工具环境，仓库内至少应提供构建/静态检查与人工验收清单。若使用原生小程序，不一定有 npm build；但可通过代码结构检查、接口 mock 调试和截图验收证明完成度。

| 验收项 | 验证方式 | 通过标准 |
|---|---|---|
| 登录流程 | 真机/开发者工具截图 | 首页加载后成功拿到 token，后续核销请求带 Bearer。 |
| 首页 | 截图 | live/idle 均可展示；当前集赞对象与人气字段正确。 |
| 人气看板 | 截图 + 数据验证 | 3 号人气最高时仍按 1→2→3 展示。 |
| 卡密核销 | 截图/录屏 | 输入、粘贴、提交、loading、防重复点击完整。 |
| 错误五态 | 截图 | 40001~40005 每个错误码有固定文案。 |
| 核销成功 | 截图 | 展示 playerName/teamName/points/photoPreviewUrl/自动收藏提示。 |
| 合规素材 | 人工核查 | 无性感擦边写真；无“打榜/排名/榜首”等敏感表达。 |

## 九、建议 Claude 裁定的问题

| 编号 | 问题 | Manus 建议 |
|---|---|---|
| C11-Q1 | C11 本期是否严格只做 4 个彩排核心页？ | 建议是。API-4 我的写真可以后置，避免扩大范围。 |
| C11-Q2 | 彬少正式稿未完成时，是否允许先用占位图开发结构？ | 建议允许。Claude 已提出此路径。[1] |
| C11-Q3 | 首页四入口是隐藏还是灰置？ | 建议彩排版隐藏；若必须保留，则统一灰置并提示“暂未开放”。 |
| C11-Q4 | 人气页 spy tab 是否展示？ | 建议保留 tab 但灰置或显示未开启，因为 C9 返回 `spyTabEnabled=false`。 |
| C11-Q5 | 是否在 C11 加“我的写真”页？ | 建议暂不列入 C11 P0；若 Claude 认为必须验证 API-4，则增加一个极简只读页。 |
| C11-Q6 | 小程序 baseURL 与环境配置如何处理？ | 建议 `config.js` 区分 dev/staging/prod，彩排使用 staging 后端地址。 |

## 十、结论

C11 应以**彩排闭环**为目标：观众能登录、看首页状态、看合规人气看板、核销卡密并看到核销成功权益。它不应在本期承担完整会员、订单、识破投票、选手详情或电商跳转。只要严格使用 C9 API 契约、处理好错误态与素材合规，C11 就可以在彬少正式稿尚未完全到位时先完成结构开发，后续替换视觉资源。

## References

[1]: /home/ubuntu/upload/Claude审查_C9通过_后端收官_C10C11开卡.md "Claude 审查 — C9通过 后端收官 C10/C11开卡"
[2]: docs/小程序运维需求_报价版V2.0-2.md "小程序运维需求_报价版V2.0-2"
[3]: docs/API契约/当前页面级API契约定稿_C9依据_V1.0.md "当前页面级 API 契约定稿_C9依据_V1.0"
[4]: 红颜局中局·小程序 UI 界面布局与功能跳转逻辑说明_V1.0.md "小程序 UI 界面布局与功能跳转逻辑说明 V1.0"
[5]: docs/UI_review_observations_V1.0.md "UI 审查观察记录 V1.0"
[6]: 红颜局中局小程序功能定义文档_V2.5.md "小程序功能定义文档 V2.5"
