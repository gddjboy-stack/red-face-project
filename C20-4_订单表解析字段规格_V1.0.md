# C20-4 抖店订单导出表解析字段规格

**文档编号**：C20-4-SPEC-V1.0
**撰写方**：Manus AI
**日期**：2026 年 7 月 31 日
**用途**：为 C20-4「订单批量录入」的解析器提供字段契约，供 Claude 裁定与拆卡前对齐
**状态**：待 Claude 裁定第五节争议项；第一至四节可直接作为实现依据

---

## 摘要

John 因店铺尚无真实订单而无法导出样表，改以一份第三方实拍截图与我方对官方文档的核验共同构成本规格的证据基础。本文档确认了三件对实现最关键的事：解析器**必须按表头名称建立列映射而非依赖列位置**，幂等键**必须取「子订单编号」而非「主订单号」**，有效订单判定**必须是「订单状态 + 售后状态」的复合条件而非单列筛选**。

同时，本次核验推翻了一个原本可能被采纳的省事方案。John 提供的截图中存在「实销金额」一列，其取值恰好完美区分三类订单（退货订单为 0、取消订单为 0、正常订单为 253），单凭这一列即可完成有效性判定，工程上极为诱人。但经比对抖店官方标准报表的 84 个字段清单，**「实销金额」并不在官方导出字段之列**，该截图实为财务人员将订单报表、售后报表与成本表二次合并后的对账表。若按截图直接实现，我方 8 月拿到真实导出文件时解析器会因找不到该列而当场失效。本文档因此以官方字段为基线重写了判定逻辑，并将截图中的加工列降级为「存在则交叉校验」的可选增强项。

此外，核验官方文档时发现三项此前双方均未纳入考虑的现场操作约束：导出文件**经加密压缩且解压密码需手机验证码接收**、**两次导出之间强制间隔 5 分钟**、部分字段**次日才能导出**。这三项直接影响 8 月 9 日现场结算的操作流程设计，已在第六节提出应对建议。

---

## 一、证据来源与可信度分级

| 编号 | 来源 | 性质 | 可信度 | 用途 |
|---|---|---|---|---|
| A | 抖音图文笔记「电商财务jack」[1] | 第三方实操经验 | 中 | 提供导出路径与「子订单编号唯一」的经验判断 |
| B | John 转述的该笔记表格截图（5 张） | 第三方实拍数据 | 中高（图像清晰、三行样例语义完整） | 提供真实数据形态与测试用例 |
| C | 抖店官方《订单报表导出使用说明》[2] | **官方文档** | 高 | **字段清单与导出机制的最终依据** |
| D | 抖店官方《「订单管理」使用指南》[3] | **官方文档** | 高 | **订单状态与售后状态枚举的最终依据** |

**取证原则**：凡官方文档（C、D）与截图（B）冲突之处，一律以官方为准，截图仅作数据形态参考。本规格所有判定逻辑均建立在官方字段之上。

> 需要向团队说明的一点：来源 A 的原始图片受站点反爬保护无法程序化获取，是 John 手工截图补齐的。这批截图质量很高，三行样例恰好覆盖了「退货成功」「未发货取消」「正常完成」三种关键状态，可直接转为测试用例，避免了我方凭空构造测试数据。

---

## 二、导出操作规格（供 John 与运营执行）

官方文档明确导出分为标准报表与自定义报表两类，前者固定输出 84 个字段，后者可自选维度与字段[2]。导出维度的选择直接决定表格的行粒度：

> 「以商品维度导出时，报表中每个商品名称（子订单）列为一行；以订单维度导出时，报表中每个主订单号一行。」[2]

由此确立三条操作约束，**这三条是解析器能否工作的前提，必须写入运营 SOP**：

| 序号 | 约束 | 理由 |
|---|---|---|
| 1 | 导出维度**必须选「商品维度」** | 订单维度按主订单聚合，会丢失 SKU 与商家编码明细，无法归属到选手 |
| 2 | 报表类型**建议选「标准报表」** | 84 字段固定，列集合稳定；自定义报表的勾选项因人而异，且「记住本次选择」是子账号维度，换人操作即变形 |
| 3 | 商品上架时**「商家编码」必填** | 官方明确该字段为**选填**[2]，留空则选手归属列全空，解析必然失败 |

第 3 条需要特别提请 Vincent 与 John 注意：这是一个**在上架时就必须做对、事后无法补救**的动作。建议直接把商家编码设为选手编号（如 `P12`），比解析商品标题文本健壮得多。若上架时已经漏填，届时只能退回到「商品 ID → 选手」的人工映射表方案，需额外维护成本。

---

## 三、字段契约（解析器实现依据）

### 3.1 必需字段（官方标准报表内，缺失即拒绝解析）

| 字段名（表头精确匹配） | 官方定义[2] | 解析用途 | 截图示例值 |
|---|---|---|---|
| 子订单编号 | 订单内对应的子订单编号，即商品单单号 | **幂等键**，唯一标识一行 | 6930409165040916426 |
| 主订单号 / 主订单编号 | 订单的主订单编号，即店铺单号 | 仅作展示与人工追溯，**会重复** | 6930409165040916426 |
| 商家编码 | 商品创建设置【库存与价格】时填写的商家编码（选填） | **选手归属键** | 6972250333020 |
| 商品数量 | 该商品的数量 | **销量累加**，不可按行计数 | 1 |
| 订单状态 | 订单状态 | **有效性主判据** | 已完成 / 已关闭 |
| 售后状态 | 售后状态 | **有效性主判据** | 退款成功 / – |
| 支付完成时间 | 支付完成时间 | **场次归属依据** | 2024-06-01 01:13:29 |
| 订单应付金额 | 应付金额 = 商品金额 + 运费 − 平台优惠 − 商家优惠 − 主播优惠 | 金额核对 | 99 |

### 3.2 可选增强字段（存在则用于交叉校验，缺失不影响主流程）

| 字段名 | 是否官方字段 | 用途 |
|---|---|---|
| 选购商品 | 是 | 商家编码缺失时的降级归属依据 |
| 商品 ID | 是 | 商家编码缺失时的降级归属依据（更可靠，但需人工维护映射表） |
| 商品规格 | 是（仅标准报表） | 人工核对 SKU |
| 实销金额 | **否，加工列** | 若存在则校验：实销金额 > 0 应与主判据结论一致，不一致时告警 |
| 售后类型 / 退货数量 / 售后原因 | **否，加工列** | 若存在则用于复盘分析（如统计「7天无理由」占比） |

**「实销金额」的处理原则必须写死为「有则校验、无则忽略」**，绝不可作为主判据。这是本次核验最重要的一条结论，理由见摘要。

### 3.3 数据清洗规则

截图显示空值以横杠 `–`（U+2013 EN DASH，非 ASCII 连字符）呈现。解析器需将 `–`、`-`、空字符串、纯空白一律归一化为空值。金额与数量列需容忍千分位逗号与全角字符。

---

## 四、有效订单判定逻辑

官方对账口径给出了明确指引，且附带一条重要警示：

> 「售后关闭不等于售后成功，如用户撤销售后或商家拒绝售后超时关闭会进入售后关闭状态。如需对账可筛选退款成功 & 换货成功 & 补寄成功。」[3]

这条警示意味着**不能用「售后状态为空」作为有效性判据**——售后关闭的订单同样是有效订单，而售后中的订单状态待定。据此确立判定逻辑：

| 步骤 | 条件 | 结论 |
|---|---|---|
| 1 | `订单状态` ∈ {待支付, 已关闭} | **无效**，直接排除 |
| 2 | `订单状态` ∈ {已发货, 部分发货, 已完成} 且 `售后状态` = 退款成功 | **无效**，已退款 |
| 3 | 同上且 `售后状态` ∈ {售后中, 待商家处理} | **挂起**，见下文 |
| 4 | 同上且 `售后状态` ∈ {空, 售后关闭} | **有效**，计入销量 |
| 5 | `订单状态` = 待发货 | **有效但需标注**，已付款未发货，正常计入 |

官方枚举为：订单状态取「待支付、待发货、已发货、已完成、已关闭」五种，售后状态取「售后关闭、售后中、退款成功、待商家处理」四种[3]。截图中另见「部分发货」，属多包裹分批发货场景，按有效处理。

**三行截图样例正好可作为验收测试用例**，语义已完整还原：

| 样例 | 订单状态 | 售后状态 | 业务语义 | 期望判定 |
|---|---|---|---|---|
| 第 2 行 | 已关闭 | 退款成功 | 已发货后买家以「7天无理由」退货退款成功，实销归零 | 无效 |
| 第 3 行 | 已关闭 | 空（–） | 未发货即取消或超时未支付 | 无效 |
| 第 4 行 | 已完成 | 空（–） | 正常完成、无售后 | 有效，计 1 件 |

---

## 五、需 Claude 裁定的三项争议

### 争议一：「售后中」订单如何处理（阻塞实现）

第三条判定路径涉及业务规则而非技术选择，我不自行决定。三种处理方式的代价各不相同：

| 方案 | 做法 | 优点 | 风险 |
|---|---|---|---|
| A | 不计入 | 结果保守，不会出现「宣布后又缩水」 | 若买家最终撤销售后，该选手被少算，可能引发争议 |
| B | 计入 | 数据完整 | 售后成功后需回冲，出现「已宣布成绩又变动」 |
| C | 计入但**单独标注为「待定 N 件」** | 信息透明，把不确定性显式暴露给主持人与选手 | 需前端多一个展示位，主持人口播需配套话术 |

**我倾向 C**。理由是这与我在《8月综合作战方案》评估中提出的极速退款问题同源：不确定性客观存在且我方无法消除，与其在后台悄悄做二选一，不如显式呈现，让运营层面用话术兜住。若 Vincent 认为现场不宜出现「待定」字样，则退到 A。

### 争议二：「当前总数」的语义（承接上一轮未决问题）

这是我在上一轮评估中已提出、尚未获裁定的问题，此处再次标注为阻塞项：C20-4 录入的「当前总数」究竟指选手总人气，还是仅指某一来源的累计值？两种算法在多来源并存时会互相吞掉贡献。此项不锁定，水位线逻辑无法动工。

### 争议三：解析结果是否落独立表

按 C20-3-FIX 已确立的「不同业务来源分表存储」原则，订单销量理应存入独立表（如 `order_sales_ledger`）而非混入人气账本。我倾向沿用该原则，但请 Claude 确认是否要在 C20-4 一并建表，或先只做解析与预览、暂不落库。

---

## 六、三项现场操作风险（本次核验新发现）

官方文档披露的以下机制，双方此前的方案讨论中均未纳入考虑，且都直接命中 8 月 9 日现场结算环节：

| 风险 | 官方原文依据[2] | 对现场的影响 | 建议应对 |
|---|---|---|---|
| 文件加密 | 「导出文件已加密处理，解压密码发送至验证手机/邮箱，需输入密码解压后才能打开」 | 拿到的不是可直接读取的 xlsx，需先收验证码再解压 | John 手机必须在场；提前演练一次完整流程 |
| 导出限频 | 「导出报表后，再次导出时间需间隔至少 5 分钟」 | 若首次导出筛选条件填错，重试要等 5 分钟 | 现场结算**不依赖临场导出**：提前导出基准数据，现场只做增量人工核对 |
| 字段延迟 | 「作者UID、作者账号字段需次日才能导出」 | 若统计口径涉及达人归属，当日拿不到 | 8 月口径不涉及达人分成，暂不受影响，但若后续引入需重新评估 |

其中**限频与加密两项合并起来意味着「现场临时导出」是一条不可靠路径**，最坏情况下需要 10 分钟以上且依赖手机信号。我建议把现场结算流程明确改为「提前导出 + 现场增量核对」，这属于运营流程设计，需 Vincent 与 John 确认后再由 Claude 决定是否影响 C20-4 的功能边界（例如是否需要「基准数据 + 增量补录」的双入口）。

---

## 七、待补充信息（非阻塞）

两项截图未能覆盖的信息，建议 John 在店铺产生首笔真实订单后补充确认：一是「选购商品」列的实际文本格式（截图中该列因列宽为空），二是是否存在赠品标识列。后者关系到我方在方案评估中提出的「赠品价款扣除」控损方案能否在数据层验证执行情况。这两项均不阻塞 C20-4 主体实现，但会影响降级方案与复盘能力。

---

## References

[1] 电商财务jack，《抖店订单导出表实操》，抖音图文笔记，2024-12-28：https://www.douyin.com/note/7453263955000167732
[2] 抖音电商学习中心，《订单报表导出使用说明》，最近更新 2026-03-09：https://school.jinritemai.com/doudian/wap/article/102418
[3] 抖音电商学习中心，《抖店「订单管理」使用指南》，最近更新 2026-07-08：https://school.jinritemai.com/doudian/wap/article/aHz4Rb6T2GLz
