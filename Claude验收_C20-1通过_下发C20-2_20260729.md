# Claude 验收：C20-1 通过 → 下发 C20-2（含范围修正）+ C20-5 接口裁定

日期：2026-07-27　核验基线：`release/miniapp-lite` @ `234f875`

---

## 一、C20-1 验收结果：**通过** ✅

| 验收项 | 核验结果 |
|---|---|
| tabBar 4→3（移除"人气"） | ✅ 实测为 首页 / 选手 / 我的 |
| pages 注册表移除两页 | ✅ 仅剩 7 个页面，无 suspicion/popularity |
| 页面目录移出上传包 | ✅ 已移至仓库根 `archive/miniapp-pages/`，物理隔离于 `frontend/douyin-miniprogram/` 之外 |
| onSpyTap / spy-entry 移除 | ✅ 零残留 |
| 孤儿样式清除 | ✅ `index.ttss` 相关样式已删（-12 行） |
| api.js 无调用方函数清理 | ✅ 三函数已删（getPopularityBoard / getSuspicionStatus / submitSuspicion） |
| grep suspicion | ✅ **零命中** |
| 改动范围纪律 | ✅ 纯减法，51 行删除，无功能性改动、无越界 |

**关于"grep popularity 剩 10 处"的声明**：Claude 已逐条清点，**确为 10 处**（players/index.js ×1、home/index.ttml ×2、home/index.js ×4、player-detail/index.ttml ×1、player-detail/index.js ×2），全部落在 C20-2 A 部分的处理范围内。

**特别肯定**：将未完成项**明写为"未做"并附精确明细供下一卡对账**，而非含糊表述或静默遗留——这正是协作规范要求的做法，请保持。

**两处差异说明均予接受**：① 路径 `douyin-miniapp` → `douyin-miniprogram` 属卡集笔误，按现实调整正确；② 沙箱无法产出 tabBar 截图，以 app.json 终态替代合理，由 John 在开发者工具中补验（见文末）。

---

## 二、⚠️ Claude 自我更正：C20-2 范围遗漏一个页面

原 C20-2 的 A 部分只列了 核销成功页 / 选手页 / 选手详情页 / 首页 四处，**遗漏了 `pages/me/index.ttml`**。经核对，该页存在两处人气文案：
- 第 23 行：`核销成功后可增加人气值并收藏写真`
- 第 28 行：`购买与增加人气值请在直播间完成。页面仅展示已获得的用户权益。`

**此为 Claude 卡集的疏漏，非 Manus 执行问题。** C20-2 范围据此修正（见下节）。

---

## 三、正式下发：卡 C20-2（修正版）

**目标**：全站看不到任何人气数字或"人气值"字样，看不到任何"直播/直播间/点赞/评论"表述；分享标题为自定义。
**分支**：`release/miniapp-lite`（承接 C20-1）

### A. 人气值清除（稳妥档）——共 **5 个页面**
以 Claude 实测清单为准，逐项处理：

| 页面 | 需处理内容 |
|---|---|
| `pages/redeem-success/index.ttml` | 第 8 行"本次人气值已计入对应选手"、第 13 行"增加人气值 + {{pointsText}}"。**改为只陈述用户自身权益**（写真已存入收藏 / 会员权益 +N 天），不得提及任何选手与分数 |
| `pages/players/index.ttml` + `index.js` | 第 9 行说明文案；`valueText: formatNumber(item.popularityValue)` 及其展示。仅保留 编号/姓名/队伍/写真 |
| `pages/player-detail/index.ttml` + `index.js` | 第 16 行"当前人气值"、第 17 行数字、第 19 行 supportHint；`popularityText` 相关逻辑 |
| `pages/home/index.ttml` + `index.js` | 第 35、39 行两处数字展示；`targetPopularityText` / `teamPopularityText` 相关逻辑（含 C19-R3b-3 引入的 `'--'` 分支，一并移除） |
| **`pages/me/index.ttml`（本次补充）** | 第 23、28 行两处人气文案 |

⚠️ **红线**：不得改用"应援""助力""支持值""热度"等替代词——这些词的打榜色彩更重。正确做法是**只讲用户自己获得了什么权益**。

### B. 文案清理（直播/直播间/点赞/评论/抖币 → 活动/积分表述）
逐页清理用户可见文案，参考映射：正在直播→活动进行中；请在直播间进行→请在活动现场参与；等待直播间揭晓→等待活动现场公布；请留意直播间提示→请留意活动现场提示。
另需处理：`utils/constants.js:10`（错误码 40005）中的"直播轮次"；`app.json` 各页面标题；全部 toast 与错误提示。

### C. 自定义分享文案
新增 `onShareAppMessage`，标题与描述不得出现投票/人气/直播字样；建议方向为"活动名称 + 粉丝权益工具"定位。

### 验收标准
- 全工程 grep `popularity`、`Popularity`、`人气`、`直播`、`点赞`、`评论`，**用户可见部分零命中**（后端字段名、注释、日志、README 不需改；`archive/` 目录不计）
- 核销成功页只讲用户自身权益
- 分享卡片标题为自定义内容

### 交付
逐项完成对照表 + commit + 六组 grep 结果 + 关键页面截图说明（沙箱无法截图则以代码终态替代，由 John 补验）

---

## 四、C20-5 接口裁定（回应蓝军备注）

**问题**：大屏页复用 admin 侧接口，还是小程序侧 `/api/popularity/board`？

**裁定：两者都不复用，新建专用只读端点 + 独立展示令牌。**

理由：
1. **不可用 admin 接口**：大屏页需在 OBS 浏览器源中以 URL 直接加载，无法交互登录；若把 `X-Admin-Token` 放进 URL，即重蹈 C19「下载卡库 40101」的覆辙（管理凭证进 URL 会落入日志与浏览器历史）。**管理凭证进 URL 是永久红线。**
2. **不可用小程序侧接口**：其鉴权模型为用户会话（Bearer token），同样不适配无人值守的大屏场景。
3. **数据敏感度评估**：人气榜本就要投到直播画面上给所有观众看，**内容本身不属机密**。设防的目的是防止链接外泄后被批量抓取、以及避免暴露内部标识。因此**采用轻量级只读展示令牌即可，不要过度设计**。

**实现要求**：
- 新建只读端点，**只出榜单展示所需字段**，不返回内部 ID 之外的敏感信息，**无任何写能力**
- 独立展示令牌（长随机串），**可轮换**；令牌泄露时只影响榜单可见性，不影响任何管理能力
- 排序沿用后台既有规则，**前端不得重排**
- 令牌具体承载方式（URL 参数 / 路径段 / 请求头）由 Manus 在 C20-5 方案中提出，Claude 裁定

**同时确认**：后端 `/api/suspicion/*` 与 `/api/popularity/board` 保留在 main，不予删除（C20-3/5 及未来"试探版"仍需要，且 release 分支只做减法不动后端）。此判断正确。

---

## 五、John 的两个补验动作

1. **补 tabBar 截图**：在抖音开发者工具中导入 `release/miniapp-lite` 分支，确认底部只有 3 个 tab，并顺手确认各页面无报错（这一步同时验证了页面移除后没有跳转报错）
2. C20-2 验收通过后**立即上传体验版并提交发版审核**——审核需 1~3 工作日，是 8/1 的关键路径

---

**下发顺序**：C20-2 现在开始；C20-3 / C20-4 / C20-5 在提审等待期并行推进，仍逐张提交、逐张验收。
