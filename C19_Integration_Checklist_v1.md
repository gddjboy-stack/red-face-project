# C19 联调与验收清单 v1

## 一、本地已验证项（沙箱环境）
| 编号 | 验证项 | 当前结果 |
|---|---|---|
| C19-BE-01 | 投票多选接口防重（重复选同一个人不报错，返回 duplicated） | 已通过 C13 专项测试 |
| C19-BE-02 | 手动加成（选手级与团队级）幂等与日志 | 已通过后端编译与全量测试 |
| C19-BE-03 | `display_code` 唯一约束与 `number` 自动递增 | 已通过后端全量测试 |
| C19-BE-04 | 卧底看板仅返回 `isSpy=true` 成员 | 已通过 C9 看板专项测试 |
| C19-FE-01 | 识破页按组展示与多选交互 | 代码已就绪，待真机体验 |
| C19-FE-02 | 下载卡库改用 fetch，不暴露凭证 | 代码已就绪，`pnpm build` 通过 |

## 二、生产/预发环境验收步骤（待运维配合）
1. **数据库更新**：
   - 执行 `db_schema.sql` 中关于 `display_code`、`team_round_stats.coefficient` 和 `team_coefficient_ledger` 的变更。
2. **后台操作验收**：
   - 登录后台，录入新选手，确认需填写 4 位数“编号”，保存后表格显示自增“序号”。
   - 在“场控操作”点击“开启卧底识破”，确认监控页状态变为“进行中”。
   - 在“手动加成”给某团队 +10，观察“基础数据-团队管理”或看板中的人气是否乘以 1.1。
3. **小程序验收**：
   - 观众端进入“卧底识破”，确认选手按队伍分组。
   - 勾选多名选手提交，提示成功；再次进入，已投选手显示“已投”且不可点。
   - 点击核销页“粘贴卡密”，若未授权剪贴板，确认是否弹出中文提示“请长按手动粘贴”。
