# C12 全链路联调检查清单 v1

> 制定人: Manus AI
> 日期: 2026.06.17
> 针对: 红颜·局中局 C12 (H5 Bridge & Full-link Integration)

---

## 〇、总览

本清单旨在指导 C12 H5 Bridge 与全链路集成的联调测试，确保所有组件（Admin, Backend, Mini-program, H5 Bridge）协同工作。清单将明确标注在开发环境已验证的项和需要等待 6/22 真实环境测试的项。

---

## 一、H5 Bridge 功能验证

| 序号 | 测试项 | 预期结果 | 验证状态 | 备注 |
|---|---|---|---|---|
| 1 | H5 页面加载 | 页面正常加载，显示标题“红颜·局中局 - 核销页” | 开发环境已验证 | |
| 2 | URL 参数 `t` (token) 解析 | URL 中包含 `t` 参数时，H5 页面能正确解析并显示在“核销凭证”区域 | 开发环境已验证 | |
| 3 | URL 参数 `oid` (订单号) 解析 | URL 中包含 `oid` 参数时，H5 页面能正确解析并显示在“订单号”区域（仅展示） | 开发环境已验证 | |
| 4 | 无 `t` 参数处理 | URL 中不包含 `t` 参数时，“核销凭证”区域显示“未找到核销凭证”，两个按钮禁用 | 开发环境已验证 | |
| 5 | “打开抖音小程序核销”按钮 (自动跳转) | **当 `miniProgramLinkBase` 配置时**，点击按钮能尝试跳转到抖音小程序核销页，并携带 `token` 参数 | 待 6/22 真机验证 | 依赖抖音平台生成链接，目前 H5 暂未配置 `miniProgramLinkBase` |
| 6 | “打开抖音小程序核销”按钮 (降级处理) | **当 `miniProgramLinkBase` 未配置时**，按钮隐藏，显示“如果无法自动打开小程序，请手动打开抖音 App...”的降级提示 | 开发环境已验证 | |
| 7 | “复制凭证并手动打开小程序”按钮 | 点击按钮能将显示的 token 复制到剪贴板，并弹出提示“核销凭证已复制到剪贴板！” | 开发环境已验证 | |
| 8 | 复制失败处理 | 复制失败时，能提示用户手动复制 token | 开发环境已验证 | 模拟 `navigator.clipboard.writeText` 失败场景 |

---

## 二、小程序核销页功能验证

| 序号 | 测试项 | 预期结果 | 验证状态 | 备注 |
|---|---|---|---|---|
| 1 | `onLoad` 读取 `options.token` | 小程序核销页 `onLoad` 时，能从 `options` 中读取 `token` 参数并预填到输入框 | 开发环境已验证 | |
| 2 | 预填 token 后核销 | 预填 token 后，用户点击“确认核销”能正常发起核销请求 | 开发环境已验证 | 需后端配合提供有效 token |
| 3 | 剪贴板粘贴功能 | 用户点击“粘贴”按钮，能从剪贴板读取 token 并填充到输入框 | 开发环境已验证 | 保持 C11 合规红线，不自动读取剪贴板 |
| 4 | 核销成功跳转 | 核销成功后，能正确跳转到核销成功页 | 开发环境已验证 | |
| 5 | 核销失败提示 | 核销失败时，能显示后端返回的错误信息和冷却时间 | 开发环境已验证 | 需后端配合提供失败场景 |

---

## 三、全链路集成测试

| 序号 | 测试项 | 预期结果 | 验证状态 | 备注 |
|---|---|---|---|---|
| 1 | Agiso 插件生成 H5 链接 | Agiso 插件能生成包含 `t` 和 `oid` 参数的 H5 链接 | 待 6/22 真机验证 | 依赖 Agiso 插件配置和真实环境 |
| 2 | H5 链接 -> H5 Bridge -> 小程序 (自动跳转) | 用户点击 Agiso 生成的 H5 链接，H5 Bridge 自动跳转到抖音小程序核销页，并预填 token | 待 6/22 真机验证 | 依赖抖音平台生成链接和真实环境 |
| 3 | H5 链接 -> H5 Bridge -> 小程序 (复制降级) | 用户点击 Agiso 生成的 H5 链接，H5 Bridge 提示复制 token，用户手动打开小程序并粘贴核销 | 开发环境已验证 | 模拟自动跳转失败场景 |
| 4 | 后端核销接口调用 | 小程序核销成功后，后端能正确处理核销请求，更新数据状态 | 开发环境已验证 | 需后端提供有效 token 和模拟环境 |
| 5 | 管理后台数据同步 | 后端核销成功后，管理后台能实时或准实时显示核销结果 | 开发环境已验证 | 需管理后台配合 |

---

## 四、编码边界与合规性检查

| 序号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 1 | `frontend/h5-bridge` 目录是否纯静态页 | ✅ 是 | 无后端逻辑，无构建工具 |
| 2 | 小程序核销页 `onLoad` 是否仅读取 `options.token` 预填 | ✅ 是 | 未改变其他逻辑或合规红线 |
| 3 | H5 跳转小程序是否采用“配置化链接 + 复制降级” | ✅ 是 | 未硬编码未验证的跳转 scheme |
| 4 | `oid` 是否仅在 H5 展示，未上报后端 | ✅ 是 | 保持核销链路简单 |
| 5 | 未新增后端生成跳转链接接口 | ✅ 是 | 遵循 Claude 裁定，留待后续 |
| 6 | 未改变 C2~C9 后端核心逻辑 | ✅ 是 | 保持后端稳定性 |
| 7 | 未改变 C11 核销页其他逻辑/合规红线 | ✅ 是 | 确保合规性 |

---

## 五、下一步计划

1.  将此联调清单上传至 GitHub。
2.  执行本地联调测试，并根据测试结果更新清单中的“验证状态”和“备注”字段。
3.  准备最终彩排报告，包含代码变更、联调清单和本地测试截图（尽力而为）。
4.  等待 6/22 真实环境测试，验证“待 6/22 真机验证”项。

---

## 引用

[1] Claude 裁定 — C12 技术方案确认(彩排底线最后一张,可编码) - `/home/ubuntu/upload/Claude裁定_C12方案_彩排底线最后一卡.md`
md`
`
`
