# 《红颜局中局》小程序 — "上线实施"补充报价需求(基础版 V2)

> 文档用途:在《运维需求报价版 V2.0》基础上,补充"一次性上线实施"的报价需求
> 出具日期:2026.06.16
> 商务联系人:John(项目经理)
> 说明:本报价聚焦"一次性上线部署实施",请单独报价,与周期性运维费用分开。

---

## 一、背景与交付目标

**系统现状**:后端核心已开发完成并通过逐模块代码审查,前端(抖音小程序 + Vue 场控后台 + H5)收尾中,文档与测试齐全。企业抖音开放平台账号已认证,具备创建小程序条件。

**本次交付目标(分两个节点)**:

> **节点一(6/22 前):完成后端到云服务器的部署上线**,含域名解析、HTTPS 证书、抖音合法域名配置,使系统具备被抖音平台真实访问的条件。
>
> **节点二(6/22):在真实云环境下完成全部功能测试通过**,包括接入抖音直播间和所有 API 对接。

**关于 6/22 这个时间节点**:抖音 API 对接有硬性要求——小程序只能调用 HTTPS 的、配置在抖音后台白名单的域名,直播与订单数据也只能推送到公网可访问的后端。因此后端部署到云服务器并配好合法域名,是真实 API 对接测试的前提。本阶段交付的是**体验版真机全功能跑通(含真实 API)**,体验版无需 ICP 备案即可真机测试。

**本阶段非面向公众正式上线**:公众正式上线预计 7 月下旬,需 ICP 备案与上架审核,作为第二阶段另行报价。

**以下由甲方自行承担,不在本次报价范围内**:

- 域名、云服务器、云数据库采购(甲方账号)
- ICP 备案主体申请(甲方并行推进,用于后续公众上线)
- 抖音小程序创建及各平台主体账号(已就绪)
- 直播玩法 API、抖店、阿奇索的对接测试(由甲方自行完成,详见第三部分)

---

## 二、需要贵方报价的工作(最小必要集合)

请按工作项分别评估**人天**与**费用**。

### D1|后端部署到生产环境(核心)

| 内容 | 要求 |
|---|---|
| 在甲方云服务器部署 Java 后端 + MySQL 数据库 | 运行环境配置、应用部署、数据库初始化建表 |
| 域名解析 + HTTPS 证书配置 | 配好 SSL,满足抖音小程序"HTTPS 合法域名"要求 |
| 抖音小程序后台"request 合法域名"配置 | 让体验版小程序能正常调用生产后端 |

> 甲方可提供部署文档与脚本,降低上手成本。

### D2|对象存储与图片(写真功能需要)

| 内容 | 要求 |
|---|---|
| 配置对象存储(OSS) | 存数字写真图片;CDN 本阶段不需要 |
| 与后台写真上传(C17)对接 | 上传图片能存入并在小程序展示 |

### D3|基础安全(够用即可)

| 内容 | 要求 |
|---|---|
| 场控后台访问控制 | 限制内网/IP 白名单,或配合开发方权限系统 |
| 数据库安全 | 不开公网、强密码 |
| 服务器基础安全 | 防火墙/安全组、关闭非必要端口 |
| 密钥管理 | 敏感密钥用环境变量,不落代码 |

> 高级安全(完整 DDoS 防护、专业渗透测试等)本阶段不做,云厂商自带基础防护即可。

### D4|基础保障(底线)

| 内容 | 要求 |
|---|---|
| 数据库自动备份 | 开启云数据库自动备份,确保数据不丢失 |
| 基础日志 | 应用日志可查,出错能定位 |

> 完整监控告警体系、压力测试本阶段不做(当前 50 人在线规模用不上),列为后续公众上线前再评估。

---

## 三、职责分工

| 工作 | 谁做 |
|---|---|
| 后端部署到云服务器 + 域名 + HTTPS + 抖音合法域名配置 | **运维方**(6/22 前完成,是测试的前提) |
| 直播玩法 API、抖店、阿奇索的对接**测试** | **甲方自行完成**(在运维方已部署好的云环境上进行) |
| 软件功能开发(含 API 对接的代码) | 开发方 |

> 简言之:运维方负责把系统部署到云、配好域名,让抖音能访问到;甲方负责在该云环境上把各项 API 真实对接测通。运维方部署完成是甲方测试的前提,因此部署须在 6/22 前到位。

---

## 四、本阶段明确不在报价范围(后续第二阶段)

| 项目 | 说明 |
|---|---|
| ICP 备案配套、公众上线、上架审核 | 第二阶段(7 月下旬),另行报价 |
| 高并发压测、Redis、CDN、完整监控告警 | 当前规模用不上,后续上规模再议 |
| 真实直播大并发优化 | 后续公众上线前再评估 |

---

## 五、报价格式(与周期性运维分开)

本次为"一次性上线实施"整体报价,涵盖第二部分 D1–D4 全部内容,请给一个总价即可,无需逐项拆分。

| 项目 | 人天 | 费用 | 可交付时间 |
|---|---|---|---|
| 基础版上线实施(含 D1–D4) | | | |

**另请说明**:

1. 是否有抖音小程序**部署上线**实操经验
2. 能否配合甲方 **6/22 真实环境测试通过** 的时间目标
3. 是否接受"开发方负责软件、贵方负责部署与基础设施"的分工

---

## 六、时间与衔接(关键)

1. **6/22 前**:运维方完成云服务器部署上线(后端 + 数据库 + 域名 + HTTPS + 抖音合法域名),这是甲方 6/22 测试的前提,为本阶段第一硬性节点。
2. **6/22**:甲方在已部署的真实云环境完成全功能测试(含抖音直播间与各 API 对接)。
3. **交接安排**:甲方项目经理 6/21 后离岗,服务器/域名/账号等交接须在此前完成。请运维方据此安排介入时间,确保部署在 6/21 前到位。
4. **第二阶段**:公众正式上线(含 ICP 备案、审核、可能的并发优化)为第二阶段,预计 7 月下旬,另行评估报价。ICP 备案由甲方并行启动,不在本报价内。

---

## 七、补充

1. 服务器与云资源费用由甲方承担。
2. 软件功能开发由开发方完成,贵方负责部署、基础设施与配合。
3. 本阶段原则:能用即可、控制成本,只做"真实环境跑通"的最小必要工作。

---

*本文档为"基础版上线实施"补充报价需求,聚焦 6/22 真实环境测试交付。公众上线工作另作第二阶段。*
