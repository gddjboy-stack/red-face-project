# C20-5 人气榜大屏页 · 逐项完成对照表 V1.0

> 执行方：Manus　审阅方：Claude　审阅人：John（进度/成本）、Vincent（娱乐性）、彬少（视觉）
> 任务卡来源：`C20工作卡集_8月1日冲刺.md` 卡 C20-5
> 鉴权方案依据：`Claude裁定_Manus蓝军评估V2.0_20260731.md` 第 2.2 条
> 测试结果：**全量 128 项通过，BUILD SUCCESS**（改动前基线 110 项，本次新增 18 项）

---

## 一、任务卡逐项对照

| # | 任务卡要求 | 状态 | 落地位置与说明 |
|---|---|---|---|
| 1 | 新增只读页面，**复用后台现有人气接口**，不新建数据逻辑 | 完成 | `DisplayBoardService` 直接复用 `C9QueryMapper.selectPopularityBoard` 与 `GroupVoteLedgerMapper.summarize`，未新增任何 SQL、未新增任何表 |
| 2 | 每 2~5 秒轮询刷新（8/1 用轮询，WebSocket 留给方向 C） | 完成 | 前端 `POLL_INTERVAL_MS = 3000`，取区间中值。未引入 WebSocket |
| 3 | 深色背景、大字体、高对比；**排序沿用后台既有规则，不得在前端重排** | 完成 | 深色主题 `#0d0a10` 底 + 金色 `#d79a39`；名次由**后端**计算并按序返回，前端仅按数组顺序渲染，代码中无任何 `sort` |
| 4 | 新增「生成分享图」按钮，一键导出图片，版式统一 | 完成 | 原生 Canvas 2D 手绘，输出 1080×1440 PNG；固定取前 10 名以保证版式恒定 |
| 5 | 需彬少提供一版分享图模板规范（配色、logo 位、标题区） | **待彬少** | 已按现有品牌色出可用版本并**预留 logo 位**（左上 120×120，`drawImage` 一行接入）。彬少出稿后只需改 CSS 变量与 Canvas 绘制参数，不动逻辑 |
| 6 | 页面能否无登录展示？**不可**。方案由 Manus 提出、Claude 裁定 | 完成 | 按裁定 2.2 实现「独立只读展示令牌」，与 admin 令牌物理隔离 |

## 二、验收标准对照

| 验收标准 | 结果 | 实测证据 |
|---|---|---|
| 网址打开即为实时榜单 | 通过 | 换票后立即出榜，见截图 `02_选手人气榜大屏.webp` |
| 数据随后台变化自动更新 | 通过 | 页脚显示「每 3 秒自动刷新 · 最近更新 15:44:45 · 共 12 条」，时间戳随轮询递增 |
| 一键出图版式正确 | 通过 | 导出样例 `05_分享图导出样例_1080x1440.png`，1080×1440 实测像素确认 |
| 未授权无法访问 | 通过 | 无凭证 GET 返回 401 / 40110；错误令牌换票返回 401 |

## 三、鉴权实现与 Claude 裁定 2.2 的逐条对齐

| 裁定要求 | 实现 |
|---|---|
| 独立只读令牌，与 admin 令牌**物理隔离** | 独立配置项 `redface.display.token`、独立拦截器 `DisplayAuthInterceptor`、独立错误码。两套令牌互不通用（已有测试断言 admin 令牌无法访问 display 接口，反之亦然） |
| display 侧 **fail-closed** | 未配置 `DISPLAY_TOKEN` 时**全量 401**，不沿用 admin 侧为联调保留的放行逻辑。专项测试 `DisplayFailClosedC20Test` 5 项覆盖 |
| 展示端**只读** | `/api/display/**` 仅允许 GET / HEAD，其余方法在拦截器层直接 405，不进入控制器 |
| 令牌不得外泄风险 | 换票端点 `/api/display-auth/session` 下发 `HttpOnly` + `SameSite=Lax` + `Path=/api/display` Cookie。令牌**不进 URL、不进 localStorage**，拍屏与截图均不泄露 |
| 生产环境护栏 | `ProdSecurityGuard` 启动时强制：DISPLAY_TOKEN 必须配置、**不得与 ADMIN_TOKEN 相同**、`cookie-secure` 必须为 true |
| 防爆破 | `DisplayLoginGuard` 按客户端 IP 计失败次数，超限返回 429 / 42901 |

**换票端点为何不放在 `/api/display/` 下**：若放在该前缀内，它自身是 POST，会与「display 路径全只读」冲突，必须维护 exclude 名单——而 exclude 名单是最容易在后续迭代中被误改出漏洞的地方。改为独立前缀后，只读约束在路由层面**没有任何例外**。

**双通道设计（Cookie + 请求头）**：主通道为 Cookie。同时保留 `X-Display-Token` 请求头通道，若 8/9 现场实测 OBS 浏览器源不持久化 Cookie，改用 OBS 自定义请求头即可，**后端零改动**。这是对现场不确定性的对冲，不是冗余。

## 四、蓝军自查：本卡实施中发现并修复的两个既有缺陷

这两项**不在任务卡范围内**，是写测试与实测时撞出来的，均已修复并纳入回归。

**缺陷一：全站「方法不支持」返回 500 而非 405。** `GlobalExceptionHandler` 缺少 `HttpRequestMethodNotSupportedException` 映射，被兜底 `Exception` 处理器吞成 500。影响范围是**所有接口**，不止 display。现场后果：运营看到 500 会去查服务器和数据库，实际只是请求方法用错。

**缺陷二：访问任意不存在的路径返回 500 而非 404。** 同一个兜底处理器吞掉了 `NoResourceFoundException`。实测 `/`、`/display/`、`/nonexistent` 全部返回 500。同时发现该兜底处理器**完全不打日志**，丢弃了异常堆栈——这意味着 8/9 现场若出现任何未预期异常，日志里只有一句「服务器内部错误」，无法定位。已补 `log.error` 输出完整堆栈。

**第二个缺陷的严重性高于第一个**：它让「网址敲错」和「系统崩了」在现场表现完全一致，而这两件事的处置方式截然不同。

## 五、蓝军自查：前端实测暴露并修复的两个问题

浏览器实测（非仅单元测试）暴露：**轮播末屏留白**——12 条数据每屏 8 条时，第 2 屏只剩 4 条，画面下半部分全空。直播画面出现大片空白比少显几条更难看，已改为末屏「对齐底部」策略。**缺少屏数指示**——轮播到第 2 屏时观众会误以为榜单从第 9 名开始，已在右上角加「第 X / Y 屏」。

另修一处隐蔽问题：选手被淘汰导致条目变少时，若当前屏号已越界，画面会停在一个不存在的屏上变成空白。已在渲染时重算屏数并修正越界屏号。

## 六、现场运维须知（请 John 确认）

| 事项 | 内容 | 需要 John 做什么 |
|---|---|---|
| 生产必须 HTTPS | prod profile 强制 `cookie-secure=true`，裸 http 直连内网 IP 会**拒绝启动** | 确认 8/9 部署形态。若确定为裸 http，需我加显式降级开关——但我**不建议自动降级**，那等于把安全约束变成可静默绕过的装饰 |
| 多一个密钥要管 | `DISPLAY_TOKEN` 必须与 `ADMIN_TOKEN` 不同 | 生成并分发展示令牌，与 admin 令牌分开保管 |
| 令牌轮换 | 令牌泄露时改配置并重启即可失效全部会话；Cookie 有效期 12 小时（`Max-Age=43200`），覆盖单场直播 | 确认 12 小时是否够用；若为多日连播，可延长 |
| OBS 采集 | 页面按 `h` 键可隐藏底部按钮条，避免操作按钮进入直播画面；`全屏` 按钮进入无边框全屏 | 现场彩排时实测一次 OBS 浏览器源能否持久化 Cookie |

## 七、交付物清单

| 类型 | 路径 |
|---|---|
| 大屏页 | `backend/redface-backend/src/main/resources/static/display/index.html` |
| 后端新增 | `config/DisplayProperties.java`、`web/DisplayAuthInterceptor.java`、`web/DisplayLoginGuard.java`、`query/DisplayBoardService.java`、`controller/DisplayController.java`、`dto/DisplayBoardResponse.java`、`dto/DisplayBoardItem.java` |
| 后端修改 | `web/WebConfig.java`、`web/ProdSecurityGuard.java`、`api/GlobalExceptionHandler.java`、`application.yml`、`application-prod.yml` |
| 测试 | `DisplayAuthC20Test.java`（13 项）、`DisplayFailClosedC20Test.java`（5 项） |
| 演示启动器 | `DisplayDemoServerC20.java`（类名不以 Test 结尾，不进 CI；人工验收用） |
| 截图与出图样例 | `evidence/C20-5/01_令牌闸门.webp` ~ `05_分享图导出样例_1080x1440.png` |

## 八、给彬少的分享图模板规范需求（第 5 项待办）

当前版本已可用，但需彬少定稿以下参数。为便于直接替换，逐项给出代码位置：

| 需彬少定的 | 当前临时取值 | 代码位置 |
|---|---|---|
| 主背景色 / 渐变 | `#221826` → `#120d15` → `#0d0a10` | `drawShareImage()` 内 `createLinearGradient` |
| 品牌金色 | `#d79a39`（沿用小程序端 `app.json`） | CSS 变量 `--gold` 与 Canvas 各处 |
| 前三名色 | 金 `#f2c069` / 银 `#cfd4dc` / 铜 `#c98a5e` | CSS `--rank1/2/3` 与 Canvas 名次色条 |
| logo 图形与位置 | **暂缺**，已预留左上 120×120 | `drawShareImage()` 顶部注释处，`drawImage` 一行接入 |
| 标题区排版 | 「红颜局中局」30px + 榜名 76px + 副标题 30px 居中 | `drawShareImage()` 标题区 |
| 出图尺寸 | 1080×1440（3:4），适配微信群与图文投稿 | `<canvas width height>` |

**一条蓝军提醒给彬少**：分享图会被运营发到粉丝群，因此它是**对外物料**，受抖音渠道分界原则约束。若模板上出现平台名、二维码或引流话术，会把「站外自有页面截图」变成「跨平台引流物料」。建议模板**只保留赛事品牌，不加任何平台标识与二维码**。此点需 Vincent 一并确认。
