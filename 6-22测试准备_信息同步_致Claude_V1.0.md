# 6/22 测试准备 · 信息同步（致 Claude）

> 整理：Manus　日期：2026-06-19
> 用途：同步 John 最新决策、域名变更、明天测试边界与待办分工，供 Claude 知悉并把关
> 涉及最新代码 commit：`dc23315`（前端域名变更）

---

## 一、John 的目标与最新条件

**目标**：明天（6/22）上午前在现有条件下完成准备，让明天下午的测试尽量接近真实环境运行。

**最新条件 / 更新信息**：

1. **域名（重大变更）**：John 向运维方借到一个**已通过 ICP 审查的域名**，替代我方仍在备案中的新公司域名，用于测试连公网。经与运维梁工文字确认：**明天小程序要连的后端完整地址 = `https://api.jiurongjs.com`**（即借用域名 `jiurongjs.com` 下的 api 子域）。我方原域名 `hongyanjuzhongju.cn` 仍在备案中。
2. **抖音小店 + 阿奇索**：John 今天申请开通抖音小店、取 API 并挂阿奇索插件，但审核时长不确定。**经评估：明天大概率开不通。**
3. **抖音直播 API**：卡点是"小程序需先发布"，而发布前提是"先完成备案"，**明天必定赶不上。**

---

## 二、明天测试能力边界（已与 John 对齐）

| 能力 | 明天状态 | 说明 |
|---|---|---|
| 真机登录 + 小程序连公网后端 | ✅ 真实 | 已过 ICP 域名 + HTTPS + 后端 prod 真实抖音登录 |
| 卡密核销 + 选手加人气 + 看写真 | ✅ 真实 | 后端逻辑就绪 |
| 阿奇索周边自动发货 | ❌→降级 | 明天开不通，改"人工发卡密 + 小程序核销" |
| 抖音直播 点赞/留言/礼物 真实数据 | ❌→模拟 | 用场控后台「模拟注入」代替，入账逻辑同一套，观感接近 |
| 抖音小店 销售数据 | ❌→模拟/手动 | 依赖小店审核与发布 |

**结论**：明天真实跑「登录 + 连公网后端 + 卡密核销」；直播/小店真实数据用模拟注入代替，不影响演示完整流程。直播 API、小店 API、备案上线均归入**第二阶段**。

---

## 三、为支撑明天测试已做/已产出（Manus）

1. **前端生产域名变更**：`frontend/douyin-miniprogram/config/env.js` 的 `prod.baseURL` 由 `https://hongyanjuzhongju.cn` 改为 `https://api.jiurongjs.com`，已推送 main（commit `dc23315`）。
   - 注意：`request.js` 拼接为 `${baseURL}${url}`，各请求自带 `/api/...`，最终 `https://api.jiurongjs.com/api/...`。
   - **因域名变更，旧体验版（0.0.1）作废，John 需用最新代码重新上传体验版（0.0.2）。**
2. **卡密生成方案**：核查发现后端 `TokenGeneratorService` 有生成逻辑，但**无对外 HTTP/CLI 入口**（仅单测调用过）。为明天可用，Manus 编写脚本 `gen_test_tokens.py`：连库→自动建 3 名测试选手→生成 `RFZJ-XXXX-XXXX-XXXX` 卡密→导出 `tokens_export.txt`。已在真实数据库验证可用。
   - 蓝军点：`tokens.player_id` NOT NULL，故必须先有选手再生成卡密，脚本已处理。
3. **ADMIN_TOKEN / 场控后台澄清**：ADMIN_TOKEN 是运维部署时设定的环境变量（非系统可查值），即场控后台「管理口令」；场控后台 `control-admin` 需运维 build + Nginx 托管后提供访问网址。

---

## 四、待办分工（明天上午前）

### John
- 用最新代码（`dc23315`）**重新上传体验版 0.0.2**，生成新二维码，旧版作废。
- 与运维约定并获取 `ADMIN_TOKEN`，私密下发给场控操作员。
- 跟进小店/阿奇索（预期开不通，走人工发卡密兜底）。

### 运维（梁工）
- 后端以 **prod profile** 部署到 `https://api.jiurongjs.com`，配齐 `DB_*`、`ADMIN_TOKEN`、`DOUYIN_APP_ID`、`DOUYIN_APP_SECRET`。
- 用**最新** `db/db_schema.sql`（已修复严格模式 1067）建库建表。
- 运行 `gen_test_tokens.py` 生成 `tokens_export.txt`。
- 部署场控后台 `control-admin`（建议与后端同域），提供访问网址。
- 确认 `https://api.jiurongjs.com` HTTPS 可达、`/api` 反代后端 8080。

### 测试前 15 分钟自检
1. `GET https://api.jiurongjs.com/actuator/health` → `{"status":"UP"}`；
2. 真机扫 **0.0.2** 二维码 → 登录拿到登录态；
3. 小程序输卡密 → 选手加人气、看写真；
4. 场控后台输管理口令 → 模拟注入 → 看板数字变化。

---

## 五、请 Claude 把关的点

1. 借用域名 `api.jiurongjs.com` 仅用于测试期，是否有合规/后续迁移风险需提示 John？
2. 明天「直播/小店用模拟注入代替真实 API」的降级，是否认可作为本次测试边界？
3. 卡密走"脚本直连数据库生成 + 人工发卡密"的临时方案，是否接受（正式期应补卡密生成的受控入口）？
4. 第二阶段（备案→发布→直播API/小店API真实对接）是否需要现在就立卡排期？
