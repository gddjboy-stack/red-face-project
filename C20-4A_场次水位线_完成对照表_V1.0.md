# C20-4A 场次水位线录入 · 完成对照表 V1.0

| 项目 | 内容 |
| --- | --- |
| 任务卡 | C20-4A 直播互动数据按「当前累计总数」录入 |
| 依据裁定 | 《Claude 补充裁定_C20-4A 场次水位线》 |
| 提交 | 后端 + 建表脚本 + 14 项专项测试 |
| 全量回归 | **142 项全部通过**（本次改动前基线 128） |
| 阻塞项 | 礼物是否纳入水位线（R-2）待 Vincent 裁定；已做成配置开关，两种结果均可一行切换 |

## 一、问题背景

抖音官方直播中控台只提供**本场直播**的点赞、评论、礼物实时累计数，不提供跨场次历史累计。每场开播时三个数字都从 0 重新开始。这意味着运营在后台录入的「当前累计总数」在跨场次时会出现断崖式下降：8/3 收官时点赞水位线是 50,000，8/9 开播后中控台从 0 重新计数，运营录入 3,000 时若直接相减，结果是 `3000 − 50000 = −47000`，**倒扣四万七千人气**。

这个错误在每一场新直播的首次录入时**必然发生**，不是概率问题。Claude 在补充裁定中查证并拦下了这一点，其价值高于本轮任何设计讨论。

## 二、逐项完成情况

| 编号 | 裁定要求 | 状态 | 实现说明 |
| --- | --- | --- | --- |
| 1 | 三条数据来源各自独立水位线 | 完成 | 新建 `live_metric_watermark` 表，`metric_type` 为主键，覆盖 `like_delta`、`comment_delta`、`gift` |
| 2 | 录入改为「当前累计总数」，增量由系统计算 | 完成 | `POST /api/admin/live/metric-entry`，增量 = 当前总数 − 水位线，运营不再手算 |
| 3 | 提供显式场次校准（归零）入口 | 完成 | `POST /api/admin/live/watermarks/calibrate`，三条来源在同一事务内归零 |
| 4 | 负值自动兜底，绝不写入负增量 | 完成 | 当前总数低于水位线时返回 HTTP 409 / 业务码 40910，**不写入任何数据**，人气与水位线原样不动 |
| 5 | 校准全程留痕 | 完成 | `operations_log` 记录归零前原值与 `popularityAffected:false` 标记 |
| 6 | 幂等防连点 | 完成 | 复用既有幂等账本，键前缀 `lme_` |

## 三、超出裁定范围的三项补强

裁定的自动兜底只防「当前总数低于水位线」这一个方向，也就是只防**倒扣**。反方向存在同等量级的风险且**不会触发任何告警**，因此补充了以下三项。

**第一项，误点校准的撤销能力。** 假设直播中途运营误点校准，此时中控台点赞是 80,000，水位线被清成 0，下次录入读到 85,000，系统算出 `85000 − 0 = +85000`，一次性多加八万五。这个结果完全符合「当前总数大于水位线」的正常路径，负值兜底对它毫无作用。因此校准时保留归零前原值，并提供 `POST /api/admin/live/watermarks/revoke-calibration` 恢复。撤销后再录入 85,000 只加 5,000，测试已覆盖该场景。

**第二项，撤销的安全边界。** 校准后若已发生录入，流水已按新周期入账，此时自动恢复水位线会造成账实不符。因此撤销仅在 `entry_count = 0` 时允许，否则明确拒绝并在错误信息中给出归零前原值，供人工冲销核算。

**第三项，并发录入的乐观锁。** 水位线推进语句带 `last_total = 期望值` 条件。两个请求并发提交时只有一个能推进成功，另一个受影响行数为 0，服务层判定冲突并拒绝，避免同一增量被重复入账。人气入账与水位线推进在同一事务内，杜绝「已入账但水位线未推进」的中间态。

## 四、实测暴露的一个设计缺陷及修复

测试中 14 项有 1 项失败，经核查**是实现缺陷而非测试写错**。

场次标识原设计为 `yyyyMMdd-HHmmss` 格式，秒级精度。连续两次校准若发生在同一秒内，会生成完全相同的标识。现场断流重连时，运营极可能在几秒内连点两次校准（第一次没反应就再点一次）。标识一旦碰撞，两段流水在 `metadata` 中无法区分，**「按场次还原入账总额」这一唯一的事后审计手段直接失效**，而这正是引入该标识的全部目的。已改为时间戳加四位随机后缀。

此处顺带说明一项方案调整：原计划为 `popularity_ledger` 新增一列存放场次标识，核查后发现该表**已有 `metadata` JSON 字段**，写入其中即可满足分组还原需求，不改表结构、不影响既有索引。这类「提议本身合理但其实不必要」的情况，是双 AI 互审最容易漏掉的一种——互审通常只检查提议对不对，不检查提议是否必要。

## 五、必须由人执行的配套动作

以下两项属于系统层面无法拦截的风险，需要通过流程和文案解决。

**校准按钮的措辞必须重写。** 「清零水位线」这个说法，运营会理解为「分数清零」。8/9 开播点击后发现选手分数没变，运营会判定系统故障，进而手动调分「修正」——这是系统完全无法识别的数据污染，因为手动调分本身是合法操作。后端已在二次确认相关字段中标注 `popularityAffected:false`，但前端按钮文案建议改为「**开始新一场直播（校准中控台读数）**」，二次确认弹窗必须写明「本操作只重置中控台读数基准，不会改变任何选手的人气值，历史人气完整保留」。

**增量异常阈值告警暂未实现。** 当前只在方向异常时拦截，未对「增量过大」告警。原因是阈值定多少都缺乏依据，8/3 首场实测拿到真实量级后再定，避免上线即误报导致运营养成无视告警的习惯。

## 六、接口清单

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/admin/live/watermarks` | 查询三条来源水位线现状，供运营核对上次录入总数 |
| GET | `/api/admin/live/metric-entry/preview` | 预演录入，返回增量或需校准信号，不写数据 |
| POST | `/api/admin/live/metric-entry` | 按当前累计总数录入并入账 |
| POST | `/api/admin/live/watermarks/calibrate` | 校准全部来源水位线（新场次开播） |
| POST | `/api/admin/live/watermarks/revoke-calibration` | 撤销最近一次校准 |

业务码 `40910` 表示需先校准，已确认与既有错误码（40001–40005、40110、40400、40501、40000、50000）无冲突。

## 七、测试覆盖

14 项专项测试覆盖以下场景：首次录入、同场连续录入只计增量、总数持平不写流水、倒扣防线不写入任何数据、校准后全额入账且历史人气保留、三条来源同时归零、校准留痕含原值、误点撤销后增量正确、已录入拒绝撤销、场次标识分段还原、预演不改状态、礼物开关关闭时明确拒绝、非法输入拒绝、水位线列表查询。
