# 《红颜局中局小程序功能定义文档_V2.5》

**项目名称：** 红颜局中局
**文档版本：** V2.5
**日期：** 2026-06-09
**作者：** Manus AI

---

## 1. 修订说明

| 版本 | 日期 | 修订人 | 修订内容摘要 |
| :--- | :--- | :--- | :--- |
| V2.3 | 2026-06-08 | Manus AI | 新增 H5 中间页逻辑。 |
| V2.4 | 2026-06-08 | Manus AI | 根据阿奇索官方文档 (detail/160) 优化发货变量与 H5 传参规范。 |
| **V2.5** | **2026-06-09** | **Manus AI** | **整合 Claude 最终审查意见，锁定 Java 后端与抖音原生前端技术栈，明确接口契约与防错规范，作为正式开工基准。** |

---

## 2. 核心功能模块与架构

### 2.1 小程序功能点概述 (MVP)

1.  **选手编队功能**：后台支持将参赛选手编为 2~4 个团队。
2.  **多维人气值计算与展示**：
    *   **类型**：团队人气值（新增）、个人人气值、卧底人气值（新增）。
    *   **来源**：免费点赞/留言互动、抖音官方直播间礼物、周边实物（卡密核销）。
    *   **展示**：前端实时展示个人人气、团队人气。
3.  **团队人气值分配**：后台支持将团队人气值自动或手动平分给团队成员，系统记录每次操作。
4.  **付费礼物记录**：后台记录每位选手、团队收到的付费礼物（含直播间礼物和周边）。
5.  **观众投票卧底功能**：后台手动/定时开启/关闭，观众可投票，显示得票情况，后台显示最高得票卧底。
6.  **人气值手动调整**：后台支持手动扣除或增加选手人气值，系统详细记录每次操作。

### 2.2 架构设计原则

*   **单一入口原则**：所有人气值变更通过统一的 `apply_popularity_change` 接口处理。
*   **目标对象中心**：人气值入口支持 `target_type` (`player` | `team` | `spy` | `pool`) 和 `target_id`，实现灵活归因。
*   **只增不改流水账**：所有人气值变动均记录在 `popularity_ledger` 中，确保可追溯性。
*   **幂等性**：通过 `idempotency_key` 确保重复请求不会导致重复计分。
*   **整数运算**：所有人气值计算均采用整数，避免浮点数精度问题。

### 2.3 核心接口契约 (修订自 Claude V1.1)

#### 2.3.1 直播免费互动（点赞/留言）——总增量模型

```
# on_live_metric_delta
metric_type: str,        # "like_delta" | "comment_delta"
delta_value: int,        # 本次窗口新增量
total_value: int,        # 平台侧累计值（可选，用于校验对账）
occurred_at: int,
idempotency_key: str
```
*   **说明**：引擎内部不接收 `target`，自动查询 M-OP 的当前场控目标进行归属。

#### 2.3.2 人气值变更统一入口

```
# apply_popularity_change
target_type: str,         # "player" | "team" | "spy" | "pool"
target_id: int | None,    # pool 可为空；player/team/spy 必填
source: str,              # "gift"|"like"|"comment"|"token"|"manual"|"refund"|"team_distribution"
raw_value: int,           # 整数
occurred_at: int,
idempotency_key: str,
round_id: int | None = None,
operator_id: str | None = None,
reason: str | None = None,
metadata: dict | None = None
```

#### 2.3.3 团队人气值平分

```
# distribute_team_popularity
team_id: int,
round_id: int,
method: str,          # "equal"(平均) | "custom"(自定义比例)
operator_id: str,
reason: str,
custom_weights: dict = None  # method=custom 时, {player_id: weight}
```
*   **说明**：内部仍调用 `apply_popularity_change(source="team_distribution")`，生成分配批次流水。

#### 2.3.4 观众投票卧底

```
# submit_suspicion
user_id: str, round_id: int, choices: list
# choices 结构（按组建模）:
# [
#   {"team_id": 1, "suspect_ids": [101, 102]},
#   {"team_id": 2, "suspect_ids": [201]}
# ]
```

### 2.4 衰减边界规则

| 边界情况 | 规则 |
| :--- | :--- |
| 恰好等于上轮 ×1.5 | **不触发**衰减（仅 `>` 触发） |
| 上轮人气值为 0（或第一轮无上轮） | **不衰减**，设最低基准阈值（如首轮或上轮为0时，本轮全额计分） |
| 退款/手动扣分导致本轮为负 | **积分最低为 0**，负值只记录在流水和审计中，不让最终积分为负 |

---

## 3. 技术栈与开发规范

### 3.1 技术栈最终确定

| 部分 | 技术选择 | 说明 |
|---|---|---|
| **后端** | **Java + Spring Boot** | 第三方确认，Java 后端事实标准，文档多、第三方易接手。 |
| **数据库** | MySQL | 不变。 |
| **ORM** | **MyBatis**（推荐）或 JPA | 推荐 MyBatis：SQL 显式可见，便于第三方审计和排查。 |
| **缓存** | 彩排阶段可选（内存缓存），正式上线前再上 Redis | 降低部署复杂度。 |
| **小程序前端 (M-FE1)** | **抖音原生框架** (ttml/ttss/js/json) | 第三方确认，最直接、层级最少。 |
| **场控后台前端 (M-FE2)** | **Vue3 + Element Plus** (Web) | Web 管理后台标准方案，成熟稳定。 |
| **H5 中间页 (M-FE3)** | Vue 或纯 HTML (Web) | 简单页面，实现灵活。 |

### 3.2 Java 特有防错规范 (Manus 必读)

1.  **数值类型**：人气值用 `long`，系数用 `int`（×100 存储），绝不用 `double`/`float` 参与人气值和金额计算。
2.  **卡密核销原子性**：使用 `@Transactional` + 条件 `UPDATE`（`markUsedIfUnused`），检查影响行数，避免并发问题。
3.  **幂等**：数据库唯一约束 + 捕获 `DuplicateKeyException`，确保重复请求不重复处理。
4.  **依赖注入**：推荐构造器注入，不用字段注入。
5.  **参数封装**：多参数方法封装成 DTO (Data Transfer Object)。

### 3.3 代码注释与文档规范

*   **代码注释**：文件头、函数/方法、复杂逻辑必须有清晰注释；禁止魔法数字，所有常量必须定义为命名常量。
*   **配套文档**：每模块 README、API 契约文档 (OpenAPI/Swagger)、数据库字典、架构总览。
*   **命名规范**：变量/函数命名清晰、语义化，业务术语建立中英对照表。

---

## 4. 阿奇索发货规范对齐 (Aqiso Alignment)

### 4.1 变量占位符规范
*   全系统统一采用阿奇索官方变量格式：`{$卡券信息}`。
*   H5 中间页新增 `oid`（订单号）参数接收能力，用于订单对账。

### 4.2 发货模板标准化
*   明确发货模板需包含：H5 自动引导链接 + 手动卡密兜底。
*   链接格式：`https://hjzj.com/go?t={$卡券信息}&oid={$订单号}`。

---

## 5. 交付计划 (6/14 彩排)

Manus AI 将立即按照以下顺序进行核心模块编码：

1.  数据库 Schema
2.  ★ PopularityService（M-CORE）+ JUnit 单元测试
3.  M-LIVE（含模拟器）
4.  M-TOKEN（卡密核销）
5.  M-OP（场控）
6.  Controller 层（M-API）
7.  前端（小程序原生 + 场控后台 Vue）

每完成一个模块，Manus AI 将对照通用自检清单和 Java 专项自检清单进行检查，并附自检报告提交 GitHub，由 John 转交 Claude 进行审查。

---

## 6. 仍待 John 确认的外部事项

| 编号 | 事项 | 对象 |
| :--- | :--- | :--- |
| 1 | 阿奇索在抖店的变量替换是否正常 | 阿奇索商家 |
| 2 | 直播玩法 API 权限申请进度 | 抖音开放平台 |

---

**报告人**：Manus AI
**日期**：2026年6月9日
