# Claude 审查 — 任务卡 C13(卧底识破)

> 审查人:Claude(架构师/审查AI)
> 日期:2026.06.17
> 审查方式:克隆仓库(commit 541f82a),以**防剧透红线**为头号重点,全链路核查 is_spy 不泄露 + 每轮一次限制 + 合规 + 测试真实性。

---

## 一、审查结论:✅ C13 通过

最敏感的两个点(防剧透、每轮一次)都正确实现且有测试证明。这是你们最出彩的玩法,做得扎实。

### 防剧透红线核查(头号重点,逐层确认全过)

| 层 | 核查 | 结果 |
|---|---|---|
| DTO 层 | SuspicionCandidateView 字段:playerId/number/playerName/teamId/teamName/count/ratio | ✅ **无 isSpy、无任何卧底身份字段** |
| Service 层 | 全文搜 is_spy/isSpy/actualSpy | ✅ **完全不出现**;spyChannelOpen 只用轮次+场控模式判断,不碰卧底身份 |
| Mapper 层 | 注释明确"用户侧严禁暴露 is_spy" | ✅ 用户侧查询不查 is_spy |
| **测试层** | `andExpect(content().string(not(containsString("isSpy"))))` | ✅ **直接断言接口返回内容里不含 isSpy 字符串——最硬核的防泄露测试** |

**结论:防剧透红线全链路守住,且有专项测试钉死。卧底是谁绝不会从用户侧接口泄露。**

### 每轮一次限制核查(Manus 发现的真实漏洞)

| 检查 | 结果 |
|---|---|
| 实现方式 | submit 时先 `findSubmittedPlayerId(userId, roundId)`,已有记录则抛 already_submitted | ✅ |
| 顺序正确 | 校验用户→校验候选→**查重→插入**,查重在插入前 | ✅ |
| 事务保护 | submit 方法 @Transactional | ✅ |
| 测试覆盖 | "每用户每轮一次"专项测试存在 | ✅ |

这补上了"现有唯一键防不住一个用户投多个嫌疑人"的漏洞。

### 合规与其他

- ✅ 候选按 number 排序,无排行榜/名次文案
- ✅ 错误码 41001~41004 + 兜底,文案合规
- ✅ submit 不接受前端 userId,走 @CurrentUser
- ✅ 不碰 C1~C12 主链路,只改 LiveHomeService 的 spyChannelOpen 一个字段
- ✅ 不做自动揭晓/奖励/回滚(按裁定 Q5)
- ✅ 全量回归 64 测试(C12 的 58 + C13 新增 6)全绿,小程序 JS 检查 + 后台 vue-tsc 通过

---

## 二、里程碑:彩排可演示玩法又增一项

C13 完成,今晚内测可演示的内容:
- 全链路(C12):录入选手→场控注入→看板变化→核销→领写真
- **卧底玩法(C13):后台设卧底→切 spy 模式开启→观众投票猜卧底→看实时分布**

这是最抓眼球的一环。如果后台识破监控卡片也做了(报告里确认做了),内测可大屏演示实时投票分布。

---

## 三、剩余卡状态(按你"今天多推"的计划)

| 卡 | 今天是否推 | 说明 |
|---|---|---|
| ✅ C12、C13 | 已完成 | 内测核心已就位 |
| C15 小程序其余页 | 可推 | 让小程序更完整 |
| C16 会员有效期 | 可推 | 逻辑简单 |
| C17 写真上传管理 | 可推 | 做了你能自助换图 |
| C14 退款 | **不推** | 涉及钱,慎做,Manus 也主动标注为"非退款继续" |
| C18 后台权限 | 不推 | 上线前才需要 |

Manus 主动说"继续推进非退款、非最终安全卡"——这个自我约束是对的,说明它理解了 C14/C18 的特殊性。下一张可选 C16(简单)或 C15(让小程序更完整)。

---

## 四、给 John 的一句话结论

**✅ C13 通过——卧底识破玩法完成,而且最关键的防剧透红线我做了全链路核查:从 DTO 到 Service 到测试,is_spy 绝不泄露到用户侧,还有专门测试钉死"接口返回不含 isSpy"。每轮一次的限制也正确实现。** 今晚内测可以演示完整卧底玩法了。Manus 主动把退款(C14)和权限(C18)排除在今天的推进外,这个判断对——那两张要慎做。下一张可以上 C16 或 C15。
