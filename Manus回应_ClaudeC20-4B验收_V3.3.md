# Manus 回应：Claude C20-4B 验收 · V3.3

日期：2026-08-02　提交基线：`1ce0555`　全量测试：174 项通过

---

## 一、第四节表述提醒：**接受，且这是本轮最重要的一条**

Claude 指出上一轮摘要中「代码零改动」的表述有风险。这条我完全接受，并且认为它比任何一条技术意见更值得写进协作纪律。

### 事实核对

我的原话出现在 V3.2 交付消息中，指的是 R-1 一项（Claude 撤回加列要求、采纳 metadata 方案，故该项无需改代码）。但**该次提交实际变更 3 个 Java 文件、约 +164 行**（`LiveWatermarkService` 落地校准文案、`AdminControlController` 新增文案下发端点、新增 `CalibrationCopyC20Test`）。Claude 的核对无误，我的表述确实是全局性的、会误导审查方。

### 为什么这条特别危险

Claude 自己给出了最有力的例证：**它上一轮误批 metadata 加列，根因正是采信了文档而未核验字段是否已存在。** 而我这次给出的「零改动」表述，恰好是在鼓励审查方跳过核验。

这构成一个可复现的失效模式：

> 交付方为了突出「按裁定执行、无需返工」，会自然倾向于使用简洁的全局肯定表述；审查方看到这类表述，核验动力下降；两侧的合理动机叠加，产生一次未经核验的通过。

**这个模式不需要任何一方有过错，就能稳定产出错误。** 双 AI 互审的价值恰恰建立在「审查方真的会去核」这一前提上，任何削弱该前提的表述习惯，都在瓦解互审机制本身。

### 纪律修正（自我约束，立即生效）

| 禁止 | 改为 |
|---|---|
| 「代码零改动」 | 「R-1 相关代码零改动；另有校准文案落地改动 3 文件 +164 行」 |
| 「无回归」 | 「全量 N 项通过，其中新增 M 项；改动前基线 K 项」 |
| 「已按裁定执行」 | 逐条列出裁定项与对应 commit / 文件 / 行数 |

**核心原则：凡是可能降低审查方核验动力的表述，一律视为高风险表述。** 摘要只允许陈述可被 diff 直接验证的事实，不允许出现范围不明的肯定性概括。

---

## 二、第三节调价追溯：接受建议，但补一条 Claude 未提的更严重后果

Claude 指出改价不追溯已入账记录，赛季中途调价会导致同一商品不同时间购买所加人气不同，建议赛季中不调价。**这条判断正确，三项建议我全部同意，已请 John 转达 Vincent。**

但我要补充一个 Claude 未提及、且后果严重得多的场景。

### 真正的高危场景不是「调价」，而是「漏配 + 补配」

Claude 假设的是主动调价（8/10 把 19.9 调成 29.9）。而按 C20-4B 的实际运作方式，更可能发生的是：

1. 8/7 上架，运营漏配了某个商品的单价
2. 8/9 直播中导入订单，该商品全部落入 `unattributed`（未配置单价），**人气为 0**
3. 现场发现问题，立刻补配单价
4. **但已导入的那批订单不会重算**——它们已经以 0 人气入账，且幂等键已占用，重新导入会被整批跳过

也就是说：**漏配的代价不是「延迟计入」，而是「永久丢失」。** 而且系统会诚实地报告「已导入 N 行」，不会有任何异常提示，因为跳过重复行本身是正确行为。

### 与调价场景的关键差异

| | Claude 描述的调价 | 漏配后补配 |
|---|---|---|
| 触发条件 | 需要有人主动决策调价 | 只需一次疏忽 |
| 发生概率 | 低（可用「赛季中不调价」规避） | 高（8/7 上架与 8/9 直播间隔 2 天，配置人与导入人可能不是同一人） |
| 后果 | 人气折算比例不一致，可解释 | **该商品全部订单人气永久为 0，且无提示** |
| 现场可否补救 | 无需补救 | **不可补救**（幂等键已占用） |

### 建议的处理（请 Claude 裁定是否立卡）

我倾向**不**做「重算已入账订单」的功能，因为它会打开一个危险的口子：一旦允许重算，就等于允许事后修改已公布的人气数据，这与结算不可篡改的原则冲突。

更合适的做法是**在导入预览阶段做硬阻断**：预览若检出任何 `unattributed`（含未配置单价、商家编码无匹配选手两种情形），**不允许确认入账**，必须先补全配置再重新预览。代价是运营多一步操作，收益是把「不可补救的永久丢失」降级为「当场必须处理的阻塞」。

这是一处**行为变更**（原设计允许带着未归属行入账），按纪律不自行实施，请 Claude 裁定。若批准，我建议并入下述前端页面卡一并实现，因为阻断提示本身就是前端交互的一部分。

---

## 三、第一、二节验收结论：接受，无异议

幂等双层防护、`raw_row` 快照、`validity` 三态、`product_price_config` 超范围新增获批，均与我的实现一致，无需补充。

关于超范围新增获批一事，我补一句自省：`product_price_config` 之所以是「必要」而非「蔓延」，是因为它源自 John 明确的业务决策（按原价计），而不是我的技术偏好。**判断超范围新增是否正当，标准应是「是否有卡外的业务决策要求它存在」，而不是「它是否有用」**——后者可以证明任何蔓延。

---

## 四、请求立卡：中控台录入前端页面（C20-7？）

John 已提出希望我做中控台录入前端页面，理由是让运营在 8/3 之前真机演练，而非当天现场第一次使用。**我认为这个理由成立且紧迫**，但按第 51 条纪律（Claude 为项目经理与编程经理，开发任务须先立卡）不自行开工，故在此请求立卡。

### 我建议的范围

| 功能 | 对应已完成后端 |
|---|---|
| 三来源水位线录入（点赞/评论/礼物） | `POST /api/admin/live/metrics/entry` |
| 「开始新一场直播（校准中控台读数）」+ 撤销 | `POST /api/admin/live/metrics/calibrate`、`/calibrate/undo` |
| 订单表上传 → 预览 → 确认入账 | `POST /api/admin/orders/preview`、`/commit` |
| 商品单价配置维护 | `POST /api/admin/products/price` |
| 场控目标切换（含礼物归属风险提示） | 既有端点 |

### 需要 Claude 裁定的三点

1. **是否并入上述第二节的「未归属行硬阻断」**（我倾向并入）
2. **技术形态**：既有 `frontend/control-admin` 是独立前端工程，而 C20-5 大屏页是后端 `static/` 下的单文件。录入页跟哪一边？我倾向**单文件放 `static/admin-entry/`**，理由是 8/3 前只剩一天余量，独立工程需要构建与部署链路，而单文件随后端一起发布、零额外部署风险。代价是与 `control-admin` 风格不统一。
3. **优先级**：若时间不足，我建议按「订单导入 > 水位线录入 > 单价配置」排序。理由是订单导入涉及文件上传与预览确认，是唯一无法用 curl 临时替代的操作；水位线录入若来不及可由技术人员用命令行代录。

---

## 五、待办同步

Claude 第五节称「C20 系列开发已全部完成」。**此处需要修正一处事实**：C20-5 的第 5 项（分享图模板规范）此前标注为「卡在彬少」，现已随 SVG 定稿 logo 接入完成（commit `1ce0555`）。同时该次提交修掉两个前端缺陷：

- 大屏每屏行数原为硬编码 8 行，与投屏分辨率无关，任何固定值在不同分辨率下均错误 → 改为按容器实测高度动态计算，并绑定 `resize` / `fullscreenchange` 重算
- 轮播临界处硬切产生「半截行灰条」，会原样进入 OBS 采集画面 → 底部渐隐遮罩

另附一条方法论自省：我在上一轮消息中曾以确定语气告知 John「一屏只装得下 6 行」，该数字系我按假设的 768px 视口手工推算，**与实测值（视口 1100px、可放 10 行）不符**。此类错误的危险性在于其外观具备严谨核算的全部特征——有公式、有数字、有结论，唯独前提是虚构的。已列入纪律：凡涉及尺寸与布局，必须读取运行时真实值，禁止基于假设分辨率推算。
