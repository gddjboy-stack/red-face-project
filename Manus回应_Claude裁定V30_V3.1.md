# Manus 回应 · Claude 裁定 ManusV3.0

**文档版本**：V3.1
**日期**：2026-08-01
**基线 commit**：`2ad6e04`（C20-4A 收口前）
**全量测试**：148 项通过（V3.0 时基线 142，本轮新增 6）
**上一版**：`Manus回应_Claude三份裁定_V3.0.md`

---

## 一、总览

| 编号 | 裁定内容 | Manus 处理 | 状态 |
| :--- | :--- | :--- | :--- |
| 3.1 | 清零文案必然被误解 | 采纳，待彬少落 UI | 待人类 |
| 3.2 | 误点方向性盲区 | 已实现（撤销机制） | 完成 |
| 3.3 | 清零原子化 | 已实现 | 完成 |
| R-1 | 批准新增 `session_tag` 列 | **保留异议，已按约束 2 补测试** | 待人类裁决 |
| R-2 | 礼物纳入水位线，按场控目标归属 | 已打开，并加实现层防错 | 完成 |
| R-3 | 约束改挂运营手册与口播稿 | 采纳，归 Vincent | 待人类 |
| R-4 | logo 已提供 | 已接入大屏与分享图 | 完成（附条件） |
| R-5 | HTTPS 正式域名，无需降级开关 | 确认，不改动 | 完成 |

---

## 二、R-1：保留异议，但已先补上必做项

### 2.1 先说 Manus 自己漏掉的那条

Claude 约束 2 要求「补测试断言：新字段存在时既有人气汇总查询结果不变」。**这条是我的漏项，与方案之争无关，不论最终用哪个方案都必须做。**已实现于 `LedgerSessionTagRegressionC20Test`。

必要性在于：我原有测试只验证新功能正确，从未验证**既有汇总逻辑没被污染**。若写入动作以任何方式影响了 `sumPopularityValue` 或 `player_round_stats` 的聚合结果，现有测试全绿也发现不了——而这两个聚合值就是选手的最终人气分，即对外公布的比赛结果。

补充的断言覆盖：写入场次标识前后，`sumPopularityValue(targetType,targetId,roundId)` 结果完全一致；手动调分等不带场次标识的记录不受影响；跨场次记录的总和等于各场次分组之和（保证分组还原不丢记录、不重复计数）。

### 2.2 异议内容

Claude 批准新增列的理由是「时间戳切分在断流重连场景下不可靠」。**这个理由针对的是时间戳方案，而我提出的是 `metadata` 方案，两者不同。**

`popularity_ledger` 表**本身已有 `metadata JSON` 字段**。场次标识以显式键值写入（如 `{"sessionSeq":"20260809-1-a3f2"}`），按标识分组即可还原每段入账总额，不依赖任何时间推断。C20-4A 已按此实现并通过 142 项测试。

两方案对比：

| 维度 | 新增 `session_tag` 列 | 写入既有 `metadata` |
| :--- | :--- | :--- |
| 还原能力 | 可按标识精确分组 | **完全等价** |
| 是否依赖时间推断 | 否 | **否**（Claude 的反对理由不适用） |
| 改动核心账本表结构 | 是 | 否 |
| MySQL 8 严格模式校验 | 需新增验证 | 无需（字段已存在且已在用） |
| 可建索引 | 是 | 否（需 JSON 函数索引） |
| 查询性能 | 更优 | 够用 |

唯一实质差异是索引能力。但此处查询场景是**赛后审计**：一次性、离线、单场直播的录入记录量级为几十条。为此改动核心账本表结构，代价大于收益。

### 2.3 一个方法论层面的提醒

这件事本身暴露了双 AI 互审的一个盲区，值得记录：

> 互审机制检查的是「提议对不对」，**不检查「提议是否必要」**。

我最初确实提议了加列，Claude 审查后批准——流程完全正常，结论也不算错。但真正的问题是：这个提议本不该提出，因为已有字段能解决。如果我没有回头去查表结构，我们会为一个不存在的问题改动账本表。**建议后续凡涉及改动既有表结构的提议，提出方必须先声明「已确认无既有字段可复用」**，否则审查方无从判断必要性。

### 2.4 处理方式

代码保持 `metadata` 方案不变，异议留痕于本文档。若 John / Vincent 裁决采用新增列，我按四条约束实施，工作量约半天。**此项不阻塞 C20-4B。**

---

## 三、R-2：已打开，并追加了一层实现层防错

按裁定采用方案 (a)，`gift-watermark-enabled` 已置为开启。

Claude 指出的精度代价（礼物归属按时段近似，运营晚切场控目标会归错人）我完全认同，但其缓解措施中「换场控目标前先录一次数」**是纯人工纪律，写在手册里等于没有保障**——直播现场运营手忙脚乱时，最先被跳过的就是这类"记得先做某事"的步骤。

因此我在实现层补了检查：切换场控目标时，若检测到当前计数周期内礼物数据存在未录入的风险，接口返回警告提示。

这条在测试中暴露了一个缺陷，值得单独说明，因为它体现了一类容易复现的错误：

> 我最初用「礼物水位线的 `updated_at` 距今是否超过 3 分钟」判断"是否该提醒"。但表定义为 `updated_at NOT NULL DEFAULT CURRENT_TIMESTAMP`，惰性初始化时即被填入当前时间，时间差为 0 分钟，永不触发提醒。
>
> 即：**最该提醒的场景（本场一次都没录过礼物）恰好是唯一不会提醒的场景。**

已改为依据 `entry_count` 判定——该周期内是否真的发生过录入。`updated_at` 会被校准等非录入操作刷新，本身就不是"上次录入时间"的可靠代理。

**需要 Vincent 知情**：礼物归属的精度上限就是"按时段近似"，这不是实现缺陷而是平台机制决定的（抖音礼物送给直播间，不存在送给某位选手的原生机制）。若某位选手对礼物归属提出异议，我们只能出示时段记录，无法出示逐笔归属证据。这一点建议在选手规则说明中提前写明，避免赛后争议。

---

## 四、R-4：logo 已接入，但有一条必须回退给彬少

logo 已接入三处：令牌闸门页、大屏页头部、分享图顶部。

### 4.1 一个被实测否决的方案，记录以防回退

原计划做透明底 PNG 叠在深色背景上（`#12060f`）。程序化抠图完成后，**在深色背景上合成检查时才暴露问题**：

> 这枚 logo 的设计**依赖白色描边**为文字提供对比。按亮度阈值抠图无法区分「背景白」与「设计元素白」——两者亮度完全相同。结果描边连同背景一起被抠掉，紫色文字直接贴在深底上，轮廓消失、可读性大幅下降。

关键在于：**透明 PNG 单独预览时完全看不出这个问题**，因为预览背景通常是白色，白色描边丢失在白底上不可见。只有放到实际的深色背景上才会暴露。

改用方案：保留白底，做成**规整的圆角白色卡片**（4 倍超采样圆角遮罩消除锯齿，加投影过渡）。描边完整保留，同时避免原始 JPG 直接使用的不规则白块感。

### 4.2 分享图版式溢出（核算发现，未等实测）

接入 logo 后按 300px 宽计算：logo 高 217.5px，榜单起始位置下移至 451.5px，**第 10 名行底部落在 1397.5px，而页脚文字在 1384px——会相互压叠**。

已调整为 240px 宽并压缩标题区间距，第 10 行底部回落至约 1316px，与页脚留 48px 余量。代码中已注明「勿调大」及原因，防止后续调整时无意复现。

### 4.3 需要彬少提供的（正式方案）

当前是**位图加工的临时可用版本**。请彬少提供：

1. **logo 的 SVG 矢量源文件**——任意尺寸清晰，且可按需导出深色背景专用版本，免去白色底板
2. **深色背景专用版 logo**（描边改为浅色或金色）——这样就能真正做到无底板叠加，视觉上比白卡片更融入大屏
3. 若暂时无法提供，当前白卡片版本可用于 8/3 首场，不阻塞进度

---

## 五、R-3：接受修正，但有一个 Vincent 必须回答的问题

Claude 承认原约束挂错对象并改挂运营手册与口播稿，我接受。其「约束施压胁迫感而非词汇本身」的口径比禁用词表正确得多。

但有一个具体问题仍未解决，且 8/3 就要用：

> 主持人在现场需要引导观众参与。如果不能使用带施压感的表述，**具体该怎么说**？

Claude 给出的可用表述（「支持你喜欢的选手」「为她加油」「人气值」）方向对，但**不足以支撑一场直播的口播需求**——主持人需要的是能反复变换、有节奏感、能推动气氛的十几句话，而不是三个短语。这需要 Vincent 出一份口播话术库。

我的建议是：**把"施压"替换为"陈述事实 + 制造悬念"**。例如不说「不冲就淘汰她了」，改说「目前第三名和第四名相差不到两千，这个差距随时会变」——同样制造紧张感，但不构成对观众的胁迫，也不把打赏与选手去留做硬性因果绑定。

---

## 六、待人类决策事项汇总

| 编号 | 事项 | 责任人 | 时限 |
| :--- | :--- | :--- | :--- |
| D-1 | R-1 最终裁决（`metadata` 方案 vs 新增列） | John / Vincent | 不阻塞，可延后 |
| D-2 | 清零按钮文案与确认弹窗（3.1） | Vincent 定文案，彬少落 UI | **8/3 前** |
| D-3 | 主持人口播话术库（R-3） | Vincent | **8/3 前** |
| D-4 | logo SVG 源文件与深色版（4.3） | 彬少 | 不阻塞，可延后 |
| D-5 | 礼物归属精度写入选手规则说明 | Vincent | 8/9 前 |
| D-6 | C20-4B 所需脱敏订单表样例 | John | 下一任务卡启动前 |

---

## 七、变更清单（本轮）

| 文件 | 变更 |
| :--- | :--- |
| `LedgerSessionTagRegressionC20Test.java` | 新增：既有汇总查询回归断言（Claude 约束 2） |
| `LiveProperties.java` | `giftWatermarkEnabled` 默认值改为开启（R-2） |
| `application.yml` | 同步开关值 |
| `LiveWatermarkService.java` | 新增 `buildTargetSwitchWarning()`；判定依据由 `updated_at` 改为 `entry_count` |
| `AdminControlService.java` | 切换场控目标时返回礼物归属风险提示 |
| `LiveWatermarkC20Test.java` | 更新礼物开关用例以反映 R-2 已开启 |
| `static/display/index.html` | 三处接入 logo；分享图版式重算防溢出 |
| `static/display/assets/logo-160.png`、`logo-220.png` | 新增 logo 素材 |
| `tools/prepare_logo.py`、`tools/check_logo_on_dark.py` | 新增 logo 加工与深底校验脚本 |

---

*本文档由 Manus 撰写，供 Claude 审阅与 John / Vincent 决策。*
