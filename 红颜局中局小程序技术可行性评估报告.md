# 《红颜局中局》小程序技术可行性评估报告

## 1. 核心功能实现路径

针对《红颜局中局》小程序的三个核心技术需求，我进行了深度调研，结论如下：

| 需求项 | 技术实现路径 | 官方支持情况 | 结论 |
| :--- | :--- | :--- | :--- |
| **抖店订单查询** | 通过抖店开放平台 `订单API` (/order/list) 获取订单列表。 | **官方支持**：提供 OpenID 转换接口，可精准匹配小程序用户与买家。 | **可行** |
| **直播间送礼抓取** | 接入抖音开放平台 `直播玩法 (Live Interaction)` 服务端 API。 | **官方支持**：通过 Webhook/WebSocket 实时推送礼物数据。 | **可行** |
| **用户身份打通** | 使用 `UnionID` 或官方 `OpenID 转换接口` 实现跨应用身份识别。 | **官方支持**：支持小程序、抖音号、抖店三方身份统一。 | **可行** |

## 2. 关键技术细节分析

### 2.1 抖店订单关联 (序号 009)
*   **实现机制**：当用户在小程序内进入“购买记录”页面时，后台会调用抖店开放平台的订单查询接口。通过官方提供的身份转换工具，系统能自动识别出当前小程序用户在抖店购买的明信片/会员包订单。
*   **前提条件**：John 需要在注册公司后，同时在抖音开放平台和抖店开放平台完成开发者认证，并确保两个平台的主体信息一致。

### 2.2 直播数据同步的稳定性 (序号 011)
*   **推送质量**：抖音官方提供秒级推送时效，并具备去重机制（通过 `msg_id`）。
*   **对账机制**：针对可能存在的极少数丢包情况，官方提供了“分页查询推送失败数据”接口。我们可以利用此接口进行数据对账，确保选手的“人气值”计算公平、无遗漏。

### 2.3 身份识别与助力加分
*   **自动加分**：用户在小黄车下单后，小程序后台会实时捕获订单状态。一旦订单状态更新（如支付成功），系统将根据订单中的 SKU 信息（对应哪位选手），自动为该选手增加相应的人气助力值。人气值在订单状态变为“支付成功”后立即生效。

## 3. 风险预警与应对

### 3.1 直播礼物接口权限申请
*   **风险**：抖音直播玩法服务端 API（礼物推送）属于互动玩法类权限，并非所有完成企业认证的开发者都能直接申请，需要额外的服务商资质审核或特定类别的业务场景认证。该审核流程和时间不确定，是“链路 A”（礼物换算人气值）的唯一技术基础。
*   **应对**：
    *   将“提交直播礼物推送接口权限申请”列为最高优先级的前置任务，在公司注册完成后立即启动（目标 6/8）。
    *   报告应明确列出此接口的申请条件（企业认证类型、业务场景说明要求等）。
    *   规划备用方案：若接口申请未在 6/10 前获批，考虑在 6/11 排练时使用人工记录替代，或暂时只开放明信片购买的人气值通道。

### 3.2 高并发承载
*   **风险**：在直播间爆火时，数据推送频率会极高。
*   **应对**：我将在代码实现中加入**消息队列 (Message Queue)** 机制，确保高并发下的数据处理平稳。

## 4. 结论

**《红颜局中局》小程序的所有核心功能在抖音官方技术框架内均有成熟的实现路径。** 我们无需担心“数据拿不到”或“接口不开放”的问题，重点应放在业务逻辑的严密性和 UI 的极致还原上。

## 5. 后端技术选型

为支撑《红颜局中局》小程序的各项功能，建议采用以下后端技术架构：

*   **后端语言/框架**：
    *   **Python + FastAPI**：Python 在数据处理和 AI 领域有广泛应用，生态成熟。FastAPI 性能高，易于开发和维护，适合构建高性能 API 服务。
*   **数据库**：
    *   **MySQL**：关系型数据库，适合存储订单、用户信息、人气值等结构化数据，保证数据一致性和可靠性。
    *   **Redis**：内存数据库，用于缓存实时人气榜单、会话管理。在 MVP 阶段，**Redis Streams** 将作为消息队列处理直播间高并发数据，避免引入新的组件。
*   **部署方案**：
    *   **云服务商**：初步考虑**字节跳动火山引擎 (VolcEngine)**。但需注意，该选择尚未得到官方文档的特殊集成优势证实。团队可根据实际报价和服务支持质量，在火山引擎/阿里云/腾讯云之间自由选择，不存在技术层面的强制约束。具体服务包括：云服务器 (ECS)、数据库服务 (RDS)、缓存服务 (Redis)。
*   **消息队列**：
    *   **MVP 阶段采用 Redis Streams**：用于处理直播间高并发数据（礼物、弹幕）的异步处理，确保数据不丢失，并实现削峰填谷。待项目规模验证后（第二季或更大规模），再评估是否升级到 Kafka/RabbitMQ。
