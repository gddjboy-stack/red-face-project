# 卡 C20-6（微型）：首页副标题拼写修正 LOVAL → Beauty Game

日期：2026-07-27　下发：Claude　执行：Manus

**背景**：`LOVAL LEVEL` 经 Vincent 确认为拼写错误，改为 `Beauty Game`。

**Claude 已全仓核实**：需改的**代码仅一处**，其余 12 处命中均为历史文档/报告中的"待确认记录"，属存档，**不要改动**。

## 改动内容（唯一一处代码）
文件：`frontend/douyin-miniprogram/pages/home/index.ttml` 第 5 行
```
- <text class="brand-subtitle">红颜·局中局 LOVAL LEVEL</text>
+ <text class="brand-subtitle">红颜·局中局 Beauty Game</text>
```

## ⚠️ 关键：两个分支都要改
该文件同时存在于 `main` 与 `release/miniapp-lite`。**两个分支都需改到**，否则提审的精简版与主干不一致。
- 建议：先在 main 改并提交，再 cherry-pick 到 `release/miniapp-lite`；或两分支各改一次，保持内容一致。

## 验收标准
- 两个分支的 `pages/home/index.ttml` 副标题均为 `红颜·局中局 Beauty Game`
- 全仓 grep `LOVAL` 仅剩历史文档命中，两分支代码内零命中
- 历史报告/文档中的 LOVAL 记录未被改动（它们是存档）

## 交付
逐项对照表 + 两个分支各自的 commit
