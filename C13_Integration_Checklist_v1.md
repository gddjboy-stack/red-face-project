# C13「卧底识破」联调清单 v1

> 作者：Manus AI  
> 日期：2026-06-17  
> 范围：C13 用户侧最小闭环 + 后台监控卡片  
> 状态：开发环境已验证，待内测与 Claude 审查。

## 〇、验证原则

本清单依据 Claude 对 C13 方案的可编码裁定编写。C13 的第一红线是**防剧透**，用户侧任何接口和页面都不能暴露 `isSpy` 或真实卧底身份；第二红线是**合规表达**，用户侧使用“识破/判断/线索/真相”包装，按选手序号展示，不做排行。[1]

## 一、后端 API 验证

| 序号 | 验证项 | 方法 | 预期结果 | 当前状态 |
|---|---|---|---|---|
| BE-01 | 未开启不能提交 | `collect_state.mode != spy` 时 POST `/api/suspicion/submit` | 返回 `41001 not_open` | 开发环境已验证 |
| BE-02 | 非法候选拒绝 | 提交不属于当前轮的 `suspectPlayerId` | 返回 `41002 invalid_candidate` | 开发环境已验证 |
| BE-03 | 已淘汰选手排除 | `player_status='eliminated'` 后提交 | 返回 `41002 invalid_candidate` | 开发环境已验证 |
| BE-04 | 每用户每轮一次 | 同一 Bearer token 提交两次不同候选 | 第二次返回 `41003 already_submitted`，数据库只新增一条 | 开发环境已验证 |
| BE-05 | 分布统计正确 | 两个用户分别提交两个候选 | status 返回两个候选 count 均为 1 | 开发环境已验证 |
| BE-06 | 防剧透 | 检查 status 响应体 | 不含 `isSpy`、`actualSpyPlayerId` | 开发环境已验证 |
| BE-07 | spyChannelOpen 开关 | `collect_state.mode=spy` 与非 spy 分别 GET `/api/live/home` | spy 时 true，非 spy 时 false | 开发环境已验证 |
| BE-08 | 后端全量回归 | `mvn clean test` | 64 tests，0 failures，0 errors | 开发环境已验证 |

## 二、小程序验证

| 序号 | 验证项 | 方法 | 预期结果 | 当前状态 |
|---|---|---|---|---|
| MP-01 | 页面注册 | 检查 `app.json` | 包含 `pages/suspicion/index` | 开发环境已验证 |
| MP-02 | 首页未开启提示 | `spyChannelOpen=false` 点击首页入口 | Toast：“该环节暂未开启” | 代码级验证已完成，待开发者工具截图 |
| MP-03 | 首页开启跳转 | `spyChannelOpen=true` 点击首页入口 | 跳转 `/pages/suspicion/index?roundId=...` | 代码级验证已完成，待开发者工具截图 |
| MP-04 | 候选按序号排列 | status API 返回候选 | 页面按后端顺序展示，不做前端排序 | 代码级验证已完成 |
| MP-05 | 提交后置灰 | 用户提交一次判断 | 按钮显示“本轮已提交”并置灰 | 代码级验证已完成，待开发者工具截图 |
| MP-06 | 错误码文案 | 模拟 41001~41004 | 页面显示固定文案 | 代码级验证已完成 |
| MP-07 | 用户侧无敏感文案 | 静态 grep | 不含“第几名/领先/冲榜/投票/打赏” | 开发环境已验证 |

## 三、后台验证

| 序号 | 验证项 | 方法 | 预期结果 | 当前状态 |
|---|---|---|---|---|
| AD-01 | API 封装 | `getSuspicionStatus(roundId)` | 请求 `/api/admin/suspicion/status` | 开发环境已验证 |
| AD-02 | 监控卡片展示 | 场控监控页 | 显示状态、轮次、候选分布 | 类型检查通过，待浏览器截图 |
| AD-03 | 不展示真实卧底 | 检查后台卡片字段 | 只展示序号、选手、队伍、判断次数 | 开发环境已验证 |
| AD-04 | 后台类型检查 | `pnpm exec vue-tsc --noEmit` | 通过 | 开发环境已验证 |

## 四、内测执行脚本

| 步骤 | 操作 | 通过标准 |
|---|---|---|
| 1 | 在基础数据页设置当前轮次、队伍、选手与卧底 | 后台“分队与卧底设置”可保存 |
| 2 | 在场控操作中切换模式为 `spy` | 首页刷新后“真相识破”入口显示进行中 |
| 3 | 用用户 A 进入小程序 C13 页面并提交候选 1 | 提交成功，按钮置灰 |
| 4 | 用用户 A 再提交候选 2 | 系统拒绝，提示本轮已提交 |
| 5 | 用用户 B 提交候选 2 | 成功提交 |
| 6 | 刷新后台监控卡片 | 分布按序号展示，不显示真实卧底身份 |
| 7 | 主持人口播揭晓 | 系统不自动揭晓、不自动奖励、不自动淘汰 |

## 五、引用

[1]: docs/c13/Claude_Ruling_C13_Spy_Recognition_Approved.md "Claude 裁定 — C13 卧底识破方案确认（可编码）"
