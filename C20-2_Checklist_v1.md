# C20-2 逐项完成对照表 v1

日期：2026-07-29　执行：Manus　分支：`release/miniapp-lite`（承接 C20-1 @ 234f875）　卡片：C20-2（修正版）人气值清除 + 文案清理 + 自定义分享

## 〇、执行前差异与超范围声明（按执行纪律第 3 条）

| # | 事项 | 说明 | 处理 |
|---|---|---|---|
| D1 | 「点赞」「评论」「抖币」在上传包内本来就零命中 | 盘点确认，无需清理 | grep 结果如实记录 |
| D2 | home 页 `support-row`（"当前支持对象 / 实时数据更新"）未列入卡集 | 两个 metric-box 删除后该标题行悬空，且"支持对象"一词有应援色彩 | **超范围 2 行**：改为"信息更新时间 + {{updatedAtText}}"，保留数据刷新时间展示。已在本表明写 |
| D3 | home 页第 25 行"当前互动计入：{{targetDisplayName}}"未列入卡集 | "计入"+选手名暗示数据计入选手，与稳妥档"不暴露榜单机制"精神冲突 | **超范围 1 行**：改为"活动进行中，敬请关注现场互动。"（else 分支同步改"活动开始后信息将自动更新。"）。已在本表明写 |
| D4 | redeem-success 页"选手 / 团队"两行信息未列入卡集（卡集只点名第 8、13 行） | 这两行展示"为谁核销"，与红线"不得提及任何选手与分数"直接冲突 | **超范围 2 行**：随第 13 行一并删除，页面只保留写真与会员权益。已在本表明写 |
| D5 | README.md:7 "人气看板" | 验收标准注明 README 不需改，但该行描述已与精简版现状不符 | 顺手更新为精简版准确描述（开发文档准确性），非清理义务项 |
| D6 | C 部分新增 `onShareAppMessage` 是功能性新增，而 release 分支纪律为"只做减法" | 卡集明确将 C 部分下发在 `release/miniapp-lite` 分支执行，卡集指令优先 | 按卡集在 release 分支实现；main 是否需要同步该功能（完整版也应避免默认分享标题），留待 Claude 在 C20-3 前裁定 |

## 一、A 部分：人气值清除（5 页）

| # | 页面 | 卡集要求 | 状态 | 实际改动 |
|---|---|---|---|---|
| A1 | `pages/redeem-success/` | 第 8 行、第 13 行改为只陈述用户自身权益 | ✅ | ttml：第 8 行改"您的专属权益已到账。"；选手/团队/增加人气值三行信息删除（见 D4），替换为"数字写真：已存入我的收藏"；js：`playerText`/`pointsText` 字段、赋值及 `formatNumber` 引用全部清除 |
| A2 | `pages/players/` | 说明文案 + valueText 展示 | ✅ | ttml：第 9 行改"选手按序号展示，点击可查看选手介绍与写真。"；`{{item.valueText}}` 展示行删除，仅留 编号/姓名/队伍/写真/查看；js：`valueText` 映射与 `formatNumber` 引用删除；ttss：`.player-value` 孤儿样式删除 |
| A3 | `pages/player-detail/` | "当前人气值"行、数字、supportHint | ✅ | ttml：`metric-row` 整块（15~18 行）与 `support-hint`（19 行）删除；js：`popularityText` 字段、赋值与 `formatNumber` 引用删除；ttss：`.metric-row`、`.support-hint` 孤儿样式删除 |
| A4 | `pages/home/` | 第 35、39 行数字展示及 js 逻辑（含 `'--'` 分支） | ✅ | ttml：两个 metric-box 整块删除，support-row 改造（见 D2）；js：`targetPopularityText`/`teamPopularityText` 字段、赋值（含 C19-R3b-3 的 `'--'` 分支）与 `formatNumber` 引用全部删除；ttss/app.ttss：`.metric-box`、`.muted-box`、`.number-strong`（全工程已无使用方）孤儿样式删除 |
| A5 | `pages/me/`（Claude 补充） | 第 23、28 行两处人气文案 | ✅ | 第 23 行改"核销成功后可收藏专属数字写真"；第 28 行改"购买请在活动现场完成。页面仅展示已获得的用户权益。" |

**红线自查**：全部替换文案只陈述用户自身权益，未使用"应援 / 助力 / 支持值 / 热度"任何替代词（附加 grep 验证零命中）。

## 二、B 部分：文案清理

| # | 位置 | 原文 → 终态 | 状态 |
|---|---|---|---|
| B1 | `home/index.ttml:21` | "正在直播 · X / 当前暂无直播互动" → "活动进行中 · X / 当前暂无进行中的活动" | ✅ |
| B2 | `home/index.ttml:27` | "* 增加人气值/留言请在直播间进行" → "* 相关互动请在活动现场参与" | ✅ |
| B3 | `home/index.ttml:25` | "当前互动计入：X" → "活动进行中，敬请关注现场互动。"（见 D3） | ✅ |
| B4 | `utils/constants.js:10`（错误码 40005） | "当前暂无可计入的直播轮次…" → "当前暂无进行中的活动轮次…" | ✅ |
| B5 | `app.json` 各页面标题 | 盘点确认：首页/选手/选手详情/卡密核销/核销成功/我的/我的写真——均无违规词 | ✅ 无需改 |
| B6 | 全部 toast 与错误提示 | 盘点确认：各页 toast（登录失败/数据获取失败/获取选手失败/缺少选手信息等）均无违规词 | ✅ 无需改 |
| B7 | 其余页面（redeem、my-photos） | 盘点确认零命中 | ✅ 无需改 |

## 三、C 部分：自定义分享

新增 `utils/share.js`（统一分享配置，单一出口便于日后改文案），并为**全部 7 个页面**挂载 `onShareAppMessage`（每页 +4 行，含 require）：

> 标题：**红颜·局中局 | 活动信息与粉丝权益**
> 描述：**查看活动动态，管理你的专属权益与数字写真**
> 落地路径：`/pages/home/index`

标题与描述不含投票/人气/直播字样，符合"活动名称 + 粉丝权益工具"定位。挂载全部 7 页而非仅首页的原因：抖音小程序分享入口按页面生效，任一页面未定义时将回退默认标题，全挂避免漏洞。全部 js 文件已通过 `node --check` 语法校验（10/10 通过）。

## 四、验收标准自检

| 验收标准 | 自检结果 |
|---|---|
| 六组 grep 用户可见部分零命中 | ✅ `popularity` / `Popularity` / `人气` / `直播` / `点赞` / `评论` 在上传包目录内**全部零命中**（README 已顺手更新故亦零命中）；附加自查 `投票/打榜/应援/助力/热度/支持值/抖币/suspicion/spy` 同样零命中。完整输出见 `C20-2_grep_result.txt` |
| 核销成功页只讲用户自身权益 | ✅ 页面仅展示：权益到账提示、写真预览、"数字写真已存入收藏"、会员天数与有效期 |
| 分享卡片标题为自定义内容 | ✅ 7/7 页面挂载统一自定义分享 |

## 五、改动统计与截图说明

18 个文件修改 + 1 个新增（`utils/share.js`），合计 +47/−70 行，净减 23 行。沙箱无抖音开发者工具无法产生运行截图，以各页面代码终态替代；请 John 在开发者工具中拉取本分支后重点核验四个页面的视觉终态：首页（活动卡片无数字）、选手列表（右侧仅"查看"）、选手详情（无人气行）、核销成功页（无选手信息），并转发一张任意页面的分享卡片预览图供 Claude 归档。

## 六、蓝军备注（两条，供 Claude 裁定）

其一，**后端 `LiveHomeResponse` 仍在向精简版下发 `targetPopularity`、`teamPopularity`、`targetDisplayName` 等字段**。前端已不展示，功能无碍，但送审版本的网络响应中仍携带榜单数据——若审核人员抓包或使用调试工具（概率低但非零），存在被识别的理论风险。是否需要后端为精简版增加字段裁剪（属 main 分支后端改动，需单独拆卡），请 Claude 权衡"改动风险 vs 暴露风险"后裁定；我的倾向是 8/1 首审不动后端（时间紧、抓包概率低），中期迭代再收敛。

其二，D6 所述：`onShareAppMessage` 是否要同步进 main（完整版分享同样会回退默认标题），建议并入下一张涉及 main 的卡一并处理，不单独拆卡。
