# C15「小程序其余页」联调清单 v1

> 作者：Manus AI  
> 日期：2026-06-18  
> 用途：供 John、Claude 与后续开发者工具/真机验收使用。

## 一、已在本地完成的验证

| 编号 | 验证项 | 当前结果 |
|---|---|---|
| C15-BE-01 | `GET /api/players` 返回选手列表且包含 `playerId` | 已通过测试 |
| C15-BE-02 | 选手列表按序号展示，不按人气值排序 | 已通过测试与静态检查 |
| C15-BE-03 | `GET /api/players/{playerId}` 返回基础资料、人气和写真 | 已通过测试 |
| C15-BE-04 | 不存在选手返回安全业务错误 | 已通过测试 |
| C15-BE-05 | 选手详情响应不含卧底身份字段 | 已通过测试与静态扫描 |
| C15-FE-01 | 小程序新增 `pages/player-detail/index` | 已注册且 JSON/JS 检查通过 |
| C15-FE-02 | 小程序新增 `pages/me/index` 并作为“我的”Tab | 已注册且 JSON/JS 检查通过 |
| C15-FE-03 | `pages/my-photos/index` 从 Tab 退为子页 | 已完成，核销成功页跳转同步调整 |
| C15-FE-04 | 小程序敏感词扫描 | 未发现禁止词 |
| C15-REG-01 | 后端全量回归 `mvn test` | 81 tests，0 failures，0 errors |

## 二、开发者工具验收建议

| 编号 | 页面 | 验收点 |
|---|---|---|
| C15-DEV-01 | 选手列表 | 能看到选手序号、姓名、队伍、人气值和清新图片预览；点击卡片进入详情 |
| C15-DEV-02 | 选手详情 | 只展示基础资料、人气、写真预览和“增加人气值请在直播间进行”提示 |
| C15-DEV-03 | 选手详情 | 页面不出现卧底身份、动态、关注、距离、相关视频、周边等内容 |
| C15-DEV-04 | 我的首页 | 底部“我的”进入 `pages/me/index`，展示会员状态、写真数量和核销入口 |
| C15-DEV-05 | 我的写真 | 从我的首页进入写真列表；有/无收藏两态正常 |
| C15-DEV-06 | 核销成功页 | 点击“查看我的写真”能进入子页而不是报 Tab 跳转错误 |
| C15-DEV-07 | 真相识破 | 入口和状态文案与 C13 逻辑一致，不重做投票流程 |

## 三、接口联调建议

| 编号 | 接口 | 期望结果 |
|---|---|---|
| C15-API-01 | `GET /api/players` | 返回 `{ code:0, data:{ roundId, roundName, items } }` |
| C15-API-02 | `GET /api/players?roundId=1` | 返回指定轮次的选手列表，items 含 `playerId` |
| C15-API-03 | `GET /api/players/{playerId}?roundId=1` | 返回详情和 `photos[]`，不含卧底身份字段 |
| C15-API-04 | `GET /api/players/999?roundId=1` | 返回安全业务错误，不泄露内部异常 |
| C15-API-05 | `GET /api/me/photos` | 我的首页可复用 `total + membership` |

## 四、不得在 C15 联调中要求的事项

| 禁止项 | 原因 | 归属 |
|---|---|---|
| 退款入口/退款状态 | 涉及资金与幂等回滚 | C14 |
| 订单查询 | 依赖 Agiso/订单系统 | T9 或后续订单卡 |
| 会员群入口 | 缺少运营和平台能力确认 | 后续运营卡 |
| 直播间跳转/小黄车 | 依赖真实抖音环境 | 6/22 真实环境验证后决定 |
| 写真上传/替换 | 用户端只展示，后台管理另做 | C17 |
| 后台权限 | 与用户端页面无关 | C18 |
| 卧底身份展示 | 会破坏 C13 防剧透边界 | 禁止 |
| 动态/关注/距离/相关视频/周边 | 社交模板残留且无后端数据 | 不建议做 |

## 五、待 John 确认事项

| 事项 | 当前状态 | 建议 |
|---|---|---|
| `LOVAL LEVEL` | C15 编码阶段未修改 | 请 John 询问 Vincent/彬少：若为笔误，再改为 `LOYAL LEVEL` 或中文副标题；若为品牌词，则保留 |

## 六、Claude 复审建议

Claude 复审时建议重点检查：`PlayerController` 是否确实只读且未污染 API-2；`PlayerDetailResponse` 与查询 SQL 是否不含卧底身份字段；小程序 `players/player-detail/me` 页面是否没有敏感词和社交模板残留；`my-photos` 退为子页后跳转链路是否合理；全量测试是否保持通过。
