# C15「小程序其余页」技术方案 v1（提交 Claude 审批版）

> 作者：Manus AI  
> 日期：2026-06-18  
> 当前状态：仅技术方案，**未编码**  
> 前置状态：C1~C13、C16 已完成；内测批次一 T1/T2/T3 已完成；Claude 已审查确认 C16 通过。[1] [2]

## 〇、方案结论

C15 建议定义为**用户端小程序剩余页面补齐卡**，核心目标是把 C11 彩排阶段刻意收敛的“选手 Tab 兼容页”和“极简我的写真页”升级为正式内测可用的用户端页面结构。C15 不应重新开发已完成的核销、人气看板、卧底识破业务逻辑，也不应提前吞入 C14 退款、C17 写真上传、C18 后台权限或 6/22 真实环境联调事项。

> C15 的蓝军原则是：**补页面闭环，不膨胀业务系统；补展示和导航，不承诺无后端、无运营、无平台能力支撑的功能。**

| 结论项 | Manus 建议 | 原因 |
|---|---|---|
| C15 主目标 | 完整选手列表/详情 + 我的完整页 + 用户端状态 polish | C11 明确把完整选手详情和我的完整页留给 C15。[3] [4] |
| 后端 API | 新增只读 `PlayerController`，不污染 API-2 人气看板 | API-2 面向看板，不适合作为详情契约。 |
| 我的页 | 新增 `pages/me/index` 作为底部“我的”Tab 首页，`my-photos` 退为子页 | C16 已给 `/api/me/photos` 增加会员摘要，可复用形成“我的”首页。 |
| 真相识破 | 不重做 C13，只做入口和状态文案一致性检查 | C13 已完成，避免返工。 |
| 禁止范围 | 不做订单查询、退款、会员群、周边购买、关注、距离、动态、相关视频 | 这些模块无当前后端/平台能力，且存在范围反弹和合规风险。[5] |

## 一、背景与现状审计

C11 已经完成首页、人气看板、卡密核销、核销成功、极简我的写真，并为了底部 Tab 兼容新增 `pages/players/index`。但 C11 实施报告明确说明：当前 `pages/players/index` **不是完整选手详情页**，只是复用 API-2 player 看板数据的兼容页；完整选手详情归属 C15。[4]

与此同时，C16 已完成会员有效期后端逻辑和小程序展示：核销成功页展示会员 +7 天，我的写真页展示会员有效期。Claude 对 C16 的审查结论为通过，确认同事务叠加、并发 +14、additive 字段和范围边界均正确。[1] 因此，C15 可以复用 C16 已有 `/api/me/photos` 中的独立 `membership` 字段组，不需要另起会员系统。

| 页面/模块 | 当前状态 | C15 判断 |
|---|---|---|
| 首页 `pages/home` | 已接 API-1，保留真相识破入口和卡密核销入口 | 不重做，只做文案和入口一致性清理 |
| 选手 `pages/players` | 仅是 player 看板别名，显示列表和当前人气 | C15 应升级为正式选手列表入口 |
| 人气 `pages/popularity` | 已完成 player/team/spy tab，按后端顺序展示 | 不纳入 C15 主开发 |
| 真相识破 `pages/suspicion` | C13 后已具备状态、候选、提交和分布展示 | 不重做业务逻辑，仅补状态体验 |
| 核销 `pages/redeem` | 已完成剪贴板合规、错误五态和跳转 | 不纳入 C15 |
| 核销成功 `pages/redeem-success` | C16 已补会员有效期展示 | 不纳入 C15 主开发 |
| 我的写真 `pages/my-photos` | C11 极简列表 + C16 会员摘要 | C15 应将其作为“我的”子页保留 |

## 二、C15 建议纳入范围

### 2.1 选手列表与选手详情

C15 应把现有“选手”Tab 从“人气看板别名”升级为正式选手列表页。列表仍保持合规表达，不显示名次，不按人气排序，不使用“打榜、榜首、应援”等敏感词。列表点击后进入新页面 `pages/player-detail/index`，详情页展示基本资料、队伍、人气值、清新写真预览和“支持请在直播间完成”的克制提示。

| 页面 | 路由 | 数据来源 | 展示内容 |
|---|---|---|---|
| 选手列表 | `pages/players/index` | 新增 `GET /api/players?roundId=...` | 序号、姓名、队伍、当前人气、头像/首张写真预览 |
| 选手详情 | `pages/player-detail/index` | 新增 `GET /api/players/{playerId}?roundId=...` | 基本信息、队伍、人气、写真预览、直播间支持提示 |

选手详情不得恢复设计稿中的“个人动态、关注、距离、相关视频、购买周边、跳转能力待确认”等模块。早前 API/UI 覆盖核查已明确指出这些内容没有独立后端契约，且部分属于社交模板残留或平台能力未确认事项。[5]

### 2.2 我的完整页

C15 建议新增 `pages/me/index`，并把底部 Tab 的“我的”从 `pages/my-photos/index` 改为 `pages/me/index`。新的“我的”首页不做复杂会员中心，只做三个稳定入口：会员有效期摘要、我的写真数量与入口、卡密核销入口。原 `pages/my-photos/index` 保留为“我的写真”子页，继续展示 C11/C16 已完成的写真列表与会员摘要。

| 模块 | 数据来源 | C15 展示方式 |
|---|---|---|
| 会员状态 | 复用 `GET /api/me/photos` 的 `membership` 独立字段组 | 显示“会员有效期至 / 暂未开通会员” |
| 写真数量 | 复用 `GET /api/me/photos.total` | 显示“已收藏 X 张写真”，点击进入 `my-photos` |
| 卡密核销 | 现有 `pages/redeem/index` | 提供入口，不重复实现核销逻辑 |
| 登录态 | 现有 `ensureLogin()` | 进入我的页时确保登录 |

这里不建议新增 `/api/me/summary`，因为当前 `GET /api/me/photos` 已经包含 `total/items/membership`，C15 可以先复用。未来如果 Claude 认为我的首页不应拉取完整写真列表，可以另开轻量 `GET /api/me/summary`，但这会增加本卡工作量。

### 2.3 真相识破入口与状态 polish

C13 已完成用户端真相识破页，因此 C15 不应重新设计玩法和投票规则。C15 只建议做轻量体验整理：保证首页入口、未开启态、已提交态、已结束态和错误提示文案一致；若 `spyChannelOpen=false`，入口继续灰置并提示“该环节暂未开启”。这属于 polish，不应新增后端业务。

| 状态 | 期望展示 |
|---|---|
| 未开启 | 首页入口灰置；进入页后显示未开启说明 |
| 已开启未提交 | 展示候选人并允许提交一次 |
| 已提交 | 显示已提交标签和当前识破分布 |
| 已结束/不可提交 | 显示结果或结束提示，不允许重复提交 |

### 2.4 首页遗留文案与入口一致性

C11 实施报告记录首页顶部 `LOVAL LEVEL` 仍按设计稿保留，待 Vincent/彬少确认是否为品牌造词或 `LOYAL` 笔误。[4] C15 可以把此项纳入“上线前 UI 文案清理”，但不应在未经 Vincent/彬少确认时擅自改品牌词。

| 遗留项 | Manus 建议 |
|---|---|
| `LOVAL LEVEL` | 提交 Claude/John 确认：若不是品牌造词，C15 改为 `LOYAL LEVEL` 或中文副标题 |
| 首页四入口 | 继续隐藏，不恢复“直播排期/联动合作/活动推荐/联系我们” |
| 本周精选推送 | 可保留静态展示，或改为更中性的“直播互动提示” |

## 三、C15 明确不做范围

C15 的最大风险是“其余页”四个字被理解为把所有 UI 设计稿功能一次性补完。蓝军判断这会严重膨胀范围，并把 C14/C17/C18/T7-T9 的复杂度提前带入。C15 应明确定义为页面补齐卡，而不是商业化、运营后台或平台能力卡。

| 不纳入 C15 | 理由 | 后续归属 |
|---|---|---|
| 订单查询 | 依赖 Agiso/订单系统与真实环境 | T9 或后续订单卡 |
| 退款入口/退款状态 | 涉及钱和幂等回滚 | C14 |
| 会员群入口 | 无后端与运营承接，且涉及外链/社群能力 | 运营确认后另开卡 |
| 写真上传/替换 | 当前只读展示，上传管理归后台 | C17 |
| 后台权限 | 与用户端页面无关 | C18 |
| 直播间/小黄车跳转 | 依赖抖音平台能力和直播商品链路 | 6/22 真实环境联调后决定 |
| 关注、距离、个人动态、相关视频 | 社交模板残留，无数据来源且可能触发审核风险 | 不建议做 |
| 卧底身份展示 | 会剧透玩法，破坏 C13 防剧透边界 | 禁止 |

## 四、后端 API 方案

### 4.1 新增 API-6：选手列表

建议新增 `GET /api/players?roundId=1`。该接口是只读页面级 API，专供 C15 选手列表页使用。它不应替代 API-2 人气看板，也不应改变 API-2 的排序和字段语义。

```http
GET /api/players?roundId=1
```

```json
{
  "roundId": 1,
  "roundName": "第1轮",
  "items": [
    {
      "playerId": 1,
      "number": 3,
      "name": "林夏",
      "teamId": 10,
      "teamName": "A组",
      "popularityValue": 125800,
      "photoPreviewUrl": "https://..."
    }
  ]
}
```

| 字段 | 说明 |
|---|---|
| `playerId` | 详情页路由使用的稳定主键 |
| `number/name/teamName` | 用户可读基础信息 |
| `popularityValue` | 当前轮次个人人气值，展示用，不排序 |
| `photoPreviewUrl` | 首张清新写真预览；无图时前端使用占位图 |

### 4.2 新增 API-7：选手详情

建议新增 `GET /api/players/{playerId}?roundId=1`。详情页返回基本信息、人气值、写真预览列表和克制的提示文案。接口不得返回 `isSpy` 或任何真实卧底身份字段，避免破坏 C13 的防剧透设计。

```http
GET /api/players/1?roundId=1
```

```json
{
  "playerId": 1,
  "number": 3,
  "name": "林夏",
  "teamId": 10,
  "teamName": "A组",
  "popularityValue": 125800,
  "photos": [
    { "assetId": "photo_p3_0001", "previewUrl": "https://..." }
  ],
  "supportHint": "增加人气值请在直播间进行。"
}
```

| 字段 | C15 设计原则 |
|---|---|
| `photos` | 只读展示 `photo_assets`，不上传、不替换 |
| `supportHint` | 后端可返回，也可前端固定；不得承诺小黄车或直播跳转 |
| 不返回 `isSpy` | 防止用户端提前知道卧底身份 |
| 不返回动态/关注/距离 | 无数据源，不做 |

### 4.3 后端类与测试建议

| 类/文件 | 职责 |
|---|---|
| `PlayerController` | 暴露 `/api/players` 和 `/api/players/{playerId}` |
| `PlayerQueryService` | 组装选手列表与详情 DTO |
| `PlayerQueryMapper` 或扩展 `C9QueryMapper` | 查询 players、player_round、teams、player_round_stats、photo_assets |
| `PlayerListResponse`、`PlayerListItem`、`PlayerDetailResponse`、`PlayerPhotoItem` | C15 DTO |
| `PlayerControllerC15Test` | 覆盖列表、详情、无图降级字段、无卧底身份泄露 |

## 五、小程序前端方案

### 5.1 页面结构调整

| 页面 | 动作 | 说明 |
|---|---|---|
| `app.json` | 新增 `pages/player-detail/index`、`pages/me/index`；tabBar “我的”指向 `pages/me/index` | 保留 `my-photos` 作为子页 |
| `pages/players/index` | 改为正式选手列表 | 点击选手进入详情 |
| `pages/player-detail/index` | 新增详情页 | 展示基础信息、人气、写真 |
| `pages/me/index` | 新增我的首页 | 展示会员、写真数量、核销入口 |
| `pages/my-photos/index` | 保留 | 从“我的”首页进入 |
| `pages/suspicion/index` | 只做轻量状态文案 polish | 不改投票规则 |

### 5.2 选手列表交互

选手列表页应默认使用当前 active 轮次；如果后端列表接口允许 `roundId` 参数，则前端可以沿用当前轮次 ID 输入，但正式体验更建议隐藏技术型 `roundId` 输入，由后端默认取当前 active 轮次。若保留轮次输入，应仅作为调试态，不应让普通用户理解轮次 ID。

| 当前 C11 页面 | C15 建议 |
|---|---|
| 顶部“彩排版说明” | 删除，改为正式页面说明 |
| 手动输入轮次 ID | 建议改为“当前轮次”只读；调试输入仅开发环境保留 |
| 列表只显示 number/name/team/value | 增加图片预览和进入详情 |
| 无详情跳转 | 点击卡片进入 `player-detail` |

### 5.3 我的首页交互

“我的”首页应避免变成营销中心。它的任务是给用户一个稳定的个人状态入口：看会员有效期、看已收藏写真、去核销卡密。订单查询、会员群、退款入口、社群入口均不显示，避免承诺无法兑现的能力。

| 区块 | 展示内容 | 交互 |
|---|---|---|
| 会员卡片 | 会员有效期至 / 暂未开通会员 | 无复杂权益解释 |
| 写真卡片 | 已收藏 X 张 | 跳转 `my-photos` |
| 核销入口 | “核销明信片卡密” | 跳转 `redeem` |
| 帮助提示 | “购买与增加人气请在直播间完成” | 不提供外链 |

## 六、测试与验收方案

C15 应同时提供后端 API 测试和小程序静态验证。由于抖音真机视觉仍依赖开发者工具，代码阶段不能声称“真机已验证”。

| 验证类别 | 建议测试 |
|---|---|
| 后端 API | `PlayerControllerC15Test`：列表返回 playerId；详情返回照片；不存在选手返回安全错误；响应不包含 `isSpy` |
| 回归测试 | `mvn test` 全量通过，不退化 C13/C16 |
| 小程序静态检查 | JSON 可解析、JS `node --check`、tabBar 路由存在、无红线敏感词 |
| 合规检查 | 不出现“打榜/排名/榜首/应援/PK/打赏/距离/关注”等风险词 |
| 防剧透检查 | 用户端详情页不展示卧底身份字段 |
| 人工验收 | 开发者工具截图：选手列表、选手详情、我的首页、我的写真、有/无会员两态 |

## 七、蓝军风险清单

| 风险 | 影响 | 控制建议 |
|---|---|---|
| 把 C15 做成完整商业化中心 | 范围失控，拖入订单、退款、会员群、直播跳转 | C15 只做页面闭环和只读展示 |
| 选手详情恢复社交模板 | 审核风险与无数据源风险 | 删除关注、距离、动态、相关视频 |
| 详情页泄露卧底身份 | 破坏 C13 玩法 | API 不返回 `isSpy`，测试断言响应无该字段 |
| 复用 API-2 导致看板契约膨胀 | 影响人气页稳定 | 新增独立只读 Player API |
| 我的页拉取写真列表过重 | 性能浪费 | C15 先复用；若 Claude 要求，再改 `GET /api/me/summary` |
| `LOVAL LEVEL` 擅自改品牌词 | 与设计/策划冲突 | 提交 John/Vincent/彬少确认后再改 |

## 八、提交 Claude 裁定的问题

| 编号 | 问题 | Manus 建议 |
|---|---|---|
| C15-Q1 | 是否批准 C15 新增只读 `PlayerController`，提供 `/api/players` 与 `/api/players/{playerId}`？ | 批准，避免污染 API-2 人气看板契约 |
| C15-Q2 | 是否批准 `pages/players/index` 从 C11 的看板别名升级为正式选手列表页？ | 批准 |
| C15-Q3 | 是否批准新增 `pages/player-detail/index`，但仅展示基础资料、人气和写真，不做动态/关注/距离/相关视频/周边？ | 必须批准，防止范围反弹 |
| C15-Q4 | 是否批准新增 `pages/me/index` 作为“我的”Tab 首页，`pages/my-photos/index` 退为子页？ | 批准，这是“我的完整页”的最小闭环 |
| C15-Q5 | 我的首页是否先复用 `/api/me/photos` 的 `total + membership`，暂不新增 `/api/me/summary`？ | 建议批准，减少后端工作量 |
| C15-Q6 | C15 是否只做真相识破入口/状态 polish，不重做 C13 提交逻辑？ | 建议批准 |
| C15-Q7 | 首页 `LOVAL LEVEL` 是否在 C15 改为 `LOYAL LEVEL` 或中文副标题？ | 建议 John 让 Vincent/彬少确认；若无品牌解释则修正 |
| C15-Q8 | 是否明确 C15 不做订单查询、退款、会员群、直播间跳转、写真上传和后台权限？ | 必须批准，分别留给 C14/C17/C18/T7-T9 |

## 九、预期交付物

如 Claude 批准，本卡编码阶段建议交付以下内容。C15 应延续 C13/C16 的流程：先审批方案，再编码，再生成实施报告与联调清单。

| 交付物 | 说明 |
|---|---|
| 后端只读 Player API | `PlayerController`、QueryService、Mapper、DTO、测试 |
| 小程序选手列表/详情 | `players` 升级、`player-detail` 新增 |
| 小程序我的首页 | `me` 新增，tabBar 指向 `me`，`my-photos` 保留为子页 |
| 状态 polish | 首页/真相识破/我的页文案一致性 |
| 测试 | `PlayerControllerC15Test`、小程序静态验证、全量回归 |
| 文档 | `C15_Implementation_Report_v1.md` 与 `C15_Integration_Checklist_v1.md` |

## 十、参考来源

[1]: Claude审查_C16通过.md "Claude 审查 — 任务卡 C16 通过（用户附件，2026-06-18）"  
[2]: C16_Implementation_Report_v1.md "C16「会员有效期」实施报告 v1"  
[3]: C11_Douyin_MiniProgram_Approved_Delta_v1.md "C11 抖音小程序前端裁定更新说明 v1：C15 小程序其余页边界"  
[4]: C11_Douyin_MiniProgram_Implementation_Report_v1.md "C11 抖音小程序前端实施报告 v1：players 仅为兼容页，完整详情归 C15"  
[5]: docs/API契约/API契约复审_UI覆盖核查记录_V1.0.md "API/UI 覆盖核查记录：选手详情、我的页与无后端模块边界"  
[6]: docs/API契约/当前页面级API契约定稿_C9依据_V1.0.md "当前页面级 API 契约定稿：API-0~API-5 与 UI 修改清单"  
